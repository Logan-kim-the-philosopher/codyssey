{
  "version": 1,
  "code": "E1-2",
  "key": "e1-2",
  "title": "파이썬 이해하기",
  "runId": "e1-2-20260804-055226Z",
  "status": "exported",
  "createdAt": "2026-08-04T05:52:26.266Z",
  "updatedAt": "2026-08-06T13:23:07.630Z",
  "mission": "분야\n입학연수\n구분\n개발 입문\n학습시간\n40시간\n컴퓨터에게 명령 내리는 말(파이썬) 처음 배우기\n문제기술\n기술적 설명\n⁠⁠⁠⁠⁠⁠⁠\n\n1. 미션 소개\n프로그래밍 언어를 배우는 가장 확실한 방법은, 동작하는 프로그램을 처음부터 끝까지 직접 만들어 보는 것입니다.\n\n문법을 아는 것과 그 문법으로 하나의 프로그램을 완성하는 것은 전혀 다른 경험입니다.\n\n이 미션에서는 그 차이를 직접 확인합니다.\n\nPython과 Git은 \"내가 만든 프로그램이 왜 이렇게 동작하는지\"를 설명하고, 그 과정을 기록하며 성장하기 위한 핵심 도구입니다.\n\n이 미션에서는 터미널에서 동작하는 나만의 퀴즈 게임을 처음부터 끝까지 구현합니다.\n\nPython 기본 문법을 사용해 입력/출력 흐름을 만들고, 클래스(객체 지향)로 코드를 역할별로 구조화합니다.\n\n아울러 JSON 파일 저장을 통해 프로그램을 종료해도 퀴즈와 점수가 유지되도록 \"데이터 영속성\"을 경험합니다.\n\n또한, Git으로 변경 이력을 관리합니다.\n\n기능 단위로 커밋하고, 브랜치를 나눠 작업한 뒤 병합하며, GitHub에 저장소를 공개합니다.\n\n이 경험은 이후 팀 협업(브랜치 전략, PR, 코드 리뷰)으로 자연스럽게 확장됩니다.\n\n2. 최종 결과물\n다음 3가지 조건을 만족하는 Python 콘솔 프로그램 1개와 GitHub 저장소 1개를 완성한다.\n\n동작하는 퀴즈 게임\n프로그램 실행 시 메뉴에서 번호를 선택하면, 선택 결과에 따라 퀴즈 출제/등록/목록/점수 확인/종료 화면이 출력된다.\n퀴즈 풀기, 퀴즈 추가, 퀴즈 목록, 점수 확인 기능이 동작한다.\n본인이 선택한 주제의 퀴즈가 5개 이상 포함되어 있다.\n프로그램을 종료하고 다시 실행해도 추가한 퀴즈와 최고 점수가 유지된다. (파일 저장)\n코드 구조\n최소 2개 이상의 클래스가 정의되어 있다. (예: Quiz, QuizGame)\n기능별로 메서드가 분리되어 있다. (입력 처리/게임 진행/저장 로직 등)\n데이터는 프로젝트 루트의 state.json에 UTF-8 인코딩으로 저장하고 불러온다.\nGitHub 저장소\n프로젝트 코드가 GitHub에 업로드되어 있다.\n최소 10개 이상의 의미 있는 커밋이 존재한다.\n최소 1회 이상의 브랜치 생성 및 병합(checkout, merge) 기록이 있다.\nclone과 pull을 각각 1회 이상 사용한 기록이 있다.\nREADME.md에 아래 항목이 포함되어 있다.\n프로젝트 개요\n퀴즈 주제 선정 이유\n실행 방법\n기능 목록\n파일 구조\n데이터 파일 설명(state.json 등)\n3. 과제 목표\n이 과제를 마친 후, 학습자는 아래를 스스로 설명할 수 있어야 한다.\n\nPython 기초\n\n변수가 무엇이고, 왜 사용하는지 설명할 수 있다.\nint, str, bool, list, dict의 차이를 설명할 수 있다.\nif/elif/else로 조건에 따라 다른 동작을 수행할 수 있다.\nfor와 while의 차이를 설명하고 적절히 선택할 수 있다.\n함수를 정의하고, 매개변수와 반환값을 활용할 수 있다.\n클래스와 객체\n\n클래스가 무엇이고, 왜 사용하는지 설명할 수 있다.\n__init__ 메서드와 self의 역할을 설명할 수 있다.\n클래스의 속성(attribute)과 메서드(method)를 정의하고 활용할 수 있다.\n파일 입출력\n\n파일을 열고, 읽고, 쓰는 기본 과정을 설명할 수 있다.\nJSON 형식이 무엇이고, 왜 데이터 저장에 사용하는지 설명할 수 있다.2\ntry/except를 사용하여 오류를 처리할 수 있다.\nGit 기초\n\nGit이 무엇이고 왜 필요한지 설명할 수 있다.\ninit, add, commit, push, pull, checkout, clone이 각각 무엇을 하는지 설명할 수 있다.\n브랜치를 생성하고 병합할 수 있다.\n원격 저장소를 clone하고, pull로 변경사항을 가져올 수 있다.\n4. 기능 요구 사항\n다음 요구사항을 모두 만족해야 한다. 구현 방법과 코드 구조는 스스로 설계한다.\n\nGit 저장소 설정\nGitHub에 새 저장소를 생성한다.\n로컬에 저장소를 설정한다.\n.gitignore, README.md 파일을 생성한다.\nGit: 초기 설정 완료 후 첫 번째 commit과 push를 수행한다.\n메뉴 기능\n프로그램 실행 시 메뉴가 출력되어야 한다.\n사용자가 기능을 선택할 수 있어야 한다.\n종료 기능이 있어야 한다.\n잘못된 입력에 대한 처리가 있어야 한다.\nGit: 메뉴 기능 완성 후 커밋한다.\n공통 입력/예외 처리 기준 (최소 요구)\n숫자 입력이 필요한 곳에서 다음 최소 케이스를 처리해야 한다.\n입력 앞뒤 공백 제거 후 처리한다. (예: 1)\n숫자 변환 실패(예: abc) 시 안내 메시지 출력 후 재입력 흐름으로 복귀한다.\n허용 범위 밖 숫자(예: 메뉴 9, 정답 0) 입력 시 안내 메시지 출력 후 재입력 흐름으로 복귀한다.\n빈 입력(그냥 Enter)인 경우 안내 메시지 출력 후 재입력 흐름으로 복귀한다.\n프로그램 실행 중 Ctrl+C(KeyboardInterrupt) 또는 입력 스트림 종료(EOFError)가 발생해도 “비정상 종료”하지 않도록 처리한다.\n안내 메시지를 출력하고, 가능한 범위에서 저장 후 안전하게 종료한다.\n데이터 파일이 없거나 손상된 경우에도 프로그램이 실행 가능해야 한다.\n파일이 없으면 기본 퀴즈 데이터를 사용한다.\n파일이 손상되었으면 안내 메시지를 출력하고, 기본 퀴즈 데이터로 복구(또는 초기화)한다.\nQuiz 클래스\n개별 퀴즈를 표현하는 Quiz 클래스를 정의한다.\n속성: 문제(question), 선택지(choices), 정답(answer)\n선택지는 4개를 기본으로 한다.\n정답은 1~4 중 하나의 번호로 관리하는 방식을 권장한다. (구현에서 일관성 유지)\n메서드: 퀴즈 출력, 정답 확인 등 필요한 기능을 구현한다.\nGit: Quiz 클래스 작성 후 커밋한다.\n기본 퀴즈 데이터\n본인이 선택한 주제의 퀴즈 5개 이상을 직접 작성한다.\n각 퀴즈는 문제, 선택지(4개), 정답을 포함한다.\nQuiz 클래스의 인스턴스로 퀴즈를 생성한다.\nGit: 기본 퀴즈 데이터 작성 후 커밋한다.\n퀴즈 풀기 (브랜치 활용)\nGit: main 브랜치 이외의 추가 브랜치를 생성하고 작업한다.\n저장된 퀴즈를 출제한다.\n사용자가 정답을 입력할 수 있다.\n정답/오답 여부를 알려준다.\n모든 문제를 풀면 결과를 표시한다.\n퀴즈가 없는 경우를 처리한다.\nGit: 기능 완성 후 커밋하고 main 브랜치로 병합한다.\n퀴즈 추가\n새로운 퀴즈를 등록할 수 있다.\n필요한 정보(문제, 선택지 4개, 정답 번호)를 입력받는다.\n잘못된 입력에 대한 처리가 있어야 한다. (공통 입력/예외 처리 기준 포함)\n추가한 퀴즈를 파일에 저장한다.\nGit: 퀴즈 추가 기능 완성 후 커밋한다.\n퀴즈 목록\n저장된 퀴즈 목록을 확인할 수 있다.\n퀴즈가 없는 경우를 처리한다.\nGit: 퀴즈 목록 기능 완성 후 커밋한다.\n점수 확인\n최고 점수를 확인할 수 있다.\n퀴즈를 풀 때마다 최고 점수와 비교하고 갱신한다.\n최고 점수를 파일에 저장한다.\n아직 퀴즈를 풀지 않은 경우를 처리한다.\nGit: 점수 기능 완성 후 커밋한다.\nQuizGame 클래스\n* 게임 전체를 관리하는 QuizGame 클래스를 정의한다.\n* 속성: 퀴즈 목록, 최고 점수 등\n* 메서드: 메뉴 표시, 퀴즈 풀기, 퀴즈 추가, 목록 보기, 점수 확인, 파일 저장/불러오기 등\n* Git: 클래스 구조 정리 후 커밋한다.\n파일 저장/불러오기 (**state.json**)\n* 퀴즈 데이터와 최고 점수를 프로젝트 루트의 state.json에 저장하는 기능을 구현한다. (UTF-8 인코딩 권장)\n* JSON 파일에서 데이터를 불러오는 기능을 구현한다.\n* 파일이 없는 경우(첫 실행) 기본 퀴즈 데이터를 사용한다.\n* 파일이 손상되었거나 읽기/쓰기 오류가 발생한 경우를 try/except로 처리한다.\n* 최소 스키마 예시(데이터 형태 참고; 키 이름/구조는 일관되게 유지):\n* quizzes: 퀴즈 목록\n* best_score: 최고 점수(또는 최고 정답 수 등)\n* Git: 파일 입출력 기능 완성 후 커밋한다.\nREADME.md 작성\n* README.md에 아래 항목이 포함되어야 한다.\n* 프로젝트 개요\n* 퀴즈 주제와 선정 이유\n* 실행 방법\n* 기능 목록\n* 파일 구조\n* 데이터 파일 설명(state.json 경로/역할/스키마)\n* Git: README 작성 후 최종 푸시한다.\nGit 저장소 복제 실습\n* 이 단계는 clone과 pull 명령어를 자연스럽게 경험하기 위한 실습이다. 퀴즈 게임 개발이 완료된 후 아래 절차를 순서대로 수행한다.\n* 미션 수행 저장소를 clone하여 별도의 로컬 디렉터리에 복제한다.\n* 복제된 저장소에서 간단한 변경(예: README에 한 줄 추가)을 하고 commit → push한다.\n* 기존 로컬 작업 디렉터리에서 pull로 변경사항을 가져온다.\n* pull 받은 내용이 정상적으로 반영되었는지 확인한다.\n5. 보너스 과제 (선택)\n랜덤 출제\n퀴즈 풀기 시 문제 순서를 랜덤하게 섞는다.\nrandom 모듈 사용법을 스스로 학습한다.\n문제 수 선택\n퀴즈 풀기 시 몇 문제를 풀지 선택할 수 있다.\n힌트 기능\nQuiz 클래스에 힌트 속성을 추가한다.\n풀이 중 힌트를 볼 수 있다.\n힌트 사용 시 점수 차감 로직을 구현한다.\n퀴즈 삭제 기능\n등록된 퀴즈를 삭제할 수 있다.\n삭제 후 파일에 반영한다.\n점수 기록 히스토리\n최고 점수뿐 아니라 모든 게임 기록을 저장한다.\n날짜/시간, 푼 문제 수, 점수를 기록한다.\n개발환경\n6. 개발 환경\nPython 3.10 이상을 사용해야 한다.\n외부 라이브러리 없이 기본 문법만 사용해야 한다. (표준 라이브러리 사용 가능)\n제약조건\n7. 제약 사항\n데이터 저장 규칙\n데이터 파일은 프로젝트 루트의 state.json을 기본으로 한다.\n파일 인코딩은 UTF-8을 권장한다.\n코드 구조\n모든 코드를 한 함수에 작성하지 않고, 기능별로 함수를 분리해야 한다.\n최소 2개 이상의 클래스로 역할을 분리해야 한다.\nGit 워크플로우\n최소 10개 이상의 의미 있는 커밋이 있어야 한다.\n기능 단위 커밋(메뉴/Quiz/플레이/추가/저장/README 등) + 커밋 메시지에 변경 요약 포함\n형식적인 커밋 메시지를 피하고, 아래처럼 작업 내용을 드러내는 형식을 권장한다.\nFeat: 퀴즈 출제 기능 구현\nFix: 점수 계산 오류 수정\nDocs: README 실행 방법 추가\nRefactor: QuizGame 책임 분리\nGit 기초 명령어 7종(init, add, commit, push, pull, checkout, clone)을 각각 한 번 이상 사용해야 한다.\n제출물\nGitHub 저장소 URL\n개발 환경 설정 스크린샷(예: VSCode, Python 버전, Git 설정)\n프로그램 실행 결과 스크린샷(퀴즈 추가, 목록, 플레이, 점수)\ngit log --oneline --graph 결과 스크린샷\nTest Case\n8. 결과 예시\n아래는 정답이 아니라 참고 예시다. 실제 문구와 디자인은 달라도 된다.\n\n메뉴 화면(예시)\n\n    ========================================\n            🎯 나만의 퀴즈 게임 🎯\n    ========================================\n    1. 퀴즈 풀기\n    2. 퀴즈 추가\n    3. 퀴즈 목록\n    4. 점수 확인\n    5. 종료\n    ========================================\n    선택:\n\n퀴즈 풀기(예시)\n\n    선택: 1\n\n    📝 퀴즈를 시작합니다! (총 5문제)\n\n    ----------------------------------------\n    [문제 1]\n    마블 시네마틱 유니버스에서 타노스가 모은 인피니티 스톤의 개수는?\n\n    1. 4개\n    2. 5개\n    3. 6개\n    4. 7개\n\n    정답 입력: 3\n    ✅ 정답입니다!\n\n    ----------------------------------------\n    ... (중략) ...\n\n    ========================================\n    🏆 결과: 5문제 중 4문제 정답! (80점)\n    🎉 새로운 최고 점수입니다!\n    ========================================\n\n퀴즈 추가(예시)\n\n선택: 2\n\n📌 새로운 퀴즈를 추가합니다.\n\n문제를 입력하세요: 영화 '기생충'의 감독은?\n선택지 1: 박찬욱\n선택지 2: 봉준호\n선택지 3: 김기덕\n선택지 4: 이창동\n정답 번호 (1-4): 2\n\n✅ 퀴즈가 추가되었습니다!\n\n퀴즈 목록(예시)\n\n    선택: 3\n\n    📋 등록된 퀴즈 목록 (총 6개)\n\n    ----------------------------------------\n    [1] 마블 시네마틱 유니버스에서 타노스가 모은 인피니티 스톤의 개수는?\n    [2] 영화 '인터스텔라'에서 주인공이 방문하지 않은 행성은?\n    ...\n    ----------------------------------------\n\n점수 확인(예시)\n\n    선택: 4\n\n    🏆 최고 점수: 80점 (5문제 중 4문제 정답)\n\n잘못된 입력 처리(예시)\n\n    선택: abc\n    ⚠️ 잘못된 입력입니다. 1-5 사이의 숫자를 입력하세요.\n\n    선택: 9\n    ⚠️ 잘못된 입력입니다. 1-5 사이의 숫자를 입력하세요.\n\n프로그램 재시작 후 데이터 유지 확인(예시)\n\n    ========================================\n            🎯 나만의 퀴즈 게임 🎯\n    ========================================\n    📂 저장된 데이터를 불러왔습니다. (퀴즈 6개, 최고점수 80점)\n    ========================================\n    1. 퀴즈 풀기\n    2. 퀴즈 추가\n    ...\n\nstate.json 예시 (데이터 형태 참고)\n\n{\n    \"quizzes\": [\n        {\n            \"question\": \"Python의 창시자는?\",\n            \"choices\": [\"Guido\", \"Linus\", \"Bjarne\", \"James\"],\n            \"answer\": 1\n        }\n    ],\n    \"best_score\": 3\n}\n\nREADME 제출 체크리스트\n프로젝트 개요\n퀴즈 주제 선정 이유\n실행 방법 (예: python main.py)\n기능 목록(퀴즈 풀기/추가/목록/점수)\n파일 구조\n데이터 파일 설명(state.json 경로/역할/필드 구조)\n실행 화면 스크린샷 구성 예시(학습자 캡처)\ndocs/screenshots/menu.png\ndocs/screenshots/play.png\ndocs/screenshots/add_quiz.png\ndocs/screenshots/score.png",
  "checklist": "",
  "plan": {
    "summary": "Python 콘솔 퀴즈 게임을 구현하고, 클래스/객체, JSON 저장, Git/GitHub 제출 흐름까지 단계별로 실습한다.",
    "requirements": [
      "Python 3.10 이상 환경과 과제 전용 Git 저장소를 준비한다.",
      "메뉴 출력, 입력 검증, 안전 종료 흐름을 구현한다.",
      "Quiz 클래스와 기본 퀴즈 데이터를 만든다.",
      "QuizGame 클래스로 메뉴/플레이/추가/목록/점수/저장 책임을 분리한다.",
      "state.json 저장/불러오기와 파일 없음/손상 복구를 처리한다.",
      "퀴즈 플레이, 정답 판정, 결과 표시를 구현한다.",
      "퀴즈 추가/목록/삭제/점수 히스토리를 구현한다.",
      "기능 단위 commit, branch/checkout/merge 기록을 남긴다.",
      "GitHub 업로드, clone, pull 실습을 수행한다.",
      "README 필수 항목과 제출 증거를 정리한다."
    ],
    "checklistItems": [
      "프로젝트 코드를 GitHub 저장소에 업로드한다.",
      "의미 있는 커밋 10개 이상을 남긴다.",
      "최소 1회 이상의 브랜치 생성 및 병합 기록을 남긴다.",
      "clone과 pull을 각각 1회 이상 사용한다.",
      "README에 프로젝트 개요, 주제 선정 이유, 실행 방법, 기능 목록, 파일 구조, 데이터 파일 설명을 포함한다."
    ],
    "concepts": [
      {
        "id": 1,
        "name": "터미널"
      },
      {
        "id": 2,
        "name": "현재 작업 디렉터리"
      },
      {
        "id": 3,
        "name": "Python"
      },
      {
        "id": 4,
        "name": "Python 버전"
      },
      {
        "id": 5,
        "name": "콘솔 프로그램"
      },
      {
        "id": 6,
        "name": "표준 라이브러리"
      },
      {
        "id": 7,
        "name": "print()"
      },
      {
        "id": 8,
        "name": "input()"
      },
      {
        "id": 9,
        "name": "문자열"
      },
      {
        "id": 10,
        "name": "정수"
      },
      {
        "id": 11,
        "name": "bool"
      },
      {
        "id": 12,
        "name": "list"
      },
      {
        "id": 13,
        "name": "dict"
      },
      {
        "id": 14,
        "name": "변수"
      },
      {
        "id": 15,
        "name": "대입"
      },
      {
        "id": 16,
        "name": "if"
      },
      {
        "id": 17,
        "name": "elif"
      },
      {
        "id": 18,
        "name": "else"
      },
      {
        "id": 19,
        "name": "while"
      },
      {
        "id": 20,
        "name": "for"
      },
      {
        "id": 21,
        "name": "함수"
      },
      {
        "id": 22,
        "name": "매개변수"
      },
      {
        "id": 23,
        "name": "반환값"
      },
      {
        "id": 24,
        "name": "try"
      },
      {
        "id": 25,
        "name": "except"
      },
      {
        "id": 26,
        "name": "KeyboardInterrupt"
      },
      {
        "id": 27,
        "name": "EOFError"
      },
      {
        "id": 28,
        "name": "open()"
      },
      {
        "id": 29,
        "name": "UTF-8"
      },
      {
        "id": 30,
        "name": "JSON"
      },
      {
        "id": 31,
        "name": "json.load"
      },
      {
        "id": 32,
        "name": "json.dump"
      },
      {
        "id": 33,
        "name": "state.json"
      },
      {
        "id": 34,
        "name": "class"
      },
      {
        "id": 35,
        "name": "객체"
      },
      {
        "id": 36,
        "name": "속성"
      },
      {
        "id": 37,
        "name": "메서드"
      },
      {
        "id": 38,
        "name": "__init__"
      },
      {
        "id": 39,
        "name": "self"
      },
      {
        "id": 40,
        "name": "Git"
      },
      {
        "id": 41,
        "name": "repository"
      },
      {
        "id": 42,
        "name": "git init"
      },
      {
        "id": 43,
        "name": "git status"
      },
      {
        "id": 44,
        "name": "git add"
      },
      {
        "id": 45,
        "name": "git commit"
      },
      {
        "id": 46,
        "name": "branch"
      },
      {
        "id": 47,
        "name": "git checkout"
      },
      {
        "id": 48,
        "name": "git merge"
      },
      {
        "id": 49,
        "name": "git push"
      },
      {
        "id": 50,
        "name": "git clone"
      },
      {
        "id": 51,
        "name": "git pull"
      },
      {
        "id": 52,
        "name": "README"
      }
    ],
    "studyPlan": [
      "Python 기본 문법을 퀴즈 게임 흐름 안에서 다시 연결한다.",
      "클래스와 객체를 Quiz / QuizGame 역할 분리로 설명한다.",
      "JSON 저장과 복구 흐름을 state.json으로 검증한다.",
      "Git 요구사항은 기능 중간 commit과 마지막 clone/pull까지 나눠서 실습한다."
    ],
    "chapters": [
      {
        "id": "chapter-1",
        "title": "개발 환경과 Git 저장소 초기화",
        "requirement": "개발 환경과 Git 저장소 준비",
        "requirementIds": [
          "env-python-version",
          "git-repo-setup",
          "meaningful-commits"
        ],
        "bonus": false,
        "themes": [
          {
            "id": "theme-1-1",
            "title": "Python 버전과 Git 설정 확인",
            "actions": [
              {
                "id": "action-1-1-1",
                "title": "Python 버전과 Git 설정 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-1-2",
            "title": "프로젝트 루트와 state.json 저장 위치 확인",
            "actions": [
              {
                "id": "action-1-2-1",
                "title": "프로젝트 루트와 state.json 저장 위치 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-1-3",
            "title": ".gitignore, README.md, main.py 생성",
            "actions": [
              {
                "id": "action-1-3-1",
                "title": ".gitignore, README.md, main.py 생성",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-1-4",
            "title": "git init/add/commit/push 첫 이력 생성",
            "actions": [
              {
                "id": "action-1-4-1",
                "title": "git init/add/commit/push 첫 이력 생성",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          }
        ]
      },
      {
        "id": "chapter-2",
        "title": "메뉴와 공통 입력 예외 처리",
        "requirement": "메뉴와 입력 예외 처리 구현",
        "requirementIds": [
          "menu-flow",
          "input-validation",
          "safe-exit",
          "meaningful-commits"
        ],
        "bonus": false,
        "themes": [
          {
            "id": "theme-2-1",
            "title": "while 반복 메뉴와 종료 흐름 구현",
            "actions": [
              {
                "id": "action-2-1-1",
                "title": "while 반복 메뉴와 종료 흐름 구현",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-2-2",
            "title": "빈 입력/공백/문자/범위 밖 숫자 처리",
            "actions": [
              {
                "id": "action-2-2-1",
                "title": "빈 입력/공백/문자/범위 밖 숫자 처리",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-2-3",
            "title": "KeyboardInterrupt와 EOFError 안전 종료 처리",
            "actions": [
              {
                "id": "action-2-3-1",
                "title": "KeyboardInterrupt와 EOFError 안전 종료 처리",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-2-4",
            "title": "메뉴 기능 완성 후 commit 기록",
            "actions": [
              {
                "id": "action-2-4-1",
                "title": "메뉴 기능 완성 후 commit 기록",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          }
        ]
      },
      {
        "id": "chapter-3",
        "title": "Quiz 클래스와 객체 기초",
        "requirement": "Quiz 클래스와 기본 퀴즈 데이터 구현",
        "requirementIds": [
          "quiz-class",
          "default-quizzes",
          "meaningful-commits"
        ],
        "bonus": false,
        "themes": [
          {
            "id": "theme-3-1",
            "title": "Quiz 클래스 뼈대 정의",
            "actions": [
              {
                "id": "action-3-1-1",
                "title": "Quiz 클래스 뼈대 정의",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-3-2",
            "title": "Quiz 객체 생성과 속성 확인",
            "actions": [
              {
                "id": "action-3-2-1",
                "title": "Quiz 객체 생성과 속성 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-3-3",
            "title": "Quiz 메서드로 출력 책임 이동",
            "actions": [
              {
                "id": "action-3-3-1",
                "title": "Quiz 메서드로 출력 책임 이동",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-3-4",
            "title": "정답 비교 메서드와 불리언 결과 확인",
            "actions": [
              {
                "id": "action-3-4-1",
                "title": "정답 비교 메서드와 불리언 결과 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-3-5",
            "title": "기본 퀴즈 5개 이상 구성 확인",
            "actions": [
              {
                "id": "action-3-5-1",
                "title": "기본 퀴즈 5개 이상 구성 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-3-6",
            "title": "Quiz 클래스 작업 commit 기록",
            "actions": [
              {
                "id": "action-3-6-1",
                "title": "Quiz 클래스 작업 commit 기록",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          }
        ]
      },
      {
        "id": "chapter-4",
        "title": "QuizGame 클래스와 기능별 메서드 분리",
        "requirement": "QuizGame 클래스 구조화",
        "requirementIds": [
          "quizgame-class",
          "meaningful-commits"
        ],
        "bonus": false,
        "themes": [
          {
            "id": "theme-4-1",
            "title": "퀴즈 목록과 최고 점수 속성 묶기",
            "actions": [
              {
                "id": "action-4-1-1",
                "title": "퀴즈 목록과 최고 점수 속성 묶기",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-4-2",
            "title": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
            "actions": [
              {
                "id": "action-4-2-1",
                "title": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-4-3",
            "title": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
            "actions": [
              {
                "id": "action-4-3-1",
                "title": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-4-4",
            "title": "QuizGame 메서드로 종료 판단 책임 옮기기",
            "actions": [
              {
                "id": "action-4-4-1",
                "title": "QuizGame 메서드로 종료 판단 책임 옮기기",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-4-5",
            "title": "한 함수에 몰리지 않는 구조 점검",
            "actions": [
              {
                "id": "action-4-5-1",
                "title": "한 함수에 몰리지 않는 구조 점검",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-4-6",
            "title": "QuizGame 구조 정리 commit 기록",
            "actions": [
              {
                "id": "action-4-6-1",
                "title": "QuizGame 구조 정리 commit 기록",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          }
        ]
      },
      {
        "id": "chapter-5",
        "title": "파일 입출력과 데이터 영속성",
        "requirement": "state.json 저장/복구 구현",
        "requirementIds": [
          "file-persistence",
          "file-recovery",
          "meaningful-commits"
        ],
        "bonus": false,
        "themes": [
          {
            "id": "theme-5-1",
            "title": "state.json 저장 구현",
            "actions": [
              {
                "id": "action-5-1-1",
                "title": "state.json 저장 구현",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-5-2",
            "title": "state.json에서 게임 상태 불러오기",
            "actions": [
              {
                "id": "action-5-2-1",
                "title": "state.json에서 게임 상태 불러오기",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-5-3",
            "title": "손상된 state.json에서도 기본값으로 복구하기",
            "actions": [
              {
                "id": "action-5-3-1",
                "title": "손상된 state.json에서도 기본값으로 복구하기",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-5-4",
            "title": "재실행 후 퀴즈와 점수 유지 확인",
            "actions": [
              {
                "id": "action-5-4-1",
                "title": "재실행 후 퀴즈와 점수 유지 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-5-5",
            "title": "파일 입출력 기능 commit 기록",
            "actions": [
              {
                "id": "action-5-5-1",
                "title": "파일 입출력 기능 commit 기록",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          }
        ]
      },
      {
        "id": "chapter-6",
        "title": "퀴즈 플레이 기능과 브랜치 병합",
        "requirement": "퀴즈 플레이와 브랜치 흐름",
        "requirementIds": [
          "play-quiz",
          "branch-merge",
          "meaningful-commits"
        ],
        "bonus": false,
        "themes": [
          {
            "id": "theme-6-1",
            "title": "기능 브랜치 checkout 생성",
            "actions": [
              {
                "id": "action-6-1-1",
                "title": "기능 브랜치 checkout 생성",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-6-2",
            "title": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
            "actions": [
              {
                "id": "action-6-2-1",
                "title": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-6-3",
            "title": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
            "actions": [
              {
                "id": "action-6-3-1",
                "title": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-6-4",
            "title": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
            "actions": [
              {
                "id": "action-6-4-1",
                "title": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-6-5",
            "title": "랜덤 출제와 문제 수 선택 적용",
            "actions": [
              {
                "id": "action-6-5-1",
                "title": "랜덤 출제와 문제 수 선택 적용",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-6-6",
            "title": "힌트 사용과 점수 차감 확인",
            "actions": [
              {
                "id": "action-6-6-1",
                "title": "힌트 사용과 점수 차감 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-6-7",
            "title": "기능 완성 commit 후 main 병합",
            "actions": [
              {
                "id": "action-6-7-1",
                "title": "기능 완성 commit 후 main 병합",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          }
        ]
      },
      {
        "id": "chapter-7",
        "title": "추가/목록/삭제/점수 히스토리 검증",
        "requirement": "추가/목록/삭제/점수 기능 구현",
        "requirementIds": [
          "add-quiz",
          "list-quiz",
          "score-check",
          "meaningful-commits"
        ],
        "bonus": false,
        "themes": [
          {
            "id": "theme-7-1",
            "title": "퀴즈 추가 후 state.json 반영 확인",
            "actions": [
              {
                "id": "action-7-1-1",
                "title": "퀴즈 추가 후 state.json 반영 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-7-2",
            "title": "목록 출력과 빈 목록 처리 확인",
            "actions": [
              {
                "id": "action-7-2-1",
                "title": "목록 출력과 빈 목록 처리 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-7-3",
            "title": "점수 확인 메뉴로 최고 점수 출력",
            "actions": [
              {
                "id": "action-7-3-1",
                "title": "점수 확인 메뉴로 최고 점수 출력",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-7-4",
            "title": "삭제 후 파일 반영 확인",
            "actions": [
              {
                "id": "action-7-4-1",
                "title": "삭제 후 파일 반영 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-7-5",
            "title": "추가/삭제 입력 범위 예외 처리 보강",
            "actions": [
              {
                "id": "action-7-5-1",
                "title": "추가/삭제 입력 범위 예외 처리 보강",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-7-6",
            "title": "날짜/시간 포함 게임 기록 저장 확인",
            "actions": [
              {
                "id": "action-7-6-1",
                "title": "날짜/시간 포함 게임 기록 저장 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-7-7",
            "title": "추가/목록/삭제/점수 기능 commit 기록",
            "actions": [
              {
                "id": "action-7-7-1",
                "title": "추가/목록/삭제/점수 기능 commit 기록",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          }
        ]
      },
      {
        "id": "chapter-8",
        "title": "README, Git 로그, clone/pull 제출 증거",
        "requirement": "README/GitHub/clone/pull/제출 증거 정리",
        "requirementIds": [
          "github-upload",
          "clone-practice",
          "pull-practice",
          "readme-required-sections",
          "submission-evidence",
          "meaningful-commits"
        ],
        "bonus": false,
        "themes": [
          {
            "id": "theme-8-1",
            "title": "README 필수 항목 작성",
            "actions": [
              {
                "id": "action-8-1-1",
                "title": "README 필수 항목 작성",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-8-2",
            "title": "git log --oneline --graph와 커밋 수 확인",
            "actions": [
              {
                "id": "action-8-2-1",
                "title": "git log --oneline --graph와 커밋 수 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-8-3",
            "title": "GitHub 저장소 연결과 업로드 확인",
            "actions": [
              {
                "id": "action-8-3-1",
                "title": "GitHub 저장소 연결과 업로드 확인",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-8-4",
            "title": "별도 디렉터리에 clone",
            "actions": [
              {
                "id": "action-8-4-1",
                "title": "별도 디렉터리에 clone",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-8-5",
            "title": "clone 변경 push 후 원래 작업 폴더에서 pull",
            "actions": [
              {
                "id": "action-8-5-1",
                "title": "clone 변경 push 후 원래 작업 폴더에서 pull",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          },
          {
            "id": "theme-8-6",
            "title": "개발 환경과 실행 화면 증거 정리",
            "actions": [
              {
                "id": "action-8-6-1",
                "title": "개발 환경과 실행 화면 증거 정리",
                "purpose": "해당 테마를 실제 실습과 출력으로 검증한다.",
                "instruction": "챕터 -> 테마 -> terminal step 순서로 실습한다.",
                "expectedResult": "실제 명령과 출력이 terminal-step으로 남는다."
              }
            ]
          }
        ]
      }
    ],
    "requirementContracts": [
      {
        "id": "env-python-version",
        "title": "Python 3.10 이상 개발 환경 확인",
        "category": "environment",
        "timing": "setup",
        "required": true,
        "sourceIndex": 0
      },
      {
        "id": "git-repo-setup",
        "title": "과제 전용 Git 저장소 초기화와 첫 commit/push",
        "category": "git",
        "timing": "setup",
        "required": true,
        "sourceIndex": 1
      },
      {
        "id": "menu-flow",
        "title": "메뉴 출력, 기능 선택, 종료 흐름 구현",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 2
      },
      {
        "id": "input-validation",
        "title": "빈 입력, 공백, 문자, 범위 밖 숫자 처리",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 3
      },
      {
        "id": "safe-exit",
        "title": "KeyboardInterrupt와 EOFError 안전 종료 처리",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 4
      },
      {
        "id": "quiz-class",
        "title": "Quiz 클래스 정의와 속성/메서드 구현",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 5
      },
      {
        "id": "default-quizzes",
        "title": "기본 퀴즈 5개 이상 작성",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 6
      },
      {
        "id": "quizgame-class",
        "title": "QuizGame 클래스와 기능별 메서드 분리",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 7
      },
      {
        "id": "file-persistence",
        "title": "state.json 저장/불러오기와 데이터 영속성",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 8
      },
      {
        "id": "file-recovery",
        "title": "파일 없음/손상/읽기쓰기 오류 복구 처리",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 9
      },
      {
        "id": "play-quiz",
        "title": "퀴즈 플레이, 정답 판정, 결과 표시",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 10
      },
      {
        "id": "add-quiz",
        "title": "퀴즈 추가와 파일 반영",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 11
      },
      {
        "id": "list-quiz",
        "title": "퀴즈 목록과 빈 목록 처리",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 12
      },
      {
        "id": "score-check",
        "title": "최고 점수 확인과 갱신",
        "category": "implementation",
        "timing": "inline",
        "required": true,
        "sourceIndex": 13
      },
      {
        "id": "branch-merge",
        "title": "브랜치 생성, checkout, merge 기록",
        "category": "git",
        "timing": "post-feature",
        "required": true,
        "sourceIndex": 14
      },
      {
        "id": "github-upload",
        "title": "프로젝트 코드를 GitHub 저장소에 업로드",
        "category": "git",
        "timing": "final",
        "required": true,
        "sourceIndex": 15
      },
      {
        "id": "meaningful-commits",
        "title": "의미 있는 커밋 10개 이상",
        "category": "git",
        "timing": "continuous",
        "required": true,
        "sourceIndex": 16
      },
      {
        "id": "clone-practice",
        "title": "clone 실습과 복제 저장소 변경 push",
        "category": "git",
        "timing": "final",
        "required": true,
        "sourceIndex": 17
      },
      {
        "id": "pull-practice",
        "title": "기존 작업 디렉터리에서 pull 반영 확인",
        "category": "git",
        "timing": "final",
        "required": true,
        "sourceIndex": 18
      },
      {
        "id": "readme-required-sections",
        "title": "README 필수 항목 작성",
        "category": "artifact",
        "timing": "final",
        "required": true,
        "sourceIndex": 19
      },
      {
        "id": "submission-evidence",
        "title": "실행 화면, git log, 개발 환경 증거 정리",
        "category": "artifact",
        "timing": "final",
        "required": true,
        "sourceIndex": 20
      }
    ]
  },
  "logs": [
    {
      "id": "log-1",
      "at": "2026-08-05T03:57:35.955Z",
      "chapter": "메뉴와 공통 입력 예외 처리",
      "theme": "while 반복 메뉴와 종료 흐름 구현",
      "action": "main.py에 반복 메뉴와 종료 조건을 구현",
      "purpose": "while True, input, break로 메뉴 반복과 종료 흐름이 동작하는지 확인",
      "frames": [
        {
          "type": "editor-change",
          "theme": "while 반복 메뉴와 종료 흐름 구현",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "overwrite",
            "from": "",
            "to": "def main():\n    print(\"퀴즈 게임 시작\")\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        choice = input(\"선택: \")\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "while 반복 메뉴와 종료 흐름 구현",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "editorChanges": [
        {
          "theme": "while 반복 메뉴와 종료 흐름 구현",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "overwrite",
            "from": "",
            "to": "def main():\n    print(\"퀴즈 게임 시작\")\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        choice = input(\"선택: \")\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "while 반복 메뉴와 종료 흐름 구현",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "메뉴 출력과 2번 종료 동작 확인"
      ],
      "interpretation": "while 반복문 안에서 메뉴가 출력되고, input으로 받은 문자열이 \"2\"일 때 break로 루프가 종료됨을 확인했다. 비대화형 입력이라 선택 프롬프트와 종료 문구가 같은 줄에 붙었지만 흐름 검증에는 문제가 없다.",
      "evidence": "메뉴 2개 출력 후 종료 문구가 실제 stdout에 나타남",
      "artifacts": []
    },
    {
      "id": "log-2",
      "at": "2026-08-05T04:09:12.152Z",
      "chapter": "메뉴와 공통 입력 예외 처리",
      "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
      "action": "메뉴 선택 입력을 정리하고 오류별 재입력 흐름 구현",
      "purpose": "strip, isdigit, 범위 검사로 잘못된 메뉴 입력을 구분 처리하고 재입력 흐름이 유지되는지 확인",
      "frames": [
        {
          "type": "editor-change",
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        choice = input(\"선택: \")\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break",
            "to": "        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break"
          }
        },
        {
          "type": "terminal-step",
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "command": "printf '\\n2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 입력이 비어 있습니다. 다시 선택하세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        },
        {
          "type": "terminal-step",
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "command": "printf 'abc\\n2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 숫자로 입력해주세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        },
        {
          "type": "terminal-step",
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "command": "printf '9\\n2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 1 또는 2만 입력할 수 있습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        },
        {
          "type": "terminal-step",
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "command": "printf ' 2 \\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "editorChanges": [
        {
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        choice = input(\"선택: \")\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break",
            "to": "        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "command": "printf '\\n2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 입력이 비어 있습니다. 다시 선택하세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        },
        {
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "command": "printf 'abc\\n2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 숫자로 입력해주세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        },
        {
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "command": "printf '9\\n2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 1 또는 2만 입력할 수 있습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        },
        {
          "theme": "빈 입력/공백/문자/범위 밖 숫자 처리",
          "command": "printf ' 2 \\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "빈 입력, 문자 입력, 범위 밖 숫자, 공백 포함 종료 입력에 대한 분기 처리 확인"
      ],
      "interpretation": "입력을 받은 직후 strip으로 앞뒤 공백을 제거하고, 빈 문자열 검사, 숫자 문자열 검사, 허용 메뉴 범위 검사를 순서대로 적용했다. 각 오류 케이스는 continue로 루프 처음으로 돌아가며 프로그램이 중단되지 않았고, 공백이 포함된 종료 입력은 정상적으로 2로 해석되어 break로 종료되었다.",
      "evidence": "실제 stdout에서 오류 메시지 뒤 메뉴가 다시 출력되고, 공백 포함 2 입력은 종료 문구로 이어짐",
      "artifacts": []
    },
    {
      "id": "log-24",
      "at": "2026-08-06T12:20:14.878Z",
      "chapter": "메뉴와 공통 입력 예외 처리",
      "theme": "메뉴 기능 완성 후 commit 기록",
      "action": "메뉴 기능을 Git 기능 단위 커밋으로 기록",
      "purpose": "메뉴/입력 검증 기능을 완성한 뒤 실제 Git 명령으로 작업 이력을 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "메뉴 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": "A  .gitignore\nA  work/main.py",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "메뉴 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "메뉴 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 반복 메뉴와 종료 흐름 구현\"",
          "output": "[codex/e1-2-history-rebuild (root-commit) 27884fe] Feat: 반복 메뉴와 종료 흐름 구현\n 2 files changed, 31 insertions(+)\n create mode 100644 .gitignore\n create mode 100644 work/main.py",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "메뉴 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph",
          "output": "* 27884fe Feat: 반복 메뉴와 종료 흐름 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "메뉴 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": "A  .gitignore\nA  work/main.py",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "메뉴 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "메뉴 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 반복 메뉴와 종료 흐름 구현\"",
          "output": "[codex/e1-2-history-rebuild (root-commit) 27884fe] Feat: 반복 메뉴와 종료 흐름 구현\n 2 files changed, 31 insertions(+)\n create mode 100644 .gitignore\n create mode 100644 work/main.py",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "메뉴 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph",
          "output": "* 27884fe Feat: 반복 메뉴와 종료 흐름 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "메뉴 기능을 첫 기능 단위 커밋으로 기록했다."
      ],
      "interpretation": "",
      "evidence": "git status, add, commit, log 출력으로 메뉴 기능 커밋 생성 확인",
      "artifacts": []
    },
    {
      "id": "log-3",
      "at": "2026-08-05T04:22:25.165Z",
      "chapter": "Quiz 클래스와 객체 기초",
      "theme": "Quiz 클래스 뼈대 정의",
      "action": "question, choices, answer를 저장하는 Quiz 클래스 추가",
      "purpose": "퀴즈 1개를 객체로 표현할 수 있는 기본 구조를 만든다",
      "frames": [
        {
          "type": "editor-change",
          "theme": "Quiz 클래스 뼈대 정의",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():",
            "to": "class Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n\ndef main():"
          }
        }
      ],
      "editorChanges": [
        {
          "theme": "Quiz 클래스 뼈대 정의",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():",
            "to": "class Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n\ndef main():"
          }
        }
      ],
      "terminalSteps": [],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "Quiz 클래스와 __init__ 메서드 추가"
      ],
      "interpretation": "Quiz 클래스는 퀴즈 1개를 표현하는 설계도 역할을 한다. __init__는 객체 생성 시 문제, 선택지, 정답을 받아 self.question, self.choices, self.answer 속성에 저장하도록 구성했다. 아직 이 단계는 구조 정의가 목적이므로 별도 터미널 실행 없이 에디터 변경만 기록한다.",
      "evidence": "main.py 상단에 Quiz 클래스 정의가 추가됨",
      "artifacts": []
    },
    {
      "id": "log-4",
      "at": "2026-08-05T04:22:37.110Z",
      "chapter": "Quiz 클래스와 객체 기초",
      "theme": "Quiz 객체 생성과 속성 확인",
      "action": "quiz1 객체를 만들고 question, choices, answer 속성 출력",
      "purpose": "Quiz 클래스가 실제 객체를 만들고 데이터를 속성으로 저장하는지 확인한다",
      "frames": [
        {
          "type": "editor-change",
          "theme": "Quiz 객체 생성과 속성 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():\n    print(\"퀴즈 게임 시작\")",
            "to": "def main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 직접 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(quiz1.question)\n    print(quiz1.choices)\n    print(quiz1.answer)"
          }
        },
        {
          "type": "terminal-step",
          "theme": "Quiz 객체 생성과 속성 확인",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\n['int', 'str', 'bool', 'list']\n2\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "editorChanges": [
        {
          "theme": "Quiz 객체 생성과 속성 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():\n    print(\"퀴즈 게임 시작\")",
            "to": "def main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 직접 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(quiz1.question)\n    print(quiz1.choices)\n    print(quiz1.answer)"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "Quiz 객체 생성과 속성 확인",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\n['int', 'str', 'bool', 'list']\n2\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "quiz1 객체 생성 후 문제, 선택지, 정답 속성 출력 확인"
      ],
      "interpretation": "Quiz(...)로 객체를 생성할 때 전달한 문제, 선택지 리스트, 정답 번호가 각 속성에 저장되었는지 print로 확인했다. 실행의 핵심 검증 대상은 quiz1.question, quiz1.choices, quiz1.answer 출력이며, 종료 입력 2는 프로그램을 깔끔하게 끝내기 위한 보조 입력이다.",
      "evidence": "stdout에 문제 문장, 선택지 리스트, 정답 번호가 순서대로 출력됨",
      "artifacts": []
    },
    {
      "id": "log-5",
      "at": "2026-08-05T04:36:41.567Z",
      "chapter": "Quiz 클래스와 객체 기초",
      "theme": "Quiz 메서드로 출력 책임 이동",
      "action": "show 메서드를 추가하고 main의 직접 출력 코드를 quiz1.show()로 치환",
      "purpose": "속성 출력 책임을 main 함수가 아니라 Quiz 객체 메서드가 담당하도록 구조를 정리한다",
      "frames": [
        {
          "type": "editor-change",
          "theme": "Quiz 메서드로 출력 책임 이동",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "class Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer",
            "to": "class Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)"
          }
        },
        {
          "type": "editor-change",
          "theme": "Quiz 메서드로 출력 책임 이동",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 객체에 저장된 값을 직접 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(quiz1.question)\n    print(quiz1.choices)\n    print(quiz1.answer)",
            "to": "    # 객체에 저장된 값을 메서드로 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()"
          }
        },
        {
          "type": "terminal-step",
          "theme": "Quiz 메서드로 출력 책임 이동",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "editorChanges": [
        {
          "theme": "Quiz 메서드로 출력 책임 이동",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "class Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer",
            "to": "class Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)"
          }
        },
        {
          "theme": "Quiz 메서드로 출력 책임 이동",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 객체에 저장된 값을 직접 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(quiz1.question)\n    print(quiz1.choices)\n    print(quiz1.answer)",
            "to": "    # 객체에 저장된 값을 메서드로 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "Quiz 메서드로 출력 책임 이동",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "Quiz.show() 메서드로 문제, 선택지, 정답 출력 확인"
      ],
      "interpretation": "Quiz 클래스에 show 메서드를 추가하여 question, choices, answer 출력 책임을 객체 내부로 옮겼다. main 함수는 개별 속성을 직접 print하지 않고 quiz1.show()만 호출하게 되어 역할 분리가 더 명확해졌다. 실행 결과에서 선택지 리스트가 한 줄 통째가 아니라 각 항목별로 나뉘어 출력되어 for 반복이 실제로 동작함을 확인했다.",
      "evidence": "stdout에 문제 1줄, 선택지 4줄, 정답 번호 1줄이 순서대로 출력됨",
      "artifacts": []
    },
    {
      "id": "log-6",
      "at": "2026-08-05T04:39:18.986Z",
      "chapter": "Quiz 클래스와 객체 기초",
      "theme": "정답 비교 메서드와 불리언 결과 확인",
      "action": "check_answer 메서드를 추가하고 정답/오답 입력에 대한 True, False 반환 확인",
      "purpose": "Quiz 객체가 사용자 답안을 정답과 비교해 불리언 값으로 판별할 수 있는지 검증한다",
      "frames": [
        {
          "type": "editor-change",
          "theme": "정답 비교 메서드와 불리언 결과 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)",
            "to": "    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer"
          }
        },
        {
          "type": "editor-change",
          "theme": "정답 비교 메서드와 불리언 결과 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 객체에 저장된 값을 메서드로 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()",
            "to": "    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))"
          }
        },
        {
          "type": "terminal-step",
          "theme": "정답 비교 메서드와 불리언 결과 확인",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "editorChanges": [
        {
          "theme": "정답 비교 메서드와 불리언 결과 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)",
            "to": "    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer"
          }
        },
        {
          "theme": "정답 비교 메서드와 불리언 결과 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 객체에 저장된 값을 메서드로 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()",
            "to": "    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "정답 비교 메서드와 불리언 결과 확인",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다."
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "check_answer 메서드가 정답에는 True, 오답에는 False를 반환하는 것 확인"
      ],
      "interpretation": "Quiz 클래스에 check_answer 메서드를 추가하여 저장된 정답 번호와 사용자 답안을 직접 비교하도록 만들었다. 정답 2를 넣었을 때는 True, 오답 1을 넣었을 때는 False가 출력되어 비교 결과가 불리언 값으로 정확히 표현됨을 확인했다.",
      "evidence": "stdout에 True와 False가 차례대로 출력됨",
      "artifacts": []
    },
    {
      "id": "log-25",
      "at": "2026-08-06T12:34:24.004Z",
      "chapter": "Quiz 클래스와 객체 기초",
      "theme": "Quiz 기능 완성 후 commit 기록",
      "action": "Quiz 클래스 기능을 Git 기능 단위 커밋으로 기록",
      "purpose": "Quiz 클래스 생성, 출력, 정답 판정 기능을 완성한 뒤 실제 Git 명령으로 작업 이력을 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "Quiz 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "Quiz 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "Quiz 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: Quiz 클래스와 정답 판정 구현\"",
          "output": "[codex/e1-2-history-rebuild 7e2694f] Feat: Quiz 클래스와 정답 판정 구현\n 1 file changed, 30 insertions(+)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "Quiz 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 7e2694f (HEAD -> codex/e1-2-history-rebuild) Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "Quiz 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "Quiz 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "Quiz 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: Quiz 클래스와 정답 판정 구현\"",
          "output": "[codex/e1-2-history-rebuild 7e2694f] Feat: Quiz 클래스와 정답 판정 구현\n 1 file changed, 30 insertions(+)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "Quiz 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 7e2694f (HEAD -> codex/e1-2-history-rebuild) Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: Quiz 클래스와 정답 판정 구현\"",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3"
      ],
      "terminalOutputs": [
        " M work/main.py",
        "",
        "[codex/e1-2-history-rebuild 7e2694f] Feat: Quiz 클래스와 정답 판정 구현\n 1 file changed, 30 insertions(+)",
        "* 7e2694f (HEAD -> codex/e1-2-history-rebuild) Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현"
      ],
      "outputs": [
        "Feat: Quiz 클래스와 정답 판정 구현 커밋과 Git 로그 확인"
      ],
      "interpretation": "Feat: Quiz 클래스와 정답 판정 구현 커밋을 만들고 status/add/commit/log 순서가 실제 터미널 출력으로 남았다.",
      "evidence": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: Quiz 클래스와 정답 판정 구현\"\n[codex/e1-2-history-rebuild 7e2694f] Feat: Quiz 클래스와 정답 판정 구현\n 1 file changed, 30 insertions(+)\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 7e2694f (HEAD -> codex/e1-2-history-rebuild) Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현",
      "artifacts": []
    },
    {
      "id": "log-7",
      "at": "2026-08-05T07:14:59.841Z",
      "chapter": "QuizGame 클래스와 기능별 메서드 분리",
      "theme": "퀴즈 목록과 최고 점수 속성 묶기",
      "action": "QuizGame 클래스를 만들고 퀴즈 목록, 최고 점수 상태를 객체 속성으로 확인",
      "purpose": "게임 전체 상태를 QuizGame 객체로 묶고 속성으로 접근할 수 있는지 검증한다",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "퀴즈 목록과 최고 점수 속성 묶기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\ndef main():",
            "to": "    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n\ndef main():"
          }
        },
        {
          "type": "editor-change",
          "theme": "퀴즈 목록과 최고 점수 속성 묶기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))",
            "to": "def main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)"
          }
        },
        {
          "type": "terminal-step",
          "theme": "퀴즈 목록과 최고 점수 속성 묶기",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x1033bc590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "퀴즈 목록과 최고 점수 속성 묶기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\ndef main():",
            "to": "    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n\ndef main():"
          }
        },
        {
          "theme": "퀴즈 목록과 최고 점수 속성 묶기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))",
            "to": "def main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "퀴즈 목록과 최고 점수 속성 묶기",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x1033bc590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "QuizGame 객체가 퀴즈 목록과 최고 점수를 저장하고 종료 입력까지 정상 동작하는 것 확인"
      ],
      "interpretation": "QuizGame 클래스를 추가해 게임 전체 상태를 한 객체에 묶었다. 실행 결과에서 game.quizzes는 Quiz 객체가 들어 있는 리스트로 출력됐고, game.best_score는 0으로 출력됐다. 즉 QuizGame이 퀴즈 목록과 최고 점수라는 두 상태를 속성으로 보관한다는 점이 실제 출력으로 확인됐다.",
      "evidence": "stdout에 Quiz 객체 리스트 표현과 최고 점수 0이 순서대로 출력되고 종료 메시지까지 정상 표시됨",
      "artifacts": []
    },
    {
      "id": "log-8",
      "at": "2026-08-05T07:36:09.474Z",
      "chapter": "QuizGame 클래스와 기능별 메서드 분리",
      "theme": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
      "action": "show_menu 메서드를 추가하고 main의 메뉴 출력 코드를 QuizGame으로 이동",
      "purpose": "메뉴 출력 책임을 QuizGame 메서드로 분리해 게임 객체가 동작도 담당하도록 구조를 개선한다",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "class QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n",
            "to": "class QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.",
            "to": "    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다."
          }
        },
        {
          "type": "terminal-step",
          "theme": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x109e88590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "class QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n",
            "to": "class QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n"
          }
        },
        {
          "theme": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.",
            "to": "    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다."
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "QuizGame 메서드로 메뉴 출력 책임 옮기기",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x109e88590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "show_menu 메서드로 메뉴 출력 책임을 옮긴 뒤에도 메뉴와 종료 흐름이 동일하게 동작하는 것 확인"
      ],
      "interpretation": "QuizGame 클래스에 show_menu 메서드를 추가하고 main에서 직접 출력하던 메뉴 문구를 game.show_menu() 호출로 바꿨다. 실행 결과에서 메뉴 문구와 종료 메시지가 이전과 동일하게 출력되어, 구조는 더 분리됐지만 사용자 입장에서 보이는 동작은 그대로 유지됨을 확인했다.",
      "evidence": "stdout에 메뉴 2줄과 종료 메시지가 정상 출력됨",
      "artifacts": []
    },
    {
      "id": "log-9",
      "at": "2026-08-05T09:49:35.771Z",
      "chapter": "QuizGame 클래스와 기능별 메서드 분리",
      "theme": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
      "action": "is_blank_choice 메서드를 추가하고 빈 입력 검사 코드를 QuizGame으로 이동",
      "purpose": "빈 메뉴 입력 검사 책임을 QuizGame 메서드로 분리해 입력 규칙도 게임 객체가 담당하도록 구조를 개선한다",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n",
            "to": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n",
            "to": "        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
          "command": "printf '\\n2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x105b44590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 입력이 비어 있습니다. 다시 선택하세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n",
            "to": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n"
          }
        },
        {
          "theme": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n",
            "to": "        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "QuizGame 메서드로 메뉴 입력 검사 책임 옮기기",
          "command": "printf '\\n2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x105b44590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 입력이 비어 있습니다. 다시 선택하세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "빈 입력 검사 책임을 QuizGame 메서드로 옮긴 뒤에도 재입력과 종료 흐름이 정상 동작하는 것 확인"
      ],
      "interpretation": "QuizGame 클래스에 is_blank_choice 메서드를 추가하고 main에서 직접 하던 빈 문자열 비교를 game.is_blank_choice(choice) 호출로 바꿨다. 실행 결과에서 첫 빈 입력에 대해 안내 문구가 출력되고 메뉴가 다시 표시된 뒤, 두 번째 입력 2로 정상 종료되어 구조 분리 후에도 기존 재입력 흐름이 유지됨을 확인했다.",
      "evidence": "stdout에 빈 입력 안내 문구, 재출력된 메뉴, 종료 메시지가 순서대로 출력됨",
      "artifacts": []
    },
    {
      "id": "log-10",
      "at": "2026-08-05T11:18:13.009Z",
      "chapter": "QuizGame 클래스와 기능별 메서드 분리",
      "theme": "QuizGame 메서드로 종료 판단 책임 옮기기",
      "action": "is_exit_choice 메서드를 추가하고 종료 비교 코드를 QuizGame으로 이동",
      "purpose": "종료 여부 판단 책임을 QuizGame 메서드로 분리해 메뉴 흐름의 조건 판단을 게임 객체로 모은다",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "QuizGame 메서드로 종료 판단 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n",
            "to": "    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "QuizGame 메서드로 종료 판단 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n",
            "to": "        if not game.is_digit_choice(choice):\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if not game.is_valid_menu_choice(choice):\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if game.is_exit_choice(choice):\n            print(\"프로그램을 종료합니다.\")\n            break\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "QuizGame 메서드로 종료 판단 책임 옮기기",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x10386c590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "QuizGame 메서드로 종료 판단 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n",
            "to": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n"
          }
        },
        {
          "theme": "QuizGame 메서드로 종료 판단 책임 옮기기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n",
            "to": "        if game.is_exit_choice(choice):\n            print(\"프로그램을 종료합니다.\")\n            break\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "QuizGame 메서드로 종료 판단 책임 옮기기",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x10386c590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "종료 판단 책임을 QuizGame 메서드로 옮긴 뒤에도 종료 흐름이 동일하게 동작하는 것 확인"
      ],
      "interpretation": "QuizGame 클래스에 is_exit_choice 메서드를 추가하고 main에서 직접 하던 종료 비교를 game.is_exit_choice(choice) 호출로 바꿨다. 실행 결과에서 메뉴 출력 뒤 입력 2가 정상적으로 종료 선택으로 처리되어, 조건 판단이 QuizGame으로 이동해도 사용자 입장에서 보이는 종료 흐름은 그대로 유지됨을 확인했다.",
      "evidence": "stdout에 메뉴 출력과 종료 메시지가 정상 표시됨",
      "artifacts": []
    },
    {
      "id": "log-11",
      "at": "2026-08-05T11:22:08.849Z",
      "chapter": "QuizGame 클래스와 기능별 메서드 분리",
      "theme": "한 함수에 몰리지 않는 구조 점검",
      "action": "run_menu 메서드를 추가하고 main의 메뉴 반복 흐름을 QuizGame으로 이동",
      "purpose": "main에 길게 몰려 있던 메뉴 반복 로직을 QuizGame 메서드로 분리해 역할을 더 명확히 나눈다",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "한 함수에 몰리지 않는 구조 점검",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n",
            "to": "    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "한 함수에 몰리지 않는 구조 점검",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not game.is_digit_choice(choice):\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if not game.is_valid_menu_choice(choice):\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if game.is_exit_choice(choice):\n            print(\"프로그램을 종료합니다.\")\n            break\n",
            "to": "    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "한 함수에 몰리지 않는 구조 점검",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x104308590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "한 함수에 몰리지 않는 구조 점검",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n",
            "to": "    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \" ).strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n"
          }
        },
        {
          "theme": "한 함수에 몰리지 않는 구조 점검",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \" ).strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not game.is_digit_choice(choice):\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if not game.is_valid_menu_choice(choice):\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if game.is_exit_choice(choice):\n            print(\"프로그램을 종료합니다.\")\n            break\n",
            "to": "    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "한 함수에 몰리지 않는 구조 점검",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x104308590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "run_menu 메서드로 메뉴 반복 흐름을 옮긴 뒤에도 메뉴와 종료 동작이 동일하게 유지되는 것 확인"
      ],
      "interpretation": "QuizGame 클래스에 run_menu 메서드를 추가해 메뉴 출력, 입력, 검사를 한 곳에서 조립하고, main에서는 game.run_menu()만 호출하도록 바꿨다. 실행 결과는 이전과 동일하게 메뉴를 출력하고 입력 2에서 종료되어, 한 함수에 몰려 있던 흐름을 분리해도 사용자 입장의 동작은 그대로 유지됨을 확인했다.",
      "evidence": "stdout에 메뉴 출력과 종료 메시지가 정상 표시됨",
      "artifacts": []
    },
    {
      "id": "log-26",
      "at": "2026-08-06T12:34:24.107Z",
      "chapter": "QuizGame 클래스와 기능별 메서드 분리",
      "theme": "QuizGame 구조 완성 후 commit 기록",
      "action": "QuizGame 리팩터링 결과를 Git 기능 단위 커밋으로 기록",
      "purpose": "메뉴 출력, 입력 검사, 종료 판단 책임을 QuizGame으로 옮긴 뒤 실제 Git 명령으로 작업 이력을 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "QuizGame 구조 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "QuizGame 구조 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "QuizGame 구조 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Refactor: QuizGame으로 메뉴 책임 분리\"",
          "output": "[codex/e1-2-history-rebuild 054e3c8] Refactor: QuizGame으로 메뉴 책임 분리\n 1 file changed, 58 insertions(+), 23 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "QuizGame 구조 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 054e3c8 (HEAD -> codex/e1-2-history-rebuild) Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "QuizGame 구조 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "QuizGame 구조 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "QuizGame 구조 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Refactor: QuizGame으로 메뉴 책임 분리\"",
          "output": "[codex/e1-2-history-rebuild 054e3c8] Refactor: QuizGame으로 메뉴 책임 분리\n 1 file changed, 58 insertions(+), 23 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "QuizGame 구조 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 054e3c8 (HEAD -> codex/e1-2-history-rebuild) Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Refactor: QuizGame으로 메뉴 책임 분리\"",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3"
      ],
      "terminalOutputs": [
        " M work/main.py",
        "",
        "[codex/e1-2-history-rebuild 054e3c8] Refactor: QuizGame으로 메뉴 책임 분리\n 1 file changed, 58 insertions(+), 23 deletions(-)",
        "* 054e3c8 (HEAD -> codex/e1-2-history-rebuild) Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현"
      ],
      "outputs": [
        "Refactor: QuizGame으로 메뉴 책임 분리 커밋과 Git 로그 확인"
      ],
      "interpretation": "Refactor: QuizGame으로 메뉴 책임 분리 커밋을 만들고 status/add/commit/log 순서가 실제 터미널 출력으로 남았다.",
      "evidence": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Refactor: QuizGame으로 메뉴 책임 분리\"\n[codex/e1-2-history-rebuild 054e3c8] Refactor: QuizGame으로 메뉴 책임 분리\n 1 file changed, 58 insertions(+), 23 deletions(-)\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 054e3c8 (HEAD -> codex/e1-2-history-rebuild) Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현",
      "artifacts": []
    },
    {
      "id": "log-12",
      "at": "2026-08-05T12:52:46.207Z",
      "chapter": "파일 입출력과 데이터 영속성",
      "theme": "state.json에서 게임 상태 불러오기",
      "action": "load_from_file 클래서드 메서드를 추가하고 저장된 JSON으로 QuizGame 객체를 복원",
      "purpose": "state.json에 저장한 데이터를 다시 읽어 Quiz 객체들과 QuizGame 객체로 복원할 수 있는지 검증한다",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "state.json에서 게임 상태 불러오기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "class Quiz:",
            "to": "import json\n\n\nclass Quiz:"
          }
        },
        {
          "type": "editor-change",
          "theme": "state.json에서 게임 상태 불러오기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n\ndef main():\n",
            "to": "    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "state.json에서 게임 상태 불러오기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n",
            "to": "    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "state.json에서 게임 상태 불러오기",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x104145fd0>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "state.json에서 게임 상태 불러오기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n",
            "to": "    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n"
          }
        },
        {
          "theme": "state.json에서 게임 상태 불러오기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n",
            "to": "    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "state.json에서 게임 상태 불러오기",
          "command": "printf '2\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x104145fd0>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "state.json을 읽어 복원한 QuizGame 객체가 저장 전과 같은 데이터 구조를 갖는 것 확인"
      ],
      "interpretation": "load_from_file 클래서드 메서드가 state.json을 읽어 quizzes 목록을 Quiz 객체들로 다시 만들고, best_score와 함께 새 QuizGame 객체를 복원했다. 실행 결과에서 game.to_dict()와 loaded_game.to_dict() 출력이 동일하게 나와 저장한 데이터와 복원한 객체 상태가 일치함을 확인했다.",
      "evidence": "stdout에 저장 전 dict와 복원 후 dict가 같은 내용으로 연속 출력됨",
      "artifacts": []
    },
    {
      "id": "log-13",
      "at": "2026-08-05T13:15:26.075Z",
      "chapter": "파일 입출력과 데이터 영속성",
      "theme": "손상된 state.json에서도 기본값으로 복구하기",
      "action": "JSONDecodeError를 처리하고 손상된 state.json일 때 기본 퀴즈로 복구",
      "purpose": "손상된 저장 파일이 있어도 프로그램이 죽지 않고 기본 퀴즈와 점수로 계속 실행되는지 검증한다",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "손상된 state.json에서도 기본값으로 복구하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n",
            "to": "    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "손상된 state.json에서도 기본값으로 복구하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n",
            "to": "    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "손상된 state.json에서도 기본값으로 복구하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n",
            "to": "def main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "손상된 state.json에서도 기본값으로 복구하기",
          "command": "tmpdir=$(mktemp -d /private/tmp/e1-2-corrupt-XXXXXX) && cp /Users/hskim/Projects/codyssey/artifacts/e1-2/work/main.py \"$tmpdir/main.py\" && printf '{ broken json\\n' > \"$tmpdir/state.json\" && cd \"$tmpdir\" && printf '2\\n' | python3 main.py",
          "output": "저장 파일이 손상되어 기본 퀴즈로 복구합니다.\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x10178a120>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "손상된 state.json에서도 기본값으로 복구하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n",
            "to": "        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "손상된 state.json에서도 기본값으로 복구하기",
          "command": "tmpdir=$(mktemp -d /private/tmp/e1-2-corrupt-XXXXXX) && cp /Users/hskim/Projects/codyssey/artifacts/e1-2/work/main.py \"$tmpdir/main.py\" && printf '{ broken json\\n' > \"$tmpdir/state.json\" && cd \"$tmpdir\" && printf '2\\n' | python3 main.py",
          "output": "저장 파일이 손상되어 기본 퀴즈로 복구합니다.\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x10178a120>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "손상된 state.json에서도 안내 문구를 출력하고 기본 퀴즈로 복구한 뒤 정상 종료되는 것 확인"
      ],
      "interpretation": "load_from_file에서 json.JSONDecodeError를 추가로 처리해, 파일은 있지만 JSON 문법이 깨진 경우에도 기본 퀴즈와 점수 0으로 새 QuizGame 객체를 만들도록 했다. 손상된 state.json을 넣은 임시 폴더에서 실행했을 때 안내 문구가 먼저 출력되고, 이후 기본 퀴즈와 메뉴가 정상 동작해 프로그램이 비정상 종료하지 않음을 확인했다.",
      "evidence": "stdout 첫 줄의 복구 안내 문구와 이후 정상적인 메뉴/종료 흐름",
      "artifacts": []
    },
    {
      "id": "log-27",
      "at": "2026-08-06T12:34:24.181Z",
      "chapter": "파일 입출력과 데이터 영속성",
      "theme": "저장 복구 기능 완성 후 commit 기록",
      "action": "state.json 저장과 복구 기능을 Git 기능 단위 커밋으로 기록",
      "purpose": "게임 상태 저장과 손상 파일 복구 기능을 완성한 뒤 실제 Git 명령으로 작업 이력을 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "저장 복구 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n?? work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "저장 복구 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "저장 복구 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: state.json 저장과 복구 처리 추가\"",
          "output": "[codex/e1-2-history-rebuild a5d5759] Feat: state.json 저장과 복구 처리 추가\n 2 files changed, 99 insertions(+), 7 deletions(-)\n create mode 100644 work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "저장 복구 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* a5d5759 (HEAD -> codex/e1-2-history-rebuild) Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "저장 복구 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n?? work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "저장 복구 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "저장 복구 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: state.json 저장과 복구 처리 추가\"",
          "output": "[codex/e1-2-history-rebuild a5d5759] Feat: state.json 저장과 복구 처리 추가\n 2 files changed, 99 insertions(+), 7 deletions(-)\n create mode 100644 work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "저장 복구 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* a5d5759 (HEAD -> codex/e1-2-history-rebuild) Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: state.json 저장과 복구 처리 추가\"",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3"
      ],
      "terminalOutputs": [
        " M work/main.py\n?? work/state.json",
        "",
        "[codex/e1-2-history-rebuild a5d5759] Feat: state.json 저장과 복구 처리 추가\n 2 files changed, 99 insertions(+), 7 deletions(-)\n create mode 100644 work/state.json",
        "* a5d5759 (HEAD -> codex/e1-2-history-rebuild) Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현"
      ],
      "outputs": [
        "Feat: state.json 저장과 복구 처리 추가 커밋과 Git 로그 확인"
      ],
      "interpretation": "Feat: state.json 저장과 복구 처리 추가 커밋을 만들고 status/add/commit/log 순서가 실제 터미널 출력으로 남았다.",
      "evidence": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n?? work/state.json\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: state.json 저장과 복구 처리 추가\"\n[codex/e1-2-history-rebuild a5d5759] Feat: state.json 저장과 복구 처리 추가\n 2 files changed, 99 insertions(+), 7 deletions(-)\n create mode 100644 work/state.json\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* a5d5759 (HEAD -> codex/e1-2-history-rebuild) Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현",
      "artifacts": []
    },
    {
      "id": "log-14",
      "at": "2026-08-06T05:16:34.145Z",
      "chapter": "퀴즈 플레이 기능과 브랜치 병합",
      "theme": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
      "action": "play_first_quiz 메서드 추가와 메뉴 연결",
      "purpose": "메뉴 1번을 선택하면 실제 퀴즈가 출제되고 정답 여부가 판정되는지 확인",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n",
            "to": "    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 첫 번째 퀴즈를 출제하고 정답 여부를 확인한다.\n    def play_first_quiz(self):\n        quiz = self.quizzes[0]\n        quiz.show()\n        answer = int(input(\"정답 번호: \").strip())\n\n        if quiz.check_answer(answer):\n            print(\"정답입니다.\")\n            self.update_best_score(1)\n            self.save_to_file()\n            print(f\"현재 최고 점수: {self.best_score}\")\n            return\n\n        print(\"오답입니다.\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if self.is_exit_choice(choice):\n",
            "to": "            if choice == \"1\":\n                self.play_first_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
          "command": "python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x103bb6120>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 1\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 2\n정답입니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 2\n프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n",
            "to": "    # 첫 번째 퀴즈를 출제하고 정답 여부를 확인한다.\n    def play_first_quiz(self):\n        quiz = self.quizzes[0]\n        quiz.show()\n        answer = int(input(\"정답 번호: \").strip())\n\n        if quiz.check_answer(answer):\n            print(\"정답입니다.\")\n            return\n\n        print(\"오답입니다.\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n"
          }
        },
        {
          "theme": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if self.is_exit_choice(choice):\n",
            "to": "            if choice == \"1\":\n                self.play_first_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "퀴즈 1개를 실제로 출제하고 정답 판정하기",
          "command": "python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x103bb6120>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 1\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 2\n정답입니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 2\n프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "메뉴 1번으로 첫 퀴즈를 풀고 정답 2를 입력해 정답 판정 후 종료까지 확인했다."
      ],
      "interpretation": "play_first_quiz가 quizzes[0]을 꺼내 문제를 출력하고, 입력한 번호를 check_answer로 검사한 뒤 정답 메시지를 출력했다. 메뉴 1번이 실제 퀴즈 풀이로 연결되고, 풀이 후 다시 메뉴로 돌아와 종료까지 이어지는 흐름이 확인됐다.",
      "evidence": "메뉴 선택 1 -> 문제 출력 -> 정답 번호 2 입력 -> 정답입니다. -> 메뉴 복귀 -> 종료 출력",
      "artifacts": []
    },
    {
      "id": "log-15",
      "at": "2026-08-06T05:48:40.448Z",
      "chapter": "퀴즈 플레이 기능과 브랜치 병합",
      "theme": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
      "action": "play_all_quizzes 메서드로 전체 퀴즈 반복 출제",
      "purpose": "첫 번째 퀴즈만 풀던 구조를 퀴즈 목록 전체 순회와 최종 점수 집계 구조로 확장했는지 검증",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 첫 번째 퀴즈를 출제하고 정답 여부를 확인한다.\n    def play_first_quiz(self):\n        quiz = self.quizzes[0]\n        quiz.show()\n        answer = int(input(\"정답 번호: \").strip())\n\n        if quiz.check_answer(answer):\n            print(\"정답입니다.\")\n            self.update_best_score(1)\n            self.save_to_file()\n            print(f\"현재 최고 점수: {self.best_score}\")\n            return\n\n        print(\"오답입니다.\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n",
            "to": "    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if choice == \"1\":\n                self.play_first_quiz()\n                continue\n",
            "to": "            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
          "command": "printf '1\n2\n3\n2\n2\n3\n2\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x1084f1fd0>, <__main__.Quiz object at 0x1084d0910>, <__main__.Quiz object at 0x1084d2850>, <__main__.Quiz object at 0x108554770>, <__main__.Quiz object at 0x1085555b0>]\n1\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 1}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 1}\n1. 퀴즈 풀기\n2. 종료\n선택: Python에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 정답입니다.\nPython에서 3 > 1의 결과는 무엇인가?\n0\nFalse\nTrue\nNone\n3\n정답 번호: 정답입니다.\n여러 값을 순서대로 저장하는 자료형은 무엇인가?\ndict\nlist\nstr\nint\n2\n정답 번호: 정답입니다.\n같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\nif\nfor\nprint\ninput\n2\n정답 번호: 정답입니다.\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\nbreak\nclass\nreturn\nimport\n3\n정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 5\n현재 최고 점수: 5\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 첫 번째 퀴즈를 출제하고 정답 여부를 확인한다.\n    def play_first_quiz(self):\n        quiz = self.quizzes[0]\n        quiz.show()\n        answer = int(input(\"정답 번호: \").strip())\n\n        if quiz.check_answer(answer):\n            print(\"정답입니다.\")\n            self.update_best_score(1)\n            self.save_to_file()\n            print(f\"현재 최고 점수: {self.best_score}\")\n            return\n\n        print(\"오답입니다.\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n",
            "to": "    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n"
          }
        },
        {
          "theme": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if choice == \"1\":\n                self.play_first_quiz()\n                continue\n",
            "to": "            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "모든 퀴즈를 순서대로 출제하고 점수 합산하기",
          "command": "printf '1\n2\n3\n2\n2\n3\n2\n' | python3 main.py",
          "output": "퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x1084f1fd0>, <__main__.Quiz object at 0x1084d0910>, <__main__.Quiz object at 0x1084d2850>, <__main__.Quiz object at 0x108554770>, <__main__.Quiz object at 0x1085555b0>]\n1\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 1}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 1}\n1. 퀴즈 풀기\n2. 종료\n선택: Python에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 정답입니다.\nPython에서 3 > 1의 결과는 무엇인가?\n0\nFalse\nTrue\nNone\n3\n정답 번호: 정답입니다.\n여러 값을 순서대로 저장하는 자료형은 무엇인가?\ndict\nlist\nstr\nint\n2\n정답 번호: 정답입니다.\n같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\nif\nfor\nprint\ninput\n2\n정답 번호: 정답입니다.\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\nbreak\nclass\nreturn\nimport\n3\n정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 5\n현재 최고 점수: 5\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "퀴즈 5문항을 순서대로 모두 출제하고 정답 수 5점으로 최고 점수를 갱신했다."
      ],
      "interpretation": "play_all_quizzes가 self.quizzes 전체를 for로 순회하며 각 퀴즈를 출제하고, 정답일 때마다 score를 1씩 올린다. 반복이 끝난 뒤 update_best_score(score)로 최고 점수를 비교 갱신하고 save_to_file로 저장한다. 실행 결과에서 다섯 문제 모두 차례로 출력됐고 마지막에 이번 점수 5, 현재 최고 점수 5가 표시되어 전체 퀴즈 플레이와 결과 합산 흐름이 확인됐다.",
      "evidence": "5개 문제가 연속 출력되고 마지막에 최고 점수가 갱신되었습니다 / 이번 점수: 5 / 현재 최고 점수: 5 출력",
      "artifacts": []
    },
    {
      "id": "log-16",
      "at": "2026-08-06T05:56:42.499Z",
      "chapter": "퀴즈 플레이 기능과 브랜치 병합",
      "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
      "action": "빈 quizzes 상태에서 안전하게 시작하고 플레이 차단",
      "purpose": "퀴즈 목록이 비어 있어도 프로그램이 비정상 종료하지 않고 안내 메시지 후 메뉴로 복귀하는지 검증",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n",
            "to": "    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n",
            "to": "    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        score = 0\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n",
            "to": "def main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    if game.has_quizzes():\n        quiz1 = game.quizzes[0]\n        quiz1.show()\n        print(quiz1.check_answer(2))\n        print(quiz1.check_answer(1))\n    else:\n        print(\"확인할 기본 퀴즈가 없습니다.\")\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
          "command": "printf '1\n2\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[]\n0\n{'quizzes': [], 'best_score': 0}\n{'quizzes': [], 'best_score': 0}\n확인할 기본 퀴즈가 없습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n",
            "to": "    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n"
          }
        },
        {
          "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n",
            "to": "    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        score = 0\n"
          }
        },
        {
          "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "def main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n",
            "to": "def main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    if game.has_quizzes():\n        quiz1 = game.quizzes[0]\n        quiz1.show()\n        print(quiz1.check_answer(2))\n        print(quiz1.check_answer(1))\n    else:\n        print(\"확인할 기본 퀴즈가 없습니다.\")\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "퀴즈가 없는 경우 안내 후 메뉴 복귀",
          "command": "printf '1\n2\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[]\n0\n{'quizzes': [], 'best_score': 0}\n{'quizzes': [], 'best_score': 0}\n확인할 기본 퀴즈가 없습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "빈 퀴즈 상태에서도 프로그램이 죽지 않고 안내 후 메뉴로 복귀한 뒤 종료까지 정상 동작했다."
      ],
      "interpretation": "has_quizzes로 퀴즈 목록 존재 여부를 먼저 확인하고, 비어 있으면 play_all_quizzes에서 즉시 안내 메시지를 출력한 뒤 반환한다. 또한 main의 확인용 출력도 빈 리스트일 때 game.quizzes[0]에 접근하지 않도록 분기했다. 실행 결과에서 quizzes가 빈 리스트로 출력된 뒤 안내 문구가 표시되고 메뉴 복귀 후 종료까지 이어져, 빈 퀴즈 상태 처리 요구사항이 충족됨을 확인했다.",
      "evidence": "stdout에 [] / {'quizzes': [], 'best_score': 0} / 등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다. 출력 확인",
      "artifacts": []
    },
    {
      "id": "log-28",
      "at": "2026-08-06T12:34:24.233Z",
      "chapter": "퀴즈 플레이 기능과 브랜치 병합",
      "theme": "플레이 기능 완성 후 commit 기록",
      "action": "퀴즈 플레이와 점수 계산 기능을 Git 기능 단위 커밋으로 기록",
      "purpose": "실제 퀴즈 풀이와 점수 합산 흐름을 완성한 뒤 실제 Git 명령으로 작업 이력을 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "플레이 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n M work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "플레이 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "플레이 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 플레이와 점수 계산 구현\"",
          "output": "[codex/e1-2-history-rebuild 9a48940] Feat: 퀴즈 플레이와 점수 계산 구현\n 2 files changed, 47 insertions(+), 5 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "플레이 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 9a48940 (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "플레이 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n M work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "플레이 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "플레이 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 플레이와 점수 계산 구현\"",
          "output": "[codex/e1-2-history-rebuild 9a48940] Feat: 퀴즈 플레이와 점수 계산 구현\n 2 files changed, 47 insertions(+), 5 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "플레이 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 9a48940 (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 플레이와 점수 계산 구현\"",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3"
      ],
      "terminalOutputs": [
        " M work/main.py\n M work/state.json",
        "",
        "[codex/e1-2-history-rebuild 9a48940] Feat: 퀴즈 플레이와 점수 계산 구현\n 2 files changed, 47 insertions(+), 5 deletions(-)",
        "* 9a48940 (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리"
      ],
      "outputs": [
        "Feat: 퀴즈 플레이와 점수 계산 구현 커밋과 Git 로그 확인"
      ],
      "interpretation": "Feat: 퀴즈 플레이와 점수 계산 구현 커밋을 만들고 status/add/commit/log 순서가 실제 터미널 출력으로 남았다.",
      "evidence": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 플레이와 점수 계산 구현\"\n[codex/e1-2-history-rebuild 9a48940] Feat: 퀴즈 플레이와 점수 계산 구현\n 2 files changed, 47 insertions(+), 5 deletions(-)\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 9a48940 (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리",
      "artifacts": []
    },
    {
      "id": "log-17",
      "at": "2026-08-06T06:19:52.281Z",
      "chapter": "추가/목록/삭제/점수 히스토리 검증",
      "theme": "퀴즈 추가 후 state.json 반영 확인",
      "action": "메뉴 2번으로 새 퀴즈를 입력받아 목록과 파일에 저장",
      "purpose": "새 퀴즈를 입력받아 quizzes 리스트에 추가하고 state.json에도 반영되는지 검증",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n",
            "to": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n",
            "to": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"3\"\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n",
            "to": "        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer = int(input(\"정답 번호(1-4): \").strip())\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n",
            "to": "            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "command": "printf '2\\n파이썬 창시자는 누구인가?\\nGuido van Rossum\\nLinus Torvalds\\nJames Gosling\\nBjarne Stroustrup\\n1\\n3\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10819dfd0>, <__main__.Quiz object at 0x10817c910>, <__main__.Quiz object at 0x10817e850>, <__main__.Quiz object at 0x1082008a0>, <__main__.Quiz object at 0x1082016e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 종료\n선택: 문제를 입력하세요: 선택지 1: 선택지 2: 선택지 3: 선택지 4: 정답 번호(1-4): 퀴즈가 추가되었습니다.\n현재 퀴즈 수: 6\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "command": "python3 - <<'PY'\nimport json\nfrom pathlib import Path\npath = Path('state.json')\ndata = json.loads(path.read_text(encoding='utf-8'))\nprint(len(data['quizzes']))\nprint(data['quizzes'][-1]['question'])\nprint(data['quizzes'][-1]['answer'])\nPY",
          "output": "6\n파이썬 창시자는 누구인가?\n1",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n",
            "to": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n"
          }
        },
        {
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n",
            "to": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"3\"\n"
          }
        },
        {
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n",
            "to": "        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer = int(input(\"정답 번호(1-4): \").strip())\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n"
          }
        },
        {
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n",
            "to": "            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "command": "printf '2\\n파이썬 창시자는 누구인가?\\nGuido van Rossum\\nLinus Torvalds\\nJames Gosling\\nBjarne Stroustrup\\n1\\n3\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10819dfd0>, <__main__.Quiz object at 0x10817c910>, <__main__.Quiz object at 0x10817e850>, <__main__.Quiz object at 0x1082008a0>, <__main__.Quiz object at 0x1082016e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 종료\n선택: 문제를 입력하세요: 선택지 1: 선택지 2: 선택지 3: 선택지 4: 정답 번호(1-4): 퀴즈가 추가되었습니다.\n현재 퀴즈 수: 6\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "퀴즈 추가 후 state.json 반영 확인",
          "command": "python3 - <<'PY'\nimport json\nfrom pathlib import Path\npath = Path('state.json')\ndata = json.loads(path.read_text(encoding='utf-8'))\nprint(len(data['quizzes']))\nprint(data['quizzes'][-1]['question'])\nprint(data['quizzes'][-1]['answer'])\nPY",
          "output": "6\n파이썬 창시자는 누구인가?\n1",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "메뉴 2번으로 새 퀴즈를 입력해 quizzes 길이가 6으로 늘었고 state.json 마지막 항목에도 저장됐다."
      ],
      "interpretation": "show_menu와 입력 검사를 3개 메뉴 구조로 확장하고, add_quiz에서 문제·선택지 4개·정답 번호를 입력받아 새 Quiz 객체를 quizzes 리스트에 append한 뒤 save_to_file로 저장했다. 실행 결과에서 퀴즈 추가 메시지와 현재 퀴즈 수 6이 출력됐고, 이후 state.json을 다시 읽어 길이 6과 마지막 질문/정답이 기대값과 일치함을 확인했다.",
      "evidence": "stdout에 퀴즈가 추가되었습니다 / 현재 퀴즈 수: 6 출력, 이후 state.json 검증 출력 6 / 파이썬 창시자는 누구인가? / 1 확인",
      "artifacts": []
    },
    {
      "id": "log-18",
      "at": "2026-08-06T06:43:09.502Z",
      "chapter": "추가/목록/삭제/점수 히스토리 검증",
      "theme": "점수 확인 메뉴로 최고 점수 출력",
      "action": "메뉴 4번으로 현재 최고 점수 확인",
      "purpose": "저장된 최고 점수를 메뉴에서 바로 확인할 수 있는지 검증",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 종료\")\n",
            "to": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 점수 확인\")\n        print(\"5. 종료\")\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"3\"\n",
            "to": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"5\"\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n",
            "to": "    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n",
            "to": "            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.show_best_score()\n                continue\n\n            if self.is_exit_choice(choice):\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "command": "printf '4\\n5\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a76e120>, <__main__.Quiz object at 0x10a74c910>, <__main__.Quiz object at 0x10a74e850>, <__main__.Quiz object at 0x10a7d48a0>, <__main__.Quiz object at 0x10a7d56e0>, <__main__.Quiz object at 0x105d62f90>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 점수 확인\n5. 종료\n선택: 현재 최고 점수: 5\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 점수 확인\n5. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 종료\")\n",
            "to": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 점수 확인\")\n        print(\"5. 종료\")\n"
          }
        },
        {
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"4\"\n",
            "to": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"5\"\n"
          }
        },
        {
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n",
            "to": "    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n"
          }
        },
        {
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if self.is_exit_choice(choice):\n",
            "to": "            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.show_best_score()\n                continue\n\n            if self.is_exit_choice(choice):\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "점수 확인 메뉴로 최고 점수 출력",
          "command": "printf '4\\n5\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a76e120>, <__main__.Quiz object at 0x10a74c910>, <__main__.Quiz object at 0x10a74e850>, <__main__.Quiz object at 0x10a7d48a0>, <__main__.Quiz object at 0x10a7d56e0>, <__main__.Quiz object at 0x105d62f90>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 점수 확인\n5. 종료\n선택: 현재 최고 점수: 5\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 점수 확인\n5. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "메뉴 4번으로 최고 점수 5를 확인하고 다시 메뉴로 복귀한 뒤 종료했다."
      ],
      "interpretation": "show_best_score 메서드를 추가해 저장된 best_score 값을 그대로 출력하도록 했다. 메뉴를 5개 구조로 확장하고 종료 번호를 5로 조정한 뒤, 메뉴 4번에서 현재 최고 점수: 5가 출력되는 것을 확인했다. 즉 점수 확인 기능이 별도 메뉴로 분리되어 동작한다.",
      "evidence": "stdout에 현재 최고 점수: 5 출력 후 메뉴 복귀, 마지막 종료 메시지 확인",
      "artifacts": []
    },
    {
      "id": "log-19",
      "at": "2026-08-06T06:45:18.664Z",
      "chapter": "추가/목록/삭제/점수 히스토리 검증",
      "theme": "삭제 후 파일 반영 확인",
      "action": "메뉴 4번으로 마지막 퀴즈 삭제 후 state.json 반영 확인",
      "purpose": "번호로 퀴즈를 삭제하고 quizzes 길이와 state.json 마지막 항목이 함께 바뀌는지 검증",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "삭제 후 파일 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 점수 확인\")\n        print(\"5. 종료\")\n",
            "to": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "삭제 후 파일 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"5\"\n",
            "to": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "삭제 후 파일 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n",
            "to": "    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "삭제 후 파일 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.show_best_score()\n                continue\n",
            "to": "            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "삭제 후 파일 반영 확인",
          "command": "printf '4\\n6\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a861fd0>, <__main__.Quiz object at 0x10a840910>, <__main__.Quiz object at 0x10a842850>, <__main__.Quiz object at 0x10a8c88a0>, <__main__.Quiz object at 0x10a8c96e0>, <__main__.Quiz object at 0x10a732f90>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 등록된 퀴즈 목록 (총 6개)\n1. Python에서 문자열을 저장하는 자료형은?\n2. Python에서 3 > 1의 결과는 무엇인가?\n3. 여러 값을 순서대로 저장하는 자료형은 무엇인가?\n4. 같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\n5. 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n6. 파이썬 창시자는 누구인가?\n삭제할 퀴즈 번호: 삭제된 퀴즈: 파이썬 창시자는 누구인가?\n현재 퀴즈 수: 5\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "삭제 후 파일 반영 확인",
          "command": "python3 - <<'PY'\nimport json\nfrom pathlib import Path\npath = Path('state.json')\ndata = json.loads(path.read_text(encoding='utf-8'))\nprint(len(data['quizzes']))\nprint(data['quizzes'][-1]['question'])\nPY",
          "output": "5\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "삭제 후 파일 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 점수 확인\")\n        print(\"5. 종료\")\n",
            "to": "    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n"
          }
        },
        {
          "theme": "삭제 후 파일 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"5\"\n",
            "to": "    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n"
          }
        },
        {
          "theme": "삭제 후 파일 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n",
            "to": "    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n"
          }
        },
        {
          "theme": "삭제 후 파일 반영 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.show_best_score()\n                continue\n",
            "to": "            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "삭제 후 파일 반영 확인",
          "command": "printf '4\\n6\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a861fd0>, <__main__.Quiz object at 0x10a840910>, <__main__.Quiz object at 0x10a842850>, <__main__.Quiz object at 0x10a8c88a0>, <__main__.Quiz object at 0x10a8c96e0>, <__main__.Quiz object at 0x10a732f90>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 등록된 퀴즈 목록 (총 6개)\n1. Python에서 문자열을 저장하는 자료형은?\n2. Python에서 3 > 1의 결과는 무엇인가?\n3. 여러 값을 순서대로 저장하는 자료형은 무엇인가?\n4. 같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\n5. 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n6. 파이썬 창시자는 누구인가?\n삭제할 퀴즈 번호: 삭제된 퀴즈: 파이썬 창시자는 누구인가?\n현재 퀴즈 수: 5\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "삭제 후 파일 반영 확인",
          "command": "python3 - <<'PY'\nimport json\nfrom pathlib import Path\npath = Path('state.json')\ndata = json.loads(path.read_text(encoding='utf-8'))\nprint(len(data['quizzes']))\nprint(data['quizzes'][-1]['question'])\nPY",
          "output": "5\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "메뉴 4번으로 마지막 퀴즈를 삭제해 quizzes 길이가 5로 줄었고 state.json 마지막 항목도 이전 퀴즈로 바뀌었다."
      ],
      "interpretation": "delete_quiz에서 show_quiz_list로 현재 번호를 보여준 뒤, 입력받은 번호에서 1을 빼 리스트 인덱스로 사용해 퀴즈를 pop했다. 삭제 후 save_to_file로 즉시 저장하고 삭제된 문제와 현재 길이를 출력했다. 이후 state.json을 다시 읽어 길이 5와 마지막 질문이 기대한 값으로 바뀐 것을 확인해 파일 반영까지 검증됐다.",
      "evidence": "stdout에 삭제된 퀴즈: 파이썬 창시자는 누구인가? / 현재 퀴즈 수: 5 출력, 이후 state.json 검증 출력 5 / 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는? 확인",
      "artifacts": []
    },
    {
      "id": "log-20",
      "at": "2026-08-06T07:01:36.885Z",
      "chapter": "추가/목록/삭제/점수 히스토리 검증",
      "theme": "추가/삭제 입력 범위 예외 처리 보강",
      "action": "정답 번호와 삭제 번호의 빈 입력/문자/범위 밖 숫자 방어",
      "purpose": "퀴즈 추가와 삭제에서 잘못된 번호 입력이 들어와도 비정상 종료하지 않고 안내 메시지 후 메뉴로 복귀하는지 검증",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "추가/삭제 입력 범위 예외 처리 보강",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer = int(input(\"정답 번호(1-4): \").strip())\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n",
            "to": "    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n"
          }
        },
        {
          "type": "editor-change",
          "theme": "추가/삭제 입력 범위 예외 처리 보강",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n",
            "to": "    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "추가/삭제 입력 범위 예외 처리 보강",
          "command": "printf '2\\n테스트 문제\\nA\\nB\\nC\\nD\\n9\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a985fd0>, <__main__.Quiz object at 0x10a964910>, <__main__.Quiz object at 0x10a966850>, <__main__.Quiz object at 0x10a9ec8a0>, <__main__.Quiz object at 0x10a9ed6e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 문제를 입력하세요: 선택지 1: 선택지 2: 선택지 3: 선택지 4: 정답 번호(1-4): 정답 번호는 1부터 4까지만 입력할 수 있습니다.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "추가/삭제 입력 범위 예외 처리 보강",
          "command": "printf '4\\n9\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a101fd0>, <__main__.Quiz object at 0x10a0e0910>, <__main__.Quiz object at 0x10a0e2850>, <__main__.Quiz object at 0x10a1688a0>, <__main__.Quiz object at 0x10a1696e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 등록된 퀴즈 목록 (총 5개)\n1. Python에서 문자열을 저장하는 자료형은?\n2. Python에서 3 > 1의 결과는 무엇인가?\n3. 여러 값을 순서대로 저장하는 자료형은 무엇인가?\n4. 같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\n5. 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n삭제할 퀴즈 번호: 삭제할 수 있는 퀴즈 번호만 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "추가/삭제 입력 범위 예외 처리 보강",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer = int(input(\"정답 번호(1-4): \").strip())\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n",
            "to": "    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n"
          }
        },
        {
          "theme": "추가/삭제 입력 범위 예외 처리 보강",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "replace",
            "from": "    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n",
            "to": "    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "추가/삭제 입력 범위 예외 처리 보강",
          "command": "printf '2\\n테스트 문제\\nA\\nB\\nC\\nD\\n9\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a985fd0>, <__main__.Quiz object at 0x10a964910>, <__main__.Quiz object at 0x10a966850>, <__main__.Quiz object at 0x10a9ec8a0>, <__main__.Quiz object at 0x10a9ed6e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 문제를 입력하세요: 선택지 1: 선택지 2: 선택지 3: 선택지 4: 정답 번호(1-4): 정답 번호는 1부터 4까지만 입력할 수 있습니다.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "추가/삭제 입력 범위 예외 처리 보강",
          "command": "printf '4\\n9\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a101fd0>, <__main__.Quiz object at 0x10a0e0910>, <__main__.Quiz object at 0x10a0e2850>, <__main__.Quiz object at 0x10a1688a0>, <__main__.Quiz object at 0x10a1696e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 등록된 퀴즈 목록 (총 5개)\n1. Python에서 문자열을 저장하는 자료형은?\n2. Python에서 3 > 1의 결과는 무엇인가?\n3. 여러 값을 순서대로 저장하는 자료형은 무엇인가?\n4. 같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\n5. 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n삭제할 퀴즈 번호: 삭제할 수 있는 퀴즈 번호만 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "잘못된 정답 번호와 삭제 번호를 입력해도 프로그램이 종료되지 않고 안내 메시지 후 메뉴로 복귀했다."
      ],
      "interpretation": "add_quiz와 delete_quiz 모두 입력을 먼저 문자열로 받아 빈 값, 숫자 여부, 허용 범위를 순서대로 검사하도록 바꿨다. 실제 실행에서 정답 번호 9 입력 시 1~4 범위 안내가, 삭제 번호 9 입력 시 현재 목록 범위 안내가 출력되고 프로그램은 메뉴로 복귀했다. 즉 추가/삭제 입력 예외가 비정상 종료 대신 안내 메시지 흐름으로 처리됨을 확인했다.",
      "evidence": "stdout에 정답 번호는 1부터 4까지만 입력할 수 있습니다. / 삭제할 수 있는 퀴즈 번호만 입력해주세요. 출력 확인",
      "artifacts": []
    },
    {
      "id": "log-29",
      "at": "2026-08-06T12:34:24.362Z",
      "chapter": "추가/목록/삭제/점수 히스토리 검증",
      "theme": "문제 관리 기능 완성 후 commit 기록",
      "action": "퀴즈 추가/삭제와 점수 메뉴 기능을 Git 기능 단위 커밋으로 기록",
      "purpose": "문제 관리와 점수 확인 메뉴를 완성한 뒤 실제 Git 명령으로 작업 이력을 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "문제 관리 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n M work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "문제 관리 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "문제 관리 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\"",
          "output": "[codex/e1-2-history-rebuild baeba2e] Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n 2 files changed, 138 insertions(+), 4 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "문제 관리 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* baeba2e (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "문제 관리 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n M work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "문제 관리 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "문제 관리 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\"",
          "output": "[codex/e1-2-history-rebuild baeba2e] Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n 2 files changed, 138 insertions(+), 4 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "문제 관리 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* baeba2e (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\"",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3"
      ],
      "terminalOutputs": [
        " M work/main.py\n M work/state.json",
        "",
        "[codex/e1-2-history-rebuild baeba2e] Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n 2 files changed, 138 insertions(+), 4 deletions(-)",
        "* baeba2e (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가"
      ],
      "outputs": [
        "Feat: 퀴즈 추가 삭제와 점수 메뉴 구현 커밋과 Git 로그 확인"
      ],
      "interpretation": "Feat: 퀴즈 추가 삭제와 점수 메뉴 구현 커밋을 만들고 status/add/commit/log 순서가 실제 터미널 출력으로 남았다.",
      "evidence": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\"\n[codex/e1-2-history-rebuild baeba2e] Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n 2 files changed, 138 insertions(+), 4 deletions(-)\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* baeba2e (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가",
      "artifacts": []
    },
    {
      "id": "log-21",
      "at": "2026-08-06T11:07:46.914Z",
      "chapter": "퀴즈 플레이 기능과 브랜치 병합",
      "theme": "랜덤 출제와 문제 수 선택 적용",
      "action": "문제 수를 선택해 랜덤 문제만 출제하고 잘못된 입력은 메뉴로 복귀",
      "purpose": "random.sample 기반 출제와 문제 수 입력 검증이 함께 동작하는지 확인",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "랜덤 출제와 문제 수 선택 적용",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "overwrite",
            "from": "",
            "to": "import json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "랜덤 출제와 문제 수 선택 적용",
          "command": "printf '1\\n2\\n2\\n2\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a96d7f0>, <__main__.Quiz object at 0x106011f90>, <__main__.Quiz object at 0x106013750>, <__main__.Quiz object at 0x10a928770>, <__main__.Quiz object at 0x10a92ab10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 0}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 2문제를 랜덤으로 출제합니다.\n여러 값을 순서대로 저장하는 자료형은 무엇인가?\ndict\nlist\nstr\nint\n2\n정답 번호: 정답입니다.\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 2\n현재 최고 점수: 2\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "랜덤 출제와 문제 수 선택 적용",
          "command": "printf '1\\n9\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x105f757f0>, <__main__.Quiz object at 0x1015e1f90>, <__main__.Quiz object at 0x1015e3750>, <__main__.Quiz object at 0x105f30770>, <__main__.Quiz object at 0x105f32b10>]\n2\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 풀 수 있는 문제 수만 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "랜덤 출제와 문제 수 선택 적용",
          "command": "printf '1\\nabc\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x1081b57f0>, <__main__.Quiz object at 0x10385df90>, <__main__.Quiz object at 0x10385f750>, <__main__.Quiz object at 0x108170770>, <__main__.Quiz object at 0x108172b10>]\n2\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 문제 수는 숫자로 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "랜덤 출제와 문제 수 선택 적용",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "overwrite",
            "from": "",
            "to": "import json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "랜덤 출제와 문제 수 선택 적용",
          "command": "printf '1\\n2\\n2\\n2\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a96d7f0>, <__main__.Quiz object at 0x106011f90>, <__main__.Quiz object at 0x106013750>, <__main__.Quiz object at 0x10a928770>, <__main__.Quiz object at 0x10a92ab10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 0}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 2문제를 랜덤으로 출제합니다.\n여러 값을 순서대로 저장하는 자료형은 무엇인가?\ndict\nlist\nstr\nint\n2\n정답 번호: 정답입니다.\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 2\n현재 최고 점수: 2\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "랜덤 출제와 문제 수 선택 적용",
          "command": "printf '1\\n9\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x105f757f0>, <__main__.Quiz object at 0x1015e1f90>, <__main__.Quiz object at 0x1015e3750>, <__main__.Quiz object at 0x105f30770>, <__main__.Quiz object at 0x105f32b10>]\n2\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 풀 수 있는 문제 수만 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "랜덤 출제와 문제 수 선택 적용",
          "command": "printf '1\\nabc\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x1081b57f0>, <__main__.Quiz object at 0x10385df90>, <__main__.Quiz object at 0x10385f750>, <__main__.Quiz object at 0x108170770>, <__main__.Quiz object at 0x108172b10>]\n2\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 문제 수는 숫자로 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "선택한 문제 수만큼 랜덤 출제되고, 범위 밖 숫자와 문자 입력은 메뉴로 안전하게 복귀했다."
      ],
      "interpretation": "ask_quiz_count가 빈 입력, 숫자 여부, 허용 범위를 먼저 검사하고 통과한 값만 play_all_quizzes로 넘긴다. pick_random_quizzes는 random.sample로 중복 없이 원하는 개수만 골라냈고, 실제 실행에서 2문제만 출제되는 흐름과 잘못된 입력 후 메뉴 복귀를 함께 확인했다.",
      "evidence": "stdout에 2문제를 랜덤으로 출제합니다. / 풀 수 있는 문제 수만 입력해주세요. / 문제 수는 숫자로 입력해주세요. 출력 확인",
      "artifacts": []
    },
    {
      "id": "log-22",
      "at": "2026-08-06T11:07:46.945Z",
      "chapter": "퀴즈 플레이 기능과 브랜치 병합",
      "theme": "힌트 사용과 점수 차감 확인",
      "action": "힌트를 보고 정답을 맞혀도 점수가 오르지 않는지 확인",
      "purpose": "Quiz 힌트 출력과 힌트 사용 시 점수 차감 규칙이 함께 동작하는지 검증",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "힌트 사용과 점수 차감 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "overwrite",
            "from": "",
            "to": "import json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "힌트 사용과 점수 차감 확인",
          "command": "printf '1\\n1\\ny\\n3\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10406d7f0>, <__main__.Quiz object at 0x103f9df90>, <__main__.Quiz object at 0x103f9f750>, <__main__.Quiz object at 0x104030770>, <__main__.Quiz object at 0x104032b10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 1문제를 랜덤으로 출제합니다.\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\nbreak\nclass\nreturn\nimport\n3\n힌트를 볼까요? (y/n): 힌트: 함수 바깥으로 값을 다시 보내는 단어입니다.\n힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\n정답 번호: 정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\n이번 점수: 0\n현재 최고 점수: 0\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "힌트 사용과 점수 차감 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "overwrite",
            "from": "",
            "to": "import json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "힌트 사용과 점수 차감 확인",
          "command": "printf '1\\n1\\ny\\n3\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x10406d7f0>, <__main__.Quiz object at 0x103f9df90>, <__main__.Quiz object at 0x103f9f750>, <__main__.Quiz object at 0x104030770>, <__main__.Quiz object at 0x104032b10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 1문제를 랜덤으로 출제합니다.\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\nbreak\nclass\nreturn\nimport\n3\n힌트를 볼까요? (y/n): 힌트: 함수 바깥으로 값을 다시 보내는 단어입니다.\n힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\n정답 번호: 정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\n이번 점수: 0\n현재 최고 점수: 0\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "실제 힌트 문구가 출력되고, 힌트를 사용한 정답은 점수에 반영되지 않았다."
      ],
      "interpretation": "Quiz는 hint 속성과 show_hint 메서드를 가지게 되었고, QuizGame의 ask_hint_usage가 y/n 입력을 받아 힌트 사용 여부를 불리언으로 돌려준다. play_all_quizzes는 정답 여부를 확인한 뒤 used_hint가 True이면 score를 올리지 않고 안내 메시지만 출력했다.",
      "evidence": "stdout에 힌트: 함수 바깥으로 값을 다시 보내는 단어입니다. / 정답이지만 힌트를 사용해 점수는 올라가지 않습니다. / 이번 점수: 0 확인",
      "artifacts": []
    },
    {
      "id": "log-30",
      "at": "2026-08-06T12:34:24.478Z",
      "chapter": "퀴즈 플레이 기능과 브랜치 병합",
      "theme": "보너스 기능 완성 후 commit 기록",
      "action": "랜덤 출제와 힌트 기능을 Git 기능 단위 커밋으로 기록",
      "purpose": "문제 수 선택, 랜덤 출제, 힌트 기능을 완성한 뒤 실제 Git 명령으로 작업 이력을 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "보너스 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n M work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "보너스 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "보너스 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 랜덤 출제와 힌트 기능 추가\"",
          "output": "[codex/e1-2-history-rebuild 599708f] Feat: 랜덤 출제와 힌트 기능 추가\n 2 files changed, 129 insertions(+), 44 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "보너스 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 599708f (HEAD -> codex/e1-2-history-rebuild) Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "보너스 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n M work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "보너스 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "보너스 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 랜덤 출제와 힌트 기능 추가\"",
          "output": "[codex/e1-2-history-rebuild 599708f] Feat: 랜덤 출제와 힌트 기능 추가\n 2 files changed, 129 insertions(+), 44 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "보너스 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 599708f (HEAD -> codex/e1-2-history-rebuild) Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 랜덤 출제와 힌트 기능 추가\"",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3"
      ],
      "terminalOutputs": [
        " M work/main.py\n M work/state.json",
        "",
        "[codex/e1-2-history-rebuild 599708f] Feat: 랜덤 출제와 힌트 기능 추가\n 2 files changed, 129 insertions(+), 44 deletions(-)",
        "* 599708f (HEAD -> codex/e1-2-history-rebuild) Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현"
      ],
      "outputs": [
        "Feat: 랜덤 출제와 힌트 기능 추가 커밋과 Git 로그 확인"
      ],
      "interpretation": "Feat: 랜덤 출제와 힌트 기능 추가 커밋을 만들고 status/add/commit/log 순서가 실제 터미널 출력으로 남았다.",
      "evidence": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 랜덤 출제와 힌트 기능 추가\"\n[codex/e1-2-history-rebuild 599708f] Feat: 랜덤 출제와 힌트 기능 추가\n 2 files changed, 129 insertions(+), 44 deletions(-)\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 599708f (HEAD -> codex/e1-2-history-rebuild) Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현",
      "artifacts": []
    },
    {
      "id": "log-23",
      "at": "2026-08-06T11:07:46.976Z",
      "chapter": "추가/목록/삭제/점수 히스토리 검증",
      "theme": "날짜/시간 포함 게임 기록 저장 확인",
      "action": "플레이 결과를 score_history에 저장하고 메뉴에서 다시 조회",
      "purpose": "날짜/시간, 푼 문제 수, 점수가 state.json과 플레이 기록 메뉴에 함께 남는지 확인",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "editor-change",
          "theme": "날짜/시간 포함 게임 기록 저장 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "overwrite",
            "from": "",
            "to": "import json\nimport random\nfrom datetime import datetime\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록, 최고 점수, 플레이 기록을 관리한다.\n    def __init__(self, quizzes, best_score, score_history):\n        self.quizzes = quizzes\n        self.best_score = best_score\n        self.score_history = score_history\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 플레이 기록\")\n        print(\"7. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\", \"7\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"7\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 이번 플레이 결과를 날짜/시간과 함께 기록 목록에 추가한다.\n    def record_score_history(self, total_questions, score):\n        self.score_history.append(\n            {\n                \"played_at\": datetime.now().isoformat(timespec=\"seconds\"),\n                \"total_questions\": total_questions,\n                \"score\": score,\n            }\n        )\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.record_score_history(count, score)\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 저장된 플레이 기록을 최근 순서대로 출력한다.\n    def show_score_history(self):\n        if len(self.score_history) == 0:\n            print(\"아직 저장된 플레이 기록이 없습니다.\")\n            return\n\n        print(\"플레이 기록\")\n        for number, history in enumerate(self.score_history, start=1):\n            print(\n                f\"{number}. {history['played_at']} | \"\n                f\"{history['total_questions']}문제 | \"\n                f\"{history['score']}점\"\n            )\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n            if choice == \"6\":\n                self.show_score_history()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n            \"score_history\": self.score_history,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"], data.get(\"score_history\", []))\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n"
          }
        },
        {
          "type": "terminal-step",
          "theme": "날짜/시간 포함 게임 기록 저장 확인",
          "command": "printf '1\\n1\\nn\\n2\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x1086a17f0>, <__main__.Quiz object at 0x103c99f90>, <__main__.Quiz object at 0x103c9b750>, <__main__.Quiz object at 0x108668770>, <__main__.Quiz object at 0x10866ab10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0, 'score_history': []}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0, 'score_history': []}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 1문제를 랜덤으로 출제합니다.\n같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\nif\nfor\nprint\ninput\n2\n힌트를 볼까요? (y/n): 정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 1\n현재 최고 점수: 1\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "날짜/시간 포함 게임 기록 저장 확인",
          "command": "python3 -m json.tool state.json",
          "output": "{\n    \"quizzes\": [\n        {\n            \"question\": \"Python에서 문자열을 저장하는 자료형은?\",\n            \"choices\": [\n                \"int\",\n                \"str\",\n                \"bool\",\n                \"list\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"문자열은 따옴표로 감싸 자주 표현합니다.\"\n        },\n        {\n            \"question\": \"Python에서 3 > 1의 결과는 무엇인가?\",\n            \"choices\": [\n                \"0\",\n                \"False\",\n                \"True\",\n                \"None\"\n            ],\n            \"answer\": 3,\n            \"hint\": \"비교 연산의 결과는 참 또는 거짓입니다.\"\n        },\n        {\n            \"question\": \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n            \"choices\": [\n                \"dict\",\n                \"list\",\n                \"str\",\n                \"int\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\"\n        },\n        {\n            \"question\": \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n            \"choices\": [\n                \"if\",\n                \"for\",\n                \"print\",\n                \"input\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"반복문 문법을 고르면 됩니다.\"\n        },\n        {\n            \"question\": \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n            \"choices\": [\n                \"break\",\n                \"class\",\n                \"return\",\n                \"import\"\n            ],\n            \"answer\": 3,\n            \"hint\": \"함수 바깥으로 값을 다시 보내는 단어입니다.\"\n        }\n    ],\n    \"best_score\": 1,\n    \"score_history\": [\n        {\n            \"played_at\": \"2026-08-06T19:55:55\",\n            \"total_questions\": 1,\n            \"score\": 1\n        }\n    ]\n}",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "날짜/시간 포함 게임 기록 저장 확인",
          "command": "printf '6\\n7\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x1083a17f0>, <__main__.Quiz object at 0x103971f90>, <__main__.Quiz object at 0x103973750>, <__main__.Quiz object at 0x1083688a0>, <__main__.Quiz object at 0x10836ac40>]\n1\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 1, 'score_history': [{'played_at': '2026-08-06T19:55:55', 'total_questions': 1, 'score': 1}]}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 1, 'score_history': [{'played_at': '2026-08-06T19:55:55', 'total_questions': 1, 'score': 1}]}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 플레이 기록\n7. 종료\n선택: 플레이 기록\n1. 2026-08-06T19:55:55 | 1문제 | 1점\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 플레이 기록\n7. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [
        {
          "theme": "날짜/시간 포함 게임 기록 저장 확인",
          "file": "main.py",
          "language": "python",
          "change": {
            "kind": "overwrite",
            "from": "",
            "to": "import json\nimport random\nfrom datetime import datetime\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록, 최고 점수, 플레이 기록을 관리한다.\n    def __init__(self, quizzes, best_score, score_history):\n        self.quizzes = quizzes\n        self.best_score = best_score\n        self.score_history = score_history\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 플레이 기록\")\n        print(\"7. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\", \"7\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"7\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 이번 플레이 결과를 날짜/시간과 함께 기록 목록에 추가한다.\n    def record_score_history(self, total_questions, score):\n        self.score_history.append(\n            {\n                \"played_at\": datetime.now().isoformat(timespec=\"seconds\"),\n                \"total_questions\": total_questions,\n                \"score\": score,\n            }\n        )\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.record_score_history(count, score)\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 저장된 플레이 기록을 최근 순서대로 출력한다.\n    def show_score_history(self):\n        if len(self.score_history) == 0:\n            print(\"아직 저장된 플레이 기록이 없습니다.\")\n            return\n\n        print(\"플레이 기록\")\n        for number, history in enumerate(self.score_history, start=1):\n            print(\n                f\"{number}. {history['played_at']} | \"\n                f\"{history['total_questions']}문제 | \"\n                f\"{history['score']}점\"\n            )\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n            if choice == \"6\":\n                self.show_score_history()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n            \"score_history\": self.score_history,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"], data.get(\"score_history\", []))\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n"
          }
        }
      ],
      "terminalSteps": [
        {
          "theme": "날짜/시간 포함 게임 기록 저장 확인",
          "command": "printf '1\\n1\\nn\\n2\\n6\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x1086a17f0>, <__main__.Quiz object at 0x103c99f90>, <__main__.Quiz object at 0x103c9b750>, <__main__.Quiz object at 0x108668770>, <__main__.Quiz object at 0x10866ab10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0, 'score_history': []}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0, 'score_history': []}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 1문제를 랜덤으로 출제합니다.\n같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\nif\nfor\nprint\ninput\n2\n힌트를 볼까요? (y/n): 정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 1\n현재 최고 점수: 1\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "날짜/시간 포함 게임 기록 저장 확인",
          "command": "python3 -m json.tool state.json",
          "output": "{\n    \"quizzes\": [\n        {\n            \"question\": \"Python에서 문자열을 저장하는 자료형은?\",\n            \"choices\": [\n                \"int\",\n                \"str\",\n                \"bool\",\n                \"list\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"문자열은 따옴표로 감싸 자주 표현합니다.\"\n        },\n        {\n            \"question\": \"Python에서 3 > 1의 결과는 무엇인가?\",\n            \"choices\": [\n                \"0\",\n                \"False\",\n                \"True\",\n                \"None\"\n            ],\n            \"answer\": 3,\n            \"hint\": \"비교 연산의 결과는 참 또는 거짓입니다.\"\n        },\n        {\n            \"question\": \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n            \"choices\": [\n                \"dict\",\n                \"list\",\n                \"str\",\n                \"int\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\"\n        },\n        {\n            \"question\": \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n            \"choices\": [\n                \"if\",\n                \"for\",\n                \"print\",\n                \"input\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"반복문 문법을 고르면 됩니다.\"\n        },\n        {\n            \"question\": \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n            \"choices\": [\n                \"break\",\n                \"class\",\n                \"return\",\n                \"import\"\n            ],\n            \"answer\": 3,\n            \"hint\": \"함수 바깥으로 값을 다시 보내는 단어입니다.\"\n        }\n    ],\n    \"best_score\": 1,\n    \"score_history\": [\n        {\n            \"played_at\": \"2026-08-06T19:55:55\",\n            \"total_questions\": 1,\n            \"score\": 1\n        }\n    ]\n}",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "날짜/시간 포함 게임 기록 저장 확인",
          "command": "printf '6\\n7\\n' | python3 main.py",
          "output": "퀴즈 게임 시작\n[<__main__.Quiz object at 0x1083a17f0>, <__main__.Quiz object at 0x103971f90>, <__main__.Quiz object at 0x103973750>, <__main__.Quiz object at 0x1083688a0>, <__main__.Quiz object at 0x10836ac40>]\n1\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 1, 'score_history': [{'played_at': '2026-08-06T19:55:55', 'total_questions': 1, 'score': 1}]}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 1, 'score_history': [{'played_at': '2026-08-06T19:55:55', 'total_questions': 1, 'score': 1}]}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 플레이 기록\n7. 종료\n선택: 플레이 기록\n1. 2026-08-06T19:55:55 | 1문제 | 1점\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 플레이 기록\n7. 종료\n선택: 프로그램을 종료합니다.",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "score_history에 played_at, total_questions, score가 저장되고 메뉴 6번에서 기록 목록을 다시 확인했다."
      ],
      "interpretation": "record_score_history가 플레이가 끝난 직후 현재 시각, 문제 수, 점수를 딕셔너리로 만들어 score_history에 append한다. to_dict와 load_from_file도 이 배열을 저장/복원하도록 바뀌었고, show_score_history가 메뉴 6번에서 날짜/시간과 점수를 사람이 읽기 쉬운 줄 단위로 출력했다.",
      "evidence": "state.json에 score_history 배열과 played_at / total_questions / score 필드가 저장됐고, 메뉴 출력에 2026-08-06T19:55:55 | 1문제 | 1점 확인",
      "artifacts": []
    },
    {
      "id": "log-31",
      "at": "2026-08-06T12:34:24.584Z",
      "chapter": "추가/목록/삭제/점수 히스토리 검증",
      "theme": "플레이 기록 기능 완성 후 commit 기록",
      "action": "플레이 기록 저장 기능을 Git 기능 단위 커밋으로 기록",
      "purpose": "날짜/시간 포함 플레이 기록 저장 기능을 완성한 뒤 실제 Git 명령으로 작업 이력을 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "플레이 기록 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n M work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "플레이 기록 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "플레이 기록 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 플레이 기록 저장 기능 추가\"",
          "output": "[codex/e1-2-history-rebuild 9d2cee1] Feat: 플레이 기록 저장 기능 추가\n 2 files changed, 47 insertions(+), 10 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "플레이 기록 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 9d2cee1 (HEAD -> codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "플레이 기록 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
          "output": " M work/main.py\n M work/state.json",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "플레이 기록 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
          "output": "",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "플레이 기록 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 플레이 기록 저장 기능 추가\"",
          "output": "[codex/e1-2-history-rebuild 9d2cee1] Feat: 플레이 기록 저장 기능 추가\n 2 files changed, 47 insertions(+), 10 deletions(-)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "플레이 기록 기능 완성 후 commit 기록",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3",
          "output": "* 9d2cee1 (HEAD -> codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 플레이 기록 저장 기능 추가\"",
        "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3"
      ],
      "terminalOutputs": [
        " M work/main.py\n M work/state.json",
        "",
        "[codex/e1-2-history-rebuild 9d2cee1] Feat: 플레이 기록 저장 기능 추가\n 2 files changed, 47 insertions(+), 10 deletions(-)",
        "* 9d2cee1 (HEAD -> codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현"
      ],
      "outputs": [
        "Feat: 플레이 기록 저장 기능 추가 커밋과 Git 로그 확인"
      ],
      "interpretation": "Feat: 플레이 기록 저장 기능 추가 커밋을 만들고 status/add/commit/log 순서가 실제 터미널 출력으로 남았다.",
      "evidence": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 플레이 기록 저장 기능 추가\"\n[codex/e1-2-history-rebuild 9d2cee1] Feat: 플레이 기록 저장 기능 추가\n 2 files changed, 47 insertions(+), 10 deletions(-)\n\ngit -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 9d2cee1 (HEAD -> codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현",
      "artifacts": []
    },
    {
      "id": "log-32",
      "at": "2026-08-06T13:21:04.533Z",
      "chapter": "README, Git 로그, clone/pull 제출 증거",
      "theme": "README 필수 항목 작성",
      "action": "과제 저장소 루트 README에 필수 항목을 정리했다.",
      "purpose": "프로젝트 개요, 주제 선정 이유, 실행 방법, 기능 목록, 파일 구조, 데이터 파일 설명을 README에 포함한다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "README 필수 항목 작성",
          "command": "cat README.md",
          "output": "# E1-2 파이썬 이해하기\n\n- 발표 링크: 발표용 HTML 링크 준비 중\n\n## 챕터\n\n- Chapter 1. 메뉴와 공통 입력 예외 처리\n- Chapter 2. Quiz 클래스와 객체 기초\n- Chapter 3. QuizGame 클래스와 기능별 메서드 분리\n- Chapter 4. 파일 입출력과 데이터 영속성\n- Chapter 5. 퀴즈 플레이 기능과 브랜치 병합\n- Chapter 6. 추가/목록/삭제/점수 히스토리 검증\n- Chapter 7. README, Git 로그, clone/pull 제출 증거\n\n## 실습 로그\n\n## Chapter 1. 메뉴와 공통 입력 예외 처리\n\n### 테마\n\n- while 반복 메뉴와 종료 흐름 구현\n- 빈 입력/공백/문자/범위 밖 숫자 처리\n- 메뉴 기능 완성 후 commit 기록\n\n### while 반복 메뉴와 종료 흐름 구현\n\n`main.py`\n\n```python\ndef main():\n    print(\"퀴즈 게임 시작\")\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        choice = input(\"선택: \")\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### while 반복 메뉴와 종료 흐름 구현\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n`main.py`\n\n```python\ndef main():\n    print(\"퀴즈 게임 시작\")\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n```bash\n$ printf '\\n2\\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 입력이 비어 있습니다. 다시 선택하세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n```bash\n$ printf 'abc\\n2\\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 숫자로 입력해주세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n```bash\n$ printf '9\\n2\\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 1 또는 2만 입력할 수 있습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n```bash\n$ printf ' 2 \\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 메뉴 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\nA  .gitignore\nA  work/main.py\n```\n\n### 메뉴 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 메뉴 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 반복 메뉴와 종료 흐름 구현\"\n[codex/e1-2-history-rebuild (root-commit) 27884fe] Feat: 반복 메뉴와 종료 흐름 구현\n 2 files changed, 31 insertions(+)\n create mode 100644 .gitignore\n create mode 100644 work/main.py\n```\n\n### 메뉴 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph\n* 27884fe Feat: 반복 메뉴와 종료 흐름 구현\n```\n\n\n## Chapter 2. Quiz 클래스와 객체 기초\n\n### 테마\n\n- Quiz 클래스 뼈대 정의\n- Quiz 객체 생성과 속성 확인\n- Quiz 메서드로 출력 책임 이동\n- 정답 비교 메서드와 불리언 결과 확인\n- Quiz 기능 완성 후 commit 기록\n\n### Quiz 클래스 뼈대 정의\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n\ndef main():\n    print(\"퀴즈 게임 시작\")\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### Quiz 객체 생성과 속성 확인\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 직접 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(quiz1.question)\n    print(quiz1.choices)\n    print(quiz1.answer)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### Quiz 객체 생성과 속성 확인\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\n['int', 'str', 'bool', 'list']\n2\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### Quiz 메서드로 출력 책임 이동\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 직접 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(quiz1.question)\n    print(quiz1.choices)\n    print(quiz1.answer)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### Quiz 메서드로 출력 책임 이동\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 메서드로 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### Quiz 메서드로 출력 책임 이동\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 정답 비교 메서드와 불리언 결과 확인\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 메서드로 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 정답 비교 메서드와 불리언 결과 확인\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 정답 비교 메서드와 불리언 결과 확인\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### Quiz 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n```\n\n### Quiz 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### Quiz 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: Quiz 클래스와 정답 판정 구현\"\n[codex/e1-2-history-rebuild 7e2694f] Feat: Quiz 클래스와 정답 판정 구현\n 1 file changed, 30 insertions(+)\n```\n\n### Quiz 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 7e2694f (HEAD -> codex/e1-2-history-rebuild) Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현\n```\n\n\n## Chapter 3. QuizGame 클래스와 기능별 메서드 분리\n\n### 테마\n\n- 퀴즈 목록과 최고 점수 속성 묶기\n- QuizGame 메서드로 메뉴 출력 책임 옮기기\n- QuizGame 메서드로 메뉴 입력 검사 책임 옮기기\n- QuizGame 메서드로 종료 판단 책임 옮기기\n- 한 함수에 몰리지 않는 구조 점검\n- QuizGame 구조 완성 후 commit 기록\n\n### 퀴즈 목록과 최고 점수 속성 묶기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 퀴즈 목록과 최고 점수 속성 묶기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 퀴즈 목록과 최고 점수 속성 묶기\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x1033bc590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### QuizGame 메서드로 메뉴 출력 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 메뉴 출력 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 메뉴 출력 책임 옮기기\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x109e88590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### QuizGame 메서드로 메뉴 입력 검사 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 메뉴 입력 검사 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 메뉴 입력 검사 책임 옮기기\n\n```bash\n$ printf '\\n2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x105b44590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 입력이 비어 있습니다. 다시 선택하세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### QuizGame 메서드로 종료 판단 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 종료 판단 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not game.is_digit_choice(choice):\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if not game.is_valid_menu_choice(choice):\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if game.is_exit_choice(choice):\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 종료 판단 책임 옮기기\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x10386c590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 한 함수에 몰리지 않는 구조 점검\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not game.is_digit_choice(choice):\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if not game.is_valid_menu_choice(choice):\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if game.is_exit_choice(choice):\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 한 함수에 몰리지 않는 구조 점검\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 한 함수에 몰리지 않는 구조 점검\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x104308590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### QuizGame 구조 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n```\n\n### QuizGame 구조 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### QuizGame 구조 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Refactor: QuizGame으로 메뉴 책임 분리\"\n[codex/e1-2-history-rebuild 054e3c8] Refactor: QuizGame으로 메뉴 책임 분리\n 1 file changed, 58 insertions(+), 23 deletions(-)\n```\n\n### QuizGame 구조 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 054e3c8 (HEAD -> codex/e1-2-history-rebuild) Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현\n```\n\n\n## Chapter 4. 파일 입출력과 데이터 영속성\n\n### 테마\n\n- state.json에서 게임 상태 불러오기\n- 손상된 state.json에서도 기본값으로 복구하기\n- 저장 복구 기능 완성 후 commit 기록\n\n### state.json에서 게임 상태 불러오기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### state.json에서 게임 상태 불러오기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### state.json에서 게임 상태 불러오기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### state.json에서 게임 상태 불러오기\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x104145fd0>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 손상된 state.json에서도 기본값으로 복구하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 손상된 state.json에서도 기본값으로 복구하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 손상된 state.json에서도 기본값으로 복구하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 손상된 state.json에서도 기본값으로 복구하기\n\n```bash\n$ tmpdir=$(mktemp -d /private/tmp/e1-2-corrupt-XXXXXX) && cp /Users/hskim/Projects/codyssey/artifacts/e1-2/work/main.py \"$tmpdir/main.py\" && printf '{ broken json\\n' > \"$tmpdir/state.json\" && cd \"$tmpdir\" && printf '2\\n' | python3 main.py\n저장 파일이 손상되어 기본 퀴즈로 복구합니다.\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x10178a120>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 저장 복구 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n?? work/state.json\n```\n\n### 저장 복구 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 저장 복구 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: state.json 저장과 복구 처리 추가\"\n[codex/e1-2-history-rebuild a5d5759] Feat: state.json 저장과 복구 처리 추가\n 2 files changed, 99 insertions(+), 7 deletions(-)\n create mode 100644 work/state.json\n```\n\n### 저장 복구 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* a5d5759 (HEAD -> codex/e1-2-history-rebuild) Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n```\n\n\n## Chapter 5. 퀴즈 플레이 기능과 브랜치 병합\n\n### 테마\n\n- 퀴즈 1개를 실제로 출제하고 정답 판정하기\n- 모든 퀴즈를 순서대로 출제하고 점수 합산하기\n- 퀴즈가 없는 경우 안내 후 메뉴 복귀\n- 플레이 기능 완성 후 commit 기록\n- 랜덤 출제와 문제 수 선택 적용\n- 힌트 사용과 점수 차감 확인\n- 보너스 기능 완성 후 commit 기록\n\n### 퀴즈 1개를 실제로 출제하고 정답 판정하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 첫 번째 퀴즈를 출제하고 정답 여부를 확인한다.\n    def play_first_quiz(self):\n        quiz = self.quizzes[0]\n        quiz.show()\n        answer = int(input(\"정답 번호: \").strip())\n\n        if quiz.check_answer(answer):\n            print(\"정답입니다.\")\n            self.update_best_score(1)\n            self.save_to_file()\n            print(f\"현재 최고 점수: {self.best_score}\")\n            return\n\n        print(\"오답입니다.\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈 1개를 실제로 출제하고 정답 판정하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 첫 번째 퀴즈를 출제하고 정답 여부를 확인한다.\n    def play_first_quiz(self):\n        quiz = self.quizzes[0]\n        quiz.show()\n        answer = int(input(\"정답 번호: \").strip())\n\n        if quiz.check_answer(answer):\n            print(\"정답입니다.\")\n            self.update_best_score(1)\n            self.save_to_file()\n            print(f\"현재 최고 점수: {self.best_score}\")\n            return\n\n        print(\"오답입니다.\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_first_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈 1개를 실제로 출제하고 정답 판정하기\n\n```bash\n$ python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x103bb6120>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 1\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 2\n정답입니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 2\n프로그램을 종료합니다.\n```\n\n### 모든 퀴즈를 순서대로 출제하고 점수 합산하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_first_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 모든 퀴즈를 순서대로 출제하고 점수 합산하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 모든 퀴즈를 순서대로 출제하고 점수 합산하기\n\n```bash\n$ printf '1\n2\n3\n2\n2\n3\n2\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x1084f1fd0>, <__main__.Quiz object at 0x1084d0910>, <__main__.Quiz object at 0x1084d2850>, <__main__.Quiz object at 0x108554770>, <__main__.Quiz object at 0x1085555b0>]\n1\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 1}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 1}\n1. 퀴즈 풀기\n2. 종료\n선택: Python에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 정답입니다.\nPython에서 3 > 1의 결과는 무엇인가?\n0\nFalse\nTrue\nNone\n3\n정답 번호: 정답입니다.\n여러 값을 순서대로 저장하는 자료형은 무엇인가?\ndict\nlist\nstr\nint\n2\n정답 번호: 정답입니다.\n같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\nif\nfor\nprint\ninput\n2\n정답 번호: 정답입니다.\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\nbreak\nclass\nreturn\nimport\n3\n정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 5\n현재 최고 점수: 5\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 퀴즈가 없는 경우 안내 후 메뉴 복귀\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈가 없는 경우 안내 후 메뉴 복귀\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈가 없는 경우 안내 후 메뉴 복귀\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    if game.has_quizzes():\n        quiz1 = game.quizzes[0]\n        quiz1.show()\n        print(quiz1.check_answer(2))\n        print(quiz1.check_answer(1))\n    else:\n        print(\"확인할 기본 퀴즈가 없습니다.\")\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈가 없는 경우 안내 후 메뉴 복귀\n\n```bash\n$ printf '1\n2\n' | python3 main.py\n퀴즈 게임 시작\n[]\n0\n{'quizzes': [], 'best_score': 0}\n{'quizzes': [], 'best_score': 0}\n확인할 기본 퀴즈가 없습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 플레이 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n```\n\n### 플레이 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 플레이 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 플레이와 점수 계산 구현\"\n[codex/e1-2-history-rebuild 9a48940] Feat: 퀴즈 플레이와 점수 계산 구현\n 2 files changed, 47 insertions(+), 5 deletions(-)\n```\n\n### 플레이 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 9a48940 (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n```\n\n### 랜덤 출제와 문제 수 선택 적용\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 랜덤 출제와 문제 수 선택 적용\n\n```bash\n$ printf '1\\n2\\n2\\n2\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a96d7f0>, <__main__.Quiz object at 0x106011f90>, <__main__.Quiz object at 0x106013750>, <__main__.Quiz object at 0x10a928770>, <__main__.Quiz object at 0x10a92ab10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 0}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 2문제를 랜덤으로 출제합니다.\n여러 값을 순서대로 저장하는 자료형은 무엇인가?\ndict\nlist\nstr\nint\n2\n정답 번호: 정답입니다.\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 2\n현재 최고 점수: 2\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 랜덤 출제와 문제 수 선택 적용\n\n```bash\n$ printf '1\\n9\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x105f757f0>, <__main__.Quiz object at 0x1015e1f90>, <__main__.Quiz object at 0x1015e3750>, <__main__.Quiz object at 0x105f30770>, <__main__.Quiz object at 0x105f32b10>]\n2\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 풀 수 있는 문제 수만 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 랜덤 출제와 문제 수 선택 적용\n\n```bash\n$ printf '1\\nabc\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x1081b57f0>, <__main__.Quiz object at 0x10385df90>, <__main__.Quiz object at 0x10385f750>, <__main__.Quiz object at 0x108170770>, <__main__.Quiz object at 0x108172b10>]\n2\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 문제 수는 숫자로 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 힌트 사용과 점수 차감 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 힌트 사용과 점수 차감 확인\n\n```bash\n$ printf '1\\n1\\ny\\n3\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10406d7f0>, <__main__.Quiz object at 0x103f9df90>, <__main__.Quiz object at 0x103f9f750>, <__main__.Quiz object at 0x104030770>, <__main__.Quiz object at 0x104032b10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 1문제를 랜덤으로 출제합니다.\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\nbreak\nclass\nreturn\nimport\n3\n힌트를 볼까요? (y/n): 힌트: 함수 바깥으로 값을 다시 보내는 단어입니다.\n힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\n정답 번호: 정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\n이번 점수: 0\n현재 최고 점수: 0\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 보너스 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n```\n\n### 보너스 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 보너스 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 랜덤 출제와 힌트 기능 추가\"\n[codex/e1-2-history-rebuild 599708f] Feat: 랜덤 출제와 힌트 기능 추가\n 2 files changed, 129 insertions(+), 44 deletions(-)\n```\n\n### 보너스 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 599708f (HEAD -> codex/e1-2-history-rebuild) Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n```\n\n\n## Chapter 6. 추가/목록/삭제/점수 히스토리 검증\n\n### 테마\n\n- 퀴즈 추가 후 state.json 반영 확인\n- 점수 확인 메뉴로 최고 점수 출력\n- 삭제 후 파일 반영 확인\n- 추가/삭제 입력 범위 예외 처리 보강\n- 문제 관리 기능 완성 후 commit 기록\n- 날짜/시간 포함 게임 기록 저장 확인\n- 플레이 기록 기능 완성 후 commit 기록\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n```bash\n$ printf '2\\n파이썬 창시자는 누구인가?\\nGuido van Rossum\\nLinus Torvalds\\nJames Gosling\\nBjarne Stroustrup\\n1\\n3\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10819dfd0>, <__main__.Quiz object at 0x10817c910>, <__main__.Quiz object at 0x10817e850>, <__main__.Quiz object at 0x1082008a0>, <__main__.Quiz object at 0x1082016e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 종료\n선택: 문제를 입력하세요: 선택지 1: 선택지 2: 선택지 3: 선택지 4: 정답 번호(1-4): 퀴즈가 추가되었습니다.\n현재 퀴즈 수: 6\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n```bash\n$ python3 - <<'PY'\nimport json\nfrom pathlib import Path\npath = Path('state.json')\ndata = json.loads(path.read_text(encoding='utf-8'))\nprint(len(data['quizzes']))\nprint(data['quizzes'][-1]['question'])\nprint(data['quizzes'][-1]['answer'])\nPY\n6\n파이썬 창시자는 누구인가?\n1\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n```bash\n$ printf '4\\n5\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a76e120>, <__main__.Quiz object at 0x10a74c910>, <__main__.Quiz object at 0x10a74e850>, <__main__.Quiz object at 0x10a7d48a0>, <__main__.Quiz object at 0x10a7d56e0>, <__main__.Quiz object at 0x105d62f90>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 점수 확인\n5. 종료\n선택: 현재 최고 점수: 5\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 점수 확인\n5. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 삭제 후 파일 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 삭제 후 파일 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 삭제 후 파일 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 삭제 후 파일 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 삭제 후 파일 반영 확인\n\n```bash\n$ printf '4\\n6\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a861fd0>, <__main__.Quiz object at 0x10a840910>, <__main__.Quiz object at 0x10a842850>, <__main__.Quiz object at 0x10a8c88a0>, <__main__.Quiz object at 0x10a8c96e0>, <__main__.Quiz object at 0x10a732f90>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 등록된 퀴즈 목록 (총 6개)\n1. Python에서 문자열을 저장하는 자료형은?\n2. Python에서 3 > 1의 결과는 무엇인가?\n3. 여러 값을 순서대로 저장하는 자료형은 무엇인가?\n4. 같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\n5. 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n6. 파이썬 창시자는 누구인가?\n삭제할 퀴즈 번호: 삭제된 퀴즈: 파이썬 창시자는 누구인가?\n현재 퀴즈 수: 5\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 삭제 후 파일 반영 확인\n\n```bash\n$ python3 - <<'PY'\nimport json\nfrom pathlib import Path\npath = Path('state.json')\ndata = json.loads(path.read_text(encoding='utf-8'))\nprint(len(data['quizzes']))\nprint(data['quizzes'][-1]['question'])\nPY\n5\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n```\n\n### 추가/삭제 입력 범위 예외 처리 보강\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 추가/삭제 입력 범위 예외 처리 보강\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 추가/삭제 입력 범위 예외 처리 보강\n\n```bash\n$ printf '2\\n테스트 문제\\nA\\nB\\nC\\nD\\n9\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a985fd0>, <__main__.Quiz object at 0x10a964910>, <__main__.Quiz object at 0x10a966850>, <__main__.Quiz object at 0x10a9ec8a0>, <__main__.Quiz object at 0x10a9ed6e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 문제를 입력하세요: 선택지 1: 선택지 2: 선택지 3: 선택지 4: 정답 번호(1-4): 정답 번호는 1부터 4까지만 입력할 수 있습니다.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 추가/삭제 입력 범위 예외 처리 보강\n\n```bash\n$ printf '4\\n9\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a101fd0>, <__main__.Quiz object at 0x10a0e0910>, <__main__.Quiz object at 0x10a0e2850>, <__main__.Quiz object at 0x10a1688a0>, <__main__.Quiz object at 0x10a1696e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 등록된 퀴즈 목록 (총 5개)\n1. Python에서 문자열을 저장하는 자료형은?\n2. Python에서 3 > 1의 결과는 무엇인가?\n3. 여러 값을 순서대로 저장하는 자료형은 무엇인가?\n4. 같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\n5. 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n삭제할 퀴즈 번호: 삭제할 수 있는 퀴즈 번호만 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 문제 관리 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n```\n\n### 문제 관리 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 문제 관리 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\"\n[codex/e1-2-history-rebuild baeba2e] Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n 2 files changed, 138 insertions(+), 4 deletions(-)\n```\n\n### 문제 관리 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* baeba2e (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n```\n\n### 날짜/시간 포함 게임 기록 저장 확인\n\n`main.py`\n\n```python\nimport json\nimport random\nfrom datetime import datetime\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록, 최고 점수, 플레이 기록을 관리한다.\n    def __init__(self, quizzes, best_score, score_history):\n        self.quizzes = quizzes\n        self.best_score = best_score\n        self.score_history = score_history\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 플레이 기록\")\n        print(\"7. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\", \"7\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"7\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 이번 플레이 결과를 날짜/시간과 함께 기록 목록에 추가한다.\n    def record_score_history(self, total_questions, score):\n        self.score_history.append(\n            {\n                \"played_at\": datetime.now().isoformat(timespec=\"seconds\"),\n                \"total_questions\": total_questions,\n                \"score\": score,\n            }\n        )\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.record_score_history(count, score)\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 저장된 플레이 기록을 최근 순서대로 출력한다.\n    def show_score_history(self):\n        if len(self.score_history) == 0:\n            print(\"아직 저장된 플레이 기록이 없습니다.\")\n            return\n\n        print(\"플레이 기록\")\n        for number, history in enumerate(self.score_history, start=1):\n            print(\n                f\"{number}. {history['played_at']} | \"\n                f\"{history['total_questions']}문제 | \"\n                f\"{history['score']}점\"\n            )\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n            if choice == \"6\":\n                self.show_score_history()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n            \"score_history\": self.score_history,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"], data.get(\"score_history\", []))\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 날짜/시간 포함 게임 기록 저장 확인\n\n```bash\n$ printf '1\\n1\\nn\\n2\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x1086a17f0>, <__main__.Quiz object at 0x103c99f90>, <__main__.Quiz object at 0x103c9b750>, <__main__.Quiz object at 0x108668770>, <__main__.Quiz object at 0x10866ab10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0, 'score_history': []}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0, 'score_history': []}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 1문제를 랜덤으로 출제합니다.\n같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\nif\nfor\nprint\ninput\n2\n힌트를 볼까요? (y/n): 정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 1\n현재 최고 점수: 1\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 날짜/시간 포함 게임 기록 저장 확인\n\n```bash\n$ python3 -m json.tool state.json\n{\n    \"quizzes\": [\n        {\n            \"question\": \"Python에서 문자열을 저장하는 자료형은?\",\n            \"choices\": [\n                \"int\",\n                \"str\",\n                \"bool\",\n                \"list\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"문자열은 따옴표로 감싸 자주 표현합니다.\"\n        },\n        {\n            \"question\": \"Python에서 3 > 1의 결과는 무엇인가?\",\n            \"choices\": [\n                \"0\",\n                \"False\",\n                \"True\",\n                \"None\"\n            ],\n            \"answer\": 3,\n            \"hint\": \"비교 연산의 결과는 참 또는 거짓입니다.\"\n        },\n        {\n            \"question\": \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n            \"choices\": [\n                \"dict\",\n                \"list\",\n                \"str\",\n                \"int\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\"\n        },\n        {\n            \"question\": \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n            \"choices\": [\n                \"if\",\n                \"for\",\n                \"print\",\n                \"input\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"반복문 문법을 고르면 됩니다.\"\n        },\n        {\n            \"question\": \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n            \"choices\": [\n                \"break\",\n                \"class\",\n                \"return\",\n                \"import\"\n            ],\n            \"answer\": 3,\n            \"hint\": \"함수 바깥으로 값을 다시 보내는 단어입니다.\"\n        }\n    ],\n    \"best_score\": 1,\n    \"score_history\": [\n        {\n            \"played_at\": \"2026-08-06T19:55:55\",\n            \"total_questions\": 1,\n            \"score\": 1\n        }\n    ]\n}\n```\n\n### 날짜/시간 포함 게임 기록 저장 확인\n\n```bash\n$ printf '6\\n7\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x1083a17f0>, <__main__.Quiz object at 0x103971f90>, <__main__.Quiz object at 0x103973750>, <__main__.Quiz object at 0x1083688a0>, <__main__.Quiz object at 0x10836ac40>]\n1\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 1, 'score_history': [{'played_at': '2026-08-06T19:55:55', 'total_questions': 1, 'score': 1}]}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 1, 'score_history': [{'played_at': '2026-08-06T19:55:55', 'total_questions': 1, 'score': 1}]}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 플레이 기록\n7. 종료\n선택: 플레이 기록\n1. 2026-08-06T19:55:55 | 1문제 | 1점\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 플레이 기록\n7. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 플레이 기록 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n```\n\n### 플레이 기록 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 플레이 기록 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 플레이 기록 저장 기능 추가\"\n[codex/e1-2-history-rebuild 9d2cee1] Feat: 플레이 기록 저장 기능 추가\n 2 files changed, 47 insertions(+), 10 deletions(-)\n```\n\n### 플레이 기록 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 9d2cee1 (HEAD -> codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n```\n\n\n## Chapter 7. README, Git 로그, clone/pull 제출 증거\n\n### 테마\n\n- README 필수 항목 작성\n- git log --oneline --graph와 커밋 수 확인\n- GitHub 저장소 연결과 업로드 확인\n- 별도 디렉터리에 clone\n- clone 변경 push 후 원래 작업 폴더에서 pull\n- 개발 환경과 실행 화면 증거 정리\n\n### README 필수 항목 작성\n\n```bash\n$ sed -n '1,80p' README.md\n# E1-2 파이썬 이해하기\n\n## 프로젝트 개요\n\n터미널에서 실행하는 Python 퀴즈 게임입니다.  \n메뉴를 통해 퀴즈 풀기, 퀴즈 추가, 퀴즈 목록 확인, 퀴즈 삭제, 최고 점수 확인, 플레이 기록 확인을 수행할 수 있습니다.\n\n## 퀴즈 주제 선정 이유\n\nPython 기초 문법을 배우는 과제이기 때문에, 퀴즈 주제도 Python 기본 개념으로 맞췄습니다.  \n문자열, 불리언, 리스트, 반복문, 반환값처럼 과제에서 직접 설명해야 하는 개념을 퀴즈 문제로 다시 확인할 수 있게 구성했습니다.\n\n## 실행 방법\n\n과제 작업 루트에서 아래 명령으로 실행합니다.\n\n```bash\ncd /Users/hskim/Projects/codyssey/artifacts/e1-2/work\npython3 main.py\n```\n\n## 기능 목록\n\n- 메뉴 출력과 종료\n- 빈 입력, 문자 입력, 범위 밖 숫자 입력 예외 처리\n- `Quiz` 클래스 기반 개별 퀴즈 표현\n- `QuizGame` 클래스 기반 전체 게임 관리\n- 퀴즈 풀기\n- 퀴즈 추가\n- 퀴즈 목록 보기\n- 퀴즈 삭제\n- 최고 점수 확인\n- 플레이 기록 저장과 조회\n- `state.json` 저장/복구\n- 랜덤 출제\n- 문제 수 선택\n- 힌트 사용과 점수 차감\n\n## 파일 구조\n\n```text\nartifacts/e1-2/\n├── README.md\n├── state.json\n├── logs/\n│   └── practice.jsonl\n├── render/\n│   └── latest/\n│       ├── README.md\n│       └── presentation.html\n└── work/\n    ├── main.py\n    └── state.json\n```\n\n## 데이터 파일 설명\n\n이 과제의 실행 데이터는 기본적으로 `artifacts/e1-2/work/state.json`에 UTF-8로 저장됩니다.\n\n주요 필드는 다음과 같습니다.\n\n- `quizzes`: 문제, 선택지, 정답, 힌트 목록\n- `best_score`: 현재 최고 점수\n- `score_history`: 날짜/시간, 푼 문제 수, 점수 기록 배열\n```\n\n### README 필수 항목 작성\n\n```bash\n$ rg -n '^## ' README.md\n3:## 프로젝트 개요\n8:## 퀴즈 주제 선정 이유\n13:## 실행 방법\n22:## 기능 목록\n39:## 파일 구조\n56:## 데이터 파일 설명\n```\n\n### git log --oneline --graph와 커밋 수 확인\n\n```bash\n$ git rev-list --count --all\n12\n```\n\n### git log --oneline --graph와 커밋 수 확인\n\n```bash\n$ git log --oneline --graph --decorate --all --max-count=20\n* a12d1b7 (HEAD -> main, origin/main) Docs: clone 저장소 검증 메모 추가\n*   302870a Merge branch 'codex/e1-2-readme-evidence'\n|\\  \n| * 443cb6f (codex/e1-2-readme-evidence) Docs: README와 제출 증거 정리\n|/  \n* 15a283c Feat: 퀴즈 게임 기본 구조와 저장 흐름 구현\n* 9d2cee1 (codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현\n```\n\n### GitHub 저장소 연결과 업로드 확인\n\n```bash\n$ git remote -v\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (fetch)\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (push)\n```\n\n### GitHub 저장소 연결과 업로드 확인\n\n```bash\n$ gh auth status\ngithub.com\n  X Failed to log in to github.com account Logan-kim-the-philosopher (default)\n  - Active account: true\n  - The token in default is invalid.\n  - To re-authenticate, run: gh auth login -h github.com\n  - To forget about this account, run: gh auth logout -h github.com -u Logan-kim-the-philosopher\n```\n\n### 별도 디렉터리에 clone\n\n```bash\n$ git clone /private/tmp/e1-2-remote-cuFnUC.git /private/tmp/e1-2-clone-BOihMa\nCloning into '/private/tmp/e1-2-clone-BOihMa'...\ndone.\n```\n\n### 별도 디렉터리에 clone\n\n```bash\n$ git -C /private/tmp/e1-2-clone-BOihMa remote -v\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (fetch)\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (push)\n```\n\n### clone 변경 push 후 원래 작업 폴더에서 pull\n\n```bash\n$ git -C /private/tmp/e1-2-clone-BOihMa commit -m \"Docs: clone 저장소 검증 메모 추가\" && git -C /private/tmp/e1-2-clone-BOihMa push origin main\n[main a12d1b7] Docs: clone 저장소 검증 메모 추가\n 1 file changed, 2 insertions(+)\nTo /private/tmp/e1-2-remote-cuFnUC.git\n   302870a..a12d1b7  main -> main\n```\n\n### clone 변경 push 후 원래 작업 폴더에서 pull\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 pull origin main\nFrom /private/tmp/e1-2-remote-cuFnUC\n * branch            main       -> FETCH_HEAD\n   302870a..a12d1b7  main       -> origin/main\nUpdating 302870a..a12d1b7\nFast-forward\n README.md | 2 ++\n 1 file changed, 2 insertions(+)\n```\n\n### clone 변경 push 후 원래 작업 폴더에서 pull\n\n```bash\n$ rg -n \"clone 저장소\" README.md\n89:- clone 저장소에서 2026-08-06 pull 실습 메모를 추가했습니다.\n```\n\n### 개발 환경과 실행 화면 증거 정리\n\n```bash\n$ python3 --version\nPython 3.14.6\n```\n\n### 개발 환경과 실행 화면 증거 정리\n\n```bash\n$ git --version\ngit version 2.55.0\n```\n\n### 개발 환경과 실행 화면 증거 정리\n\n```bash\n$ cd /Users/hskim/Projects/codyssey/artifacts/e1-2/work && pwd && ls -la\n/Users/hskim/Projects/codyssey/artifacts/e1-2/work\ntotal 64\ndrwxr-xr-x@ 8 hskim  staff    256 Aug  6 20:05 .\ndrwxr-xr-x@ 9 hskim  staff    288 Aug  6 22:17 ..\ndrwxr-xr-x@ 3 hskim  staff     96 Aug  6 20:05 __pycache__\n-rw-r--r--@ 1 hskim  staff  14378 Aug  6 20:00 main.py\n-rw-r--r--@ 1 hskim  staff    234 Aug  6 15:16 state.before-5-quizzes.json\n-rw-r--r--@ 1 hskim  staff   1550 Aug  6 20:01 state.json\n-rw-r--r--@ 1 hskim  staff   1066 Aug  6 18:54 state.json.bak\n-rw-r--r--@ 1 hskim  staff   1356 Aug  6 19:39 state.pre_hint_backup.json\n```\n\n### 증빙\n\n- artifacts/e1-2/README.md\n### 증빙\n\n- artifacts/e1-2/README.md",
          "cwd": "artifacts/e1-2"
        },
        {
          "type": "terminal-step",
          "theme": "README 필수 항목 작성",
          "command": "ls -la README.md",
          "output": "-rw-r--r--@ 1 hskim  staff  397412 Aug  6 22:21 /Users/hskim/Projects/codyssey/artifacts/e1-2/README.md",
          "cwd": "artifacts/e1-2"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "README 필수 항목 작성",
          "command": "cat README.md",
          "output": "# E1-2 파이썬 이해하기\n\n- 발표 링크: 발표용 HTML 링크 준비 중\n\n## 챕터\n\n- Chapter 1. 메뉴와 공통 입력 예외 처리\n- Chapter 2. Quiz 클래스와 객체 기초\n- Chapter 3. QuizGame 클래스와 기능별 메서드 분리\n- Chapter 4. 파일 입출력과 데이터 영속성\n- Chapter 5. 퀴즈 플레이 기능과 브랜치 병합\n- Chapter 6. 추가/목록/삭제/점수 히스토리 검증\n- Chapter 7. README, Git 로그, clone/pull 제출 증거\n\n## 실습 로그\n\n## Chapter 1. 메뉴와 공통 입력 예외 처리\n\n### 테마\n\n- while 반복 메뉴와 종료 흐름 구현\n- 빈 입력/공백/문자/범위 밖 숫자 처리\n- 메뉴 기능 완성 후 commit 기록\n\n### while 반복 메뉴와 종료 흐름 구현\n\n`main.py`\n\n```python\ndef main():\n    print(\"퀴즈 게임 시작\")\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        choice = input(\"선택: \")\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### while 반복 메뉴와 종료 흐름 구현\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n`main.py`\n\n```python\ndef main():\n    print(\"퀴즈 게임 시작\")\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n```bash\n$ printf '\\n2\\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 입력이 비어 있습니다. 다시 선택하세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n```bash\n$ printf 'abc\\n2\\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 숫자로 입력해주세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n```bash\n$ printf '9\\n2\\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 1 또는 2만 입력할 수 있습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 빈 입력/공백/문자/범위 밖 숫자 처리\n\n```bash\n$ printf ' 2 \\n' | python3 main.py\n퀴즈 게임 시작\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 메뉴 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\nA  .gitignore\nA  work/main.py\n```\n\n### 메뉴 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 메뉴 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 반복 메뉴와 종료 흐름 구현\"\n[codex/e1-2-history-rebuild (root-commit) 27884fe] Feat: 반복 메뉴와 종료 흐름 구현\n 2 files changed, 31 insertions(+)\n create mode 100644 .gitignore\n create mode 100644 work/main.py\n```\n\n### 메뉴 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph\n* 27884fe Feat: 반복 메뉴와 종료 흐름 구현\n```\n\n\n## Chapter 2. Quiz 클래스와 객체 기초\n\n### 테마\n\n- Quiz 클래스 뼈대 정의\n- Quiz 객체 생성과 속성 확인\n- Quiz 메서드로 출력 책임 이동\n- 정답 비교 메서드와 불리언 결과 확인\n- Quiz 기능 완성 후 commit 기록\n\n### Quiz 클래스 뼈대 정의\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n\ndef main():\n    print(\"퀴즈 게임 시작\")\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### Quiz 객체 생성과 속성 확인\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 직접 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(quiz1.question)\n    print(quiz1.choices)\n    print(quiz1.answer)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### Quiz 객체 생성과 속성 확인\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\n['int', 'str', 'bool', 'list']\n2\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### Quiz 메서드로 출력 책임 이동\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 직접 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(quiz1.question)\n    print(quiz1.choices)\n    print(quiz1.answer)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### Quiz 메서드로 출력 책임 이동\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 메서드로 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### Quiz 메서드로 출력 책임 이동\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 정답 비교 메서드와 불리언 결과 확인\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값을 메서드로 출력해 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 정답 비교 메서드와 불리언 결과 확인\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 정답 비교 메서드와 불리언 결과 확인\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### Quiz 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n```\n\n### Quiz 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### Quiz 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: Quiz 클래스와 정답 판정 구현\"\n[codex/e1-2-history-rebuild 7e2694f] Feat: Quiz 클래스와 정답 판정 구현\n 1 file changed, 30 insertions(+)\n```\n\n### Quiz 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 7e2694f (HEAD -> codex/e1-2-history-rebuild) Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현\n```\n\n\n## Chapter 3. QuizGame 클래스와 기능별 메서드 분리\n\n### 테마\n\n- 퀴즈 목록과 최고 점수 속성 묶기\n- QuizGame 메서드로 메뉴 출력 책임 옮기기\n- QuizGame 메서드로 메뉴 입력 검사 책임 옮기기\n- QuizGame 메서드로 종료 판단 책임 옮기기\n- 한 함수에 몰리지 않는 구조 점검\n- QuizGame 구조 완성 후 commit 기록\n\n### 퀴즈 목록과 최고 점수 속성 묶기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n\ndef main():\n    # 실습용 예시 퀴즈를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 퀴즈 목록과 최고 점수 속성 묶기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 퀴즈 목록과 최고 점수 속성 묶기\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x1033bc590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### QuizGame 메서드로 메뉴 출력 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 메뉴 출력 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 메뉴 출력 책임 옮기기\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x109e88590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### QuizGame 메서드로 메뉴 입력 검사 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if choice == \"\":\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 메뉴 입력 검사 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 메뉴 입력 검사 책임 옮기기\n\n```bash\n$ printf '\\n2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x105b44590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 입력이 비어 있습니다. 다시 선택하세요.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### QuizGame 메서드로 종료 판단 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not choice.isdigit():\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if choice not in [\"1\", \"2\"]:\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if choice == \"2\":\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 종료 판단 책임 옮기기\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not game.is_digit_choice(choice):\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if not game.is_valid_menu_choice(choice):\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if game.is_exit_choice(choice):\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### QuizGame 메서드로 종료 판단 책임 옮기기\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x10386c590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 한 함수에 몰리지 않는 구조 점검\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴를 반복해서 보여주고 종료 입력을 기다린다.\n    while True:\n        game.show_menu()\n\n        # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n        choice = input(\"선택: \").strip()\n\n        if game.is_blank_choice(choice):\n            print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n            continue\n\n        if not game.is_digit_choice(choice):\n            print(\"숫자로 입력해주세요.\")\n            continue\n\n        if not game.is_valid_menu_choice(choice):\n            print(\"1 또는 2만 입력할 수 있습니다.\")\n            continue\n\n        if game.is_exit_choice(choice):\n            print(\"프로그램을 종료합니다.\")\n            break\n\n\nmain()\n```\n\n### 한 함수에 몰리지 않는 구조 점검\n\n`main.py`\n\n```python\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 한 함수에 몰리지 않는 구조 점검\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x104308590>]\n0\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### QuizGame 구조 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n```\n\n### QuizGame 구조 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### QuizGame 구조 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Refactor: QuizGame으로 메뉴 책임 분리\"\n[codex/e1-2-history-rebuild 054e3c8] Refactor: QuizGame으로 메뉴 책임 분리\n 1 file changed, 58 insertions(+), 23 deletions(-)\n```\n\n### QuizGame 구조 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 054e3c8 (HEAD -> codex/e1-2-history-rebuild) Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현\n```\n\n\n## Chapter 4. 파일 입출력과 데이터 영속성\n\n### 테마\n\n- state.json에서 게임 상태 불러오기\n- 손상된 state.json에서도 기본값으로 복구하기\n- 저장 복구 기능 완성 후 commit 기록\n\n### state.json에서 게임 상태 불러오기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### state.json에서 게임 상태 불러오기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### state.json에서 게임 상태 불러오기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### state.json에서 게임 상태 불러오기\n\n```bash\n$ printf '2\\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x104145fd0>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 손상된 state.json에서도 기본값으로 복구하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n            data = json.load(file)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 손상된 state.json에서도 기본값으로 복구하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 실습용 예시 퀴즈와 게임 상태를 만든다.\n    quiz1 = Quiz(\n        \"Python에서 문자열을 저장하는 자료형은?\",\n        [\"int\", \"str\", \"bool\", \"list\"],\n        2,\n    )\n    game = QuizGame([quiz1], 0)\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 손상된 state.json에서도 기본값으로 복구하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 손상된 state.json에서도 기본값으로 복구하기\n\n```bash\n$ tmpdir=$(mktemp -d /private/tmp/e1-2-corrupt-XXXXXX) && cp /Users/hskim/Projects/codyssey/artifacts/e1-2/work/main.py \"$tmpdir/main.py\" && printf '{ broken json\\n' > \"$tmpdir/state.json\" && cd \"$tmpdir\" && printf '2\\n' | python3 main.py\n저장 파일이 손상되어 기본 퀴즈로 복구합니다.\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x10178a120>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 저장 복구 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n?? work/state.json\n```\n\n### 저장 복구 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 저장 복구 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: state.json 저장과 복구 처리 추가\"\n[codex/e1-2-history-rebuild a5d5759] Feat: state.json 저장과 복구 처리 추가\n 2 files changed, 99 insertions(+), 7 deletions(-)\n create mode 100644 work/state.json\n```\n\n### 저장 복구 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* a5d5759 (HEAD -> codex/e1-2-history-rebuild) Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n```\n\n\n## Chapter 5. 퀴즈 플레이 기능과 브랜치 병합\n\n### 테마\n\n- 퀴즈 1개를 실제로 출제하고 정답 판정하기\n- 모든 퀴즈를 순서대로 출제하고 점수 합산하기\n- 퀴즈가 없는 경우 안내 후 메뉴 복귀\n- 플레이 기능 완성 후 commit 기록\n- 랜덤 출제와 문제 수 선택 적용\n- 힌트 사용과 점수 차감 확인\n- 보너스 기능 완성 후 commit 기록\n\n### 퀴즈 1개를 실제로 출제하고 정답 판정하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 첫 번째 퀴즈를 출제하고 정답 여부를 확인한다.\n    def play_first_quiz(self):\n        quiz = self.quizzes[0]\n        quiz.show()\n        answer = int(input(\"정답 번호: \").strip())\n\n        if quiz.check_answer(answer):\n            print(\"정답입니다.\")\n            self.update_best_score(1)\n            self.save_to_file()\n            print(f\"현재 최고 점수: {self.best_score}\")\n            return\n\n        print(\"오답입니다.\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈 1개를 실제로 출제하고 정답 판정하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 첫 번째 퀴즈를 출제하고 정답 여부를 확인한다.\n    def play_first_quiz(self):\n        quiz = self.quizzes[0]\n        quiz.show()\n        answer = int(input(\"정답 번호: \").strip())\n\n        if quiz.check_answer(answer):\n            print(\"정답입니다.\")\n            self.update_best_score(1)\n            self.save_to_file()\n            print(f\"현재 최고 점수: {self.best_score}\")\n            return\n\n        print(\"오답입니다.\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_first_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈 1개를 실제로 출제하고 정답 판정하기\n\n```bash\n$ python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x103bb6120>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}], 'best_score': 0}\n1. 퀴즈 풀기\n2. 종료\n선택: 1\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 2\n정답입니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 2\n프로그램을 종료합니다.\n```\n\n### 모든 퀴즈를 순서대로 출제하고 점수 합산하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_first_quiz()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 모든 퀴즈를 순서대로 출제하고 점수 합산하기\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 모든 퀴즈를 순서대로 출제하고 점수 합산하기\n\n```bash\n$ printf '1\n2\n3\n2\n2\n3\n2\n' | python3 main.py\n퀴즈 게임 시작\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n[<__main__.Quiz object at 0x1084f1fd0>, <__main__.Quiz object at 0x1084d0910>, <__main__.Quiz object at 0x1084d2850>, <__main__.Quiz object at 0x108554770>, <__main__.Quiz object at 0x1085555b0>]\n1\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 1}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 1}\n1. 퀴즈 풀기\n2. 종료\n선택: Python에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 정답입니다.\nPython에서 3 > 1의 결과는 무엇인가?\n0\nFalse\nTrue\nNone\n3\n정답 번호: 정답입니다.\n여러 값을 순서대로 저장하는 자료형은 무엇인가?\ndict\nlist\nstr\nint\n2\n정답 번호: 정답입니다.\n같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\nif\nfor\nprint\ninput\n2\n정답 번호: 정답입니다.\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\nbreak\nclass\nreturn\nimport\n3\n정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 5\n현재 최고 점수: 5\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 퀴즈가 없는 경우 안내 후 메뉴 복귀\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈가 없는 경우 안내 후 메뉴 복귀\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n    quiz1 = game.quizzes[0]\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    quiz1.show()\n    print(quiz1.check_answer(2))\n    print(quiz1.check_answer(1))\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈가 없는 경우 안내 후 메뉴 복귀\n\n`main.py`\n\n```python\nimport json\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"2\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        score = 0\n\n        for quiz in self.quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n            json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(item[\"question\"], item[\"choices\"], item[\"answer\"])\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n    game = QuizGame.load_from_file()\n\n    # 객체에 저장된 값과 메서드 동작을 확인한다.\n    print(\"퀴즈 게임 시작\")\n    print(game.quizzes)\n    print(game.best_score)\n    print(game.to_dict())\n    game.save_to_file()\n    loaded_game = QuizGame.load_from_file()\n    print(loaded_game.to_dict())\n\n    if game.has_quizzes():\n        quiz1 = game.quizzes[0]\n        quiz1.show()\n        print(quiz1.check_answer(2))\n        print(quiz1.check_answer(1))\n    else:\n        print(\"확인할 기본 퀴즈가 없습니다.\")\n\n    # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n    game.run_menu()\n\n\nmain()\n```\n\n### 퀴즈가 없는 경우 안내 후 메뉴 복귀\n\n```bash\n$ printf '1\n2\n' | python3 main.py\n퀴즈 게임 시작\n[]\n0\n{'quizzes': [], 'best_score': 0}\n{'quizzes': [], 'best_score': 0}\n확인할 기본 퀴즈가 없습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\n1. 퀴즈 풀기\n2. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 플레이 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n```\n\n### 플레이 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 플레이 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 플레이와 점수 계산 구현\"\n[codex/e1-2-history-rebuild 9a48940] Feat: 퀴즈 플레이와 점수 계산 구현\n 2 files changed, 47 insertions(+), 5 deletions(-)\n```\n\n### 플레이 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 9a48940 (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n```\n\n### 랜덤 출제와 문제 수 선택 적용\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답을 저장한다.\n    def __init__(self, question, choices, answer):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                print(\"정답입니다.\")\n                score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 랜덤 출제와 문제 수 선택 적용\n\n```bash\n$ printf '1\\n2\\n2\\n2\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a96d7f0>, <__main__.Quiz object at 0x106011f90>, <__main__.Quiz object at 0x106013750>, <__main__.Quiz object at 0x10a928770>, <__main__.Quiz object at 0x10a92ab10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 0}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 2문제를 랜덤으로 출제합니다.\n여러 값을 순서대로 저장하는 자료형은 무엇인가?\ndict\nlist\nstr\nint\n2\n정답 번호: 정답입니다.\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\n정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 2\n현재 최고 점수: 2\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 랜덤 출제와 문제 수 선택 적용\n\n```bash\n$ printf '1\\n9\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x105f757f0>, <__main__.Quiz object at 0x1015e1f90>, <__main__.Quiz object at 0x1015e3750>, <__main__.Quiz object at 0x105f30770>, <__main__.Quiz object at 0x105f32b10>]\n2\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 풀 수 있는 문제 수만 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 랜덤 출제와 문제 수 선택 적용\n\n```bash\n$ printf '1\\nabc\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x1081b57f0>, <__main__.Quiz object at 0x10385df90>, <__main__.Quiz object at 0x10385f750>, <__main__.Quiz object at 0x108170770>, <__main__.Quiz object at 0x108172b10>]\n2\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 2}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 문제 수는 숫자로 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 힌트 사용과 점수 차감 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 힌트 사용과 점수 차감 확인\n\n```bash\n$ printf '1\\n1\\ny\\n3\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10406d7f0>, <__main__.Quiz object at 0x103f9df90>, <__main__.Quiz object at 0x103f9f750>, <__main__.Quiz object at 0x104030770>, <__main__.Quiz object at 0x104032b10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 1문제를 랜덤으로 출제합니다.\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\nbreak\nclass\nreturn\nimport\n3\n힌트를 볼까요? (y/n): 힌트: 함수 바깥으로 값을 다시 보내는 단어입니다.\n힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\n정답 번호: 정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\n이번 점수: 0\n현재 최고 점수: 0\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 보너스 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n```\n\n### 보너스 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 보너스 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 랜덤 출제와 힌트 기능 추가\"\n[codex/e1-2-history-rebuild 599708f] Feat: 랜덤 출제와 힌트 기능 추가\n 2 files changed, 129 insertions(+), 44 deletions(-)\n```\n\n### 보너스 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 599708f (HEAD -> codex/e1-2-history-rebuild) Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n```\n\n\n## Chapter 6. 추가/목록/삭제/점수 히스토리 검증\n\n### 테마\n\n- 퀴즈 추가 후 state.json 반영 확인\n- 점수 확인 메뉴로 최고 점수 출력\n- 삭제 후 파일 반영 확인\n- 추가/삭제 입력 범위 예외 처리 보강\n- 문제 관리 기능 완성 후 commit 기록\n- 날짜/시간 포함 게임 기록 저장 확인\n- 플레이 기록 기능 완성 후 commit 기록\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n```bash\n$ printf '2\\n파이썬 창시자는 누구인가?\\nGuido van Rossum\\nLinus Torvalds\\nJames Gosling\\nBjarne Stroustrup\\n1\\n3\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10819dfd0>, <__main__.Quiz object at 0x10817c910>, <__main__.Quiz object at 0x10817e850>, <__main__.Quiz object at 0x1082008a0>, <__main__.Quiz object at 0x1082016e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 종료\n선택: 문제를 입력하세요: 선택지 1: 선택지 2: 선택지 3: 선택지 4: 정답 번호(1-4): 퀴즈가 추가되었습니다.\n현재 퀴즈 수: 6\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 퀴즈 추가 후 state.json 반영 확인\n\n```bash\n$ python3 - <<'PY'\nimport json\nfrom pathlib import Path\npath = Path('state.json')\ndata = json.loads(path.read_text(encoding='utf-8'))\nprint(len(data['quizzes']))\nprint(data['quizzes'][-1]['question'])\nprint(data['quizzes'][-1]['answer'])\nPY\n6\n파이썬 창시자는 누구인가?\n1\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 점수 확인 메뉴로 최고 점수 출력\n\n```bash\n$ printf '4\\n5\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a76e120>, <__main__.Quiz object at 0x10a74c910>, <__main__.Quiz object at 0x10a74e850>, <__main__.Quiz object at 0x10a7d48a0>, <__main__.Quiz object at 0x10a7d56e0>, <__main__.Quiz object at 0x105d62f90>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 점수 확인\n5. 종료\n선택: 현재 최고 점수: 5\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 점수 확인\n5. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 삭제 후 파일 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 삭제 후 파일 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 삭제 후 파일 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 삭제 후 파일 반영 확인\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 삭제 후 파일 반영 확인\n\n```bash\n$ printf '4\\n6\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a861fd0>, <__main__.Quiz object at 0x10a840910>, <__main__.Quiz object at 0x10a842850>, <__main__.Quiz object at 0x10a8c88a0>, <__main__.Quiz object at 0x10a8c96e0>, <__main__.Quiz object at 0x10a732f90>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}, {'question': '파이썬 창시자는 누구인가?', 'choices': ['Guido van Rossum', 'Linus Torvalds', 'James Gosling', 'Bjarne Stroustrup'], 'answer': 1}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 등록된 퀴즈 목록 (총 6개)\n1. Python에서 문자열을 저장하는 자료형은?\n2. Python에서 3 > 1의 결과는 무엇인가?\n3. 여러 값을 순서대로 저장하는 자료형은 무엇인가?\n4. 같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\n5. 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n6. 파이썬 창시자는 누구인가?\n삭제할 퀴즈 번호: 삭제된 퀴즈: 파이썬 창시자는 누구인가?\n현재 퀴즈 수: 5\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 삭제 후 파일 반영 확인\n\n```bash\n$ python3 - <<'PY'\nimport json\nfrom pathlib import Path\npath = Path('state.json')\ndata = json.loads(path.read_text(encoding='utf-8'))\nprint(len(data['quizzes']))\nprint(data['quizzes'][-1]['question'])\nPY\n5\n함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n```\n\n### 추가/삭제 입력 범위 예외 처리 보강\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number = int(input(\"삭제할 퀴즈 번호: \").strip())\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 추가/삭제 입력 범위 예외 처리 보강\n\n`main.py`\n\n```python\nimport json\nimport random\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록과 최고 점수를 관리한다.\n    def __init__(self, quizzes, best_score):\n        self.quizzes = quizzes\n        self.best_score = best_score\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"6\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0)\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"])\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 추가/삭제 입력 범위 예외 처리 보강\n\n```bash\n$ printf '2\\n테스트 문제\\nA\\nB\\nC\\nD\\n9\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a985fd0>, <__main__.Quiz object at 0x10a964910>, <__main__.Quiz object at 0x10a966850>, <__main__.Quiz object at 0x10a9ec8a0>, <__main__.Quiz object at 0x10a9ed6e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 문제를 입력하세요: 선택지 1: 선택지 2: 선택지 3: 선택지 4: 정답 번호(1-4): 정답 번호는 1부터 4까지만 입력할 수 있습니다.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 추가/삭제 입력 범위 예외 처리 보강\n\n```bash\n$ printf '4\\n9\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x10a101fd0>, <__main__.Quiz object at 0x10a0e0910>, <__main__.Quiz object at 0x10a0e2850>, <__main__.Quiz object at 0x10a1688a0>, <__main__.Quiz object at 0x10a1696e0>]\n5\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3}], 'best_score': 5}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 등록된 퀴즈 목록 (총 5개)\n1. Python에서 문자열을 저장하는 자료형은?\n2. Python에서 3 > 1의 결과는 무엇인가?\n3. 여러 값을 순서대로 저장하는 자료형은 무엇인가?\n4. 같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\n5. 함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\n삭제할 퀴즈 번호: 삭제할 수 있는 퀴즈 번호만 입력해주세요.\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 문제 관리 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n```\n\n### 문제 관리 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 문제 관리 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\"\n[codex/e1-2-history-rebuild baeba2e] Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n 2 files changed, 138 insertions(+), 4 deletions(-)\n```\n\n### 문제 관리 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* baeba2e (HEAD -> codex/e1-2-history-rebuild) Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n```\n\n### 날짜/시간 포함 게임 기록 저장 확인\n\n`main.py`\n\n```python\nimport json\nimport random\nfrom datetime import datetime\n\n\nclass Quiz:\n    # 퀴즈 1개의 문제, 선택지, 정답, 힌트를 저장한다.\n    def __init__(self, question, choices, answer, hint):\n        self.question = question\n        self.choices = choices\n        self.answer = answer\n        self.hint = hint\n\n    # 저장된 퀴즈 내용을 한 번에 출력한다.\n    def show(self):\n        print(self.question)\n        for choice in self.choices:\n            print(choice)\n        print(self.answer)\n\n    # 사용자 답이 정답 번호와 같은지 확인한다.\n    def check_answer(self, user_answer):\n        return self.answer == user_answer\n\n    # 현재 퀴즈의 힌트를 출력한다.\n    def show_hint(self):\n        print(f\"힌트: {self.hint}\")\n\n\nclass QuizGame:\n    # 게임 전체에서 퀴즈 목록, 최고 점수, 플레이 기록을 관리한다.\n    def __init__(self, quizzes, best_score, score_history):\n        self.quizzes = quizzes\n        self.best_score = best_score\n        self.score_history = score_history\n\n    # 파일이 없을 때 사용할 기본 퀴즈 목록을 만든다.\n    @staticmethod\n    def make_default_quizzes():\n        return [\n            # 문자열 자료형을 묻는 기본 퀴즈다.\n            Quiz(\n                \"Python에서 문자열을 저장하는 자료형은?\",\n                [\"int\", \"str\", \"bool\", \"list\"],\n                2,\n                \"문자열은 따옴표로 감싸 자주 표현합니다.\",\n            ),\n            # 불리언 결과를 만드는 비교 연산 퀴즈다.\n            Quiz(\n                \"Python에서 3 > 1의 결과는 무엇인가?\",\n                [\"0\", \"False\", \"True\", \"None\"],\n                3,\n                \"비교 연산의 결과는 참 또는 거짓입니다.\",\n            ),\n            # 리스트 자료형을 고르는 퀴즈다.\n            Quiz(\n                \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n                [\"dict\", \"list\", \"str\", \"int\"],\n                2,\n                \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\",\n            ),\n            # 반복문 역할을 묻는 퀴즈다.\n            Quiz(\n                \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n                [\"if\", \"for\", \"print\", \"input\"],\n                2,\n                \"반복문 문법을 고르면 됩니다.\",\n            ),\n            # 함수 반환값 개념을 묻는 퀴즈다.\n            Quiz(\n                \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n                [\"break\", \"class\", \"return\", \"import\"],\n                3,\n                \"함수 바깥으로 값을 다시 보내는 단어입니다.\",\n            ),\n        ]\n\n    # 메뉴 문구를 한 곳에서 출력한다.\n    def show_menu(self):\n        print(\"1. 퀴즈 풀기\")\n        print(\"2. 퀴즈 추가\")\n        print(\"3. 퀴즈 목록\")\n        print(\"4. 퀴즈 삭제\")\n        print(\"5. 점수 확인\")\n        print(\"6. 플레이 기록\")\n        print(\"7. 종료\")\n\n    # 메뉴 입력이 비어 있는지 먼저 검사한다.\n    def is_blank_choice(self, choice):\n        return choice == \"\"\n\n    # 메뉴 입력이 숫자인지 검사한다.\n    def is_digit_choice(self, choice):\n        return choice.isdigit()\n\n    # 메뉴 입력이 허용된 번호인지 검사한다.\n    def is_valid_menu_choice(self, choice):\n        return choice in [\"1\", \"2\", \"3\", \"4\", \"5\", \"6\", \"7\"]\n\n    # 종료 메뉴를 선택했는지 검사한다.\n    def is_exit_choice(self, choice):\n        return choice == \"7\"\n\n    # 출제할 퀴즈가 1개 이상 있는지 확인한다.\n    def has_quizzes(self):\n        return len(self.quizzes) > 0\n\n    # 이번에 풀 문제 수를 입력받고 가능한 범위인지 확인한다.\n    def ask_quiz_count(self):\n        count_text = input(f\"몇 문제를 풀까요? (1-{len(self.quizzes)}): \").strip()\n\n        if count_text == \"\":\n            print(\"문제 수를 입력해주세요.\")\n            return None\n\n        if not count_text.isdigit():\n            print(\"문제 수는 숫자로 입력해주세요.\")\n            return None\n\n        count = int(count_text)\n\n        if count < 1 or count > len(self.quizzes):\n            print(\"풀 수 있는 문제 수만 입력해주세요.\")\n            return None\n\n        return count\n\n    # 저장된 퀴즈 중에서 요청한 개수만큼 랜덤하게 뽑는다.\n    def pick_random_quizzes(self, count):\n        return random.sample(self.quizzes, count)\n\n    # 힌트를 볼지 입력받고, 봤다면 점수 차감 여부를 알려준다.\n    def ask_hint_usage(self, quiz):\n        choice = input(\"힌트를 볼까요? (y/n): \").strip().lower()\n\n        if choice == \"y\":\n            quiz.show_hint()\n            print(\"힌트를 사용해 이 문제는 점수를 얻을 수 없습니다.\")\n            return True\n\n        return False\n\n    # 이번 플레이 점수가 더 높으면 최고 점수를 갱신한다.\n    def update_best_score(self, score):\n        if score > self.best_score:\n            self.best_score = score\n            print(\"최고 점수가 갱신되었습니다.\")\n\n    # 이번 플레이 결과를 날짜/시간과 함께 기록 목록에 추가한다.\n    def record_score_history(self, total_questions, score):\n        self.score_history.append(\n            {\n                \"played_at\": datetime.now().isoformat(timespec=\"seconds\"),\n                \"total_questions\": total_questions,\n                \"score\": score,\n            }\n        )\n\n    # 저장된 모든 퀴즈를 순서대로 출제하고 맞은 개수를 센다.\n    def play_all_quizzes(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없어 퀴즈를 시작할 수 없습니다.\")\n            return\n\n        count = self.ask_quiz_count()\n        if count is None:\n            return\n\n        # 사용자가 고른 개수만큼 문제를 섞어서 이번 라운드를 만든다.\n        selected_quizzes = self.pick_random_quizzes(count)\n        score = 0\n\n        print(f\"{count}문제를 랜덤으로 출제합니다.\")\n\n        for quiz in selected_quizzes:\n            quiz.show()\n            used_hint = self.ask_hint_usage(quiz)\n            answer = int(input(\"정답 번호: \").strip())\n\n            if quiz.check_answer(answer):\n                if used_hint:\n                    print(\"정답이지만 힌트를 사용해 점수는 올라가지 않습니다.\")\n                else:\n                    print(\"정답입니다.\")\n                    score += 1\n                continue\n\n            print(\"오답입니다.\")\n\n        self.record_score_history(count, score)\n        self.update_best_score(score)\n        self.save_to_file()\n        print(f\"이번 점수: {score}\")\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 새 퀴즈 입력을 받아 목록에 추가하고 파일에 저장한다.\n    def add_quiz(self):\n        question = input(\"문제를 입력하세요: \").strip()\n        choices = []\n\n        for number in range(1, 5):\n            # 선택지 4개를 순서대로 입력받는다.\n            choice = input(f\"선택지 {number}: \").strip()\n            choices.append(choice)\n\n        answer_text = input(\"정답 번호(1-4): \").strip()\n\n        if answer_text == \"\":\n            print(\"정답 번호를 입력해주세요.\")\n            return\n\n        if not answer_text.isdigit():\n            print(\"정답 번호는 숫자로 입력해주세요.\")\n            return\n\n        answer = int(answer_text)\n\n        if answer < 1 or answer > 4:\n            print(\"정답 번호는 1부터 4까지만 입력할 수 있습니다.\")\n            return\n\n        self.quizzes.append(Quiz(question, choices, answer))\n        self.save_to_file()\n        print(\"퀴즈가 추가되었습니다.\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 저장된 퀴즈 목록을 번호와 함께 출력한다.\n    def show_quiz_list(self):\n        if not self.has_quizzes():\n            print(\"등록된 퀴즈가 없습니다.\")\n            return\n\n        print(f\"등록된 퀴즈 목록 (총 {len(self.quizzes)}개)\")\n        for number, quiz in enumerate(self.quizzes, start=1):\n            # 번호와 문제 제목만 먼저 보여준다.\n            print(f\"{number}. {quiz.question}\")\n\n    # 현재 최고 점수를 출력한다.\n    def show_best_score(self):\n        print(f\"현재 최고 점수: {self.best_score}\")\n\n    # 저장된 플레이 기록을 최근 순서대로 출력한다.\n    def show_score_history(self):\n        if len(self.score_history) == 0:\n            print(\"아직 저장된 플레이 기록이 없습니다.\")\n            return\n\n        print(\"플레이 기록\")\n        for number, history in enumerate(self.score_history, start=1):\n            print(\n                f\"{number}. {history['played_at']} | \"\n                f\"{history['total_questions']}문제 | \"\n                f\"{history['score']}점\"\n            )\n\n    # 번호로 퀴즈 1개를 삭제하고 파일에 저장한다.\n    def delete_quiz(self):\n        if not self.has_quizzes():\n            print(\"삭제할 퀴즈가 없습니다.\")\n            return\n\n        self.show_quiz_list()\n        number_text = input(\"삭제할 퀴즈 번호: \").strip()\n\n        if number_text == \"\":\n            print(\"삭제 번호를 입력해주세요.\")\n            return\n\n        if not number_text.isdigit():\n            print(\"삭제 번호는 숫자로 입력해주세요.\")\n            return\n\n        number = int(number_text)\n\n        if number < 1 or number > len(self.quizzes):\n            print(\"삭제할 수 있는 퀴즈 번호만 입력해주세요.\")\n            return\n\n        deleted_quiz = self.quizzes.pop(number - 1)\n        self.save_to_file()\n        print(f\"삭제된 퀴즈: {deleted_quiz.question}\")\n        print(f\"현재 퀴즈 수: {len(self.quizzes)}\")\n\n    # 메뉴 반복 흐름을 게임 객체 안에서 실행한다.\n    def run_menu(self):\n        while True:\n            self.show_menu()\n\n            # 공백을 제거한 뒤 메뉴 입력을 검사한다.\n            choice = input(\"선택: \").strip()\n\n            if self.is_blank_choice(choice):\n                print(\"입력이 비어 있습니다. 다시 선택하세요.\")\n                continue\n\n            if not self.is_digit_choice(choice):\n                print(\"숫자로 입력해주세요.\")\n                continue\n\n            if not self.is_valid_menu_choice(choice):\n                print(\"1 또는 2만 입력할 수 있습니다.\")\n                continue\n\n            if choice == \"1\":\n                self.play_all_quizzes()\n                continue\n\n            if choice == \"2\":\n                self.add_quiz()\n                continue\n\n            if choice == \"3\":\n                self.show_quiz_list()\n                continue\n\n            if choice == \"4\":\n                self.delete_quiz()\n                continue\n\n            if choice == \"5\":\n                self.show_best_score()\n                continue\n\n            if choice == \"6\":\n                self.show_score_history()\n                continue\n\n            if self.is_exit_choice(choice):\n                print(\"프로그램을 종료합니다.\")\n                break\n\n    # 현재 게임 상태를 저장용 딕셔너리로 만든다.\n    def to_dict(self):\n        return {\n            \"quizzes\": [\n                {\n                    \"question\": quiz.question,\n                    \"choices\": quiz.choices,\n                    \"answer\": quiz.answer,\n                    \"hint\": quiz.hint,\n                }\n                for quiz in self.quizzes\n            ],\n            \"best_score\": self.best_score,\n            \"score_history\": self.score_history,\n        }\n\n    # 현재 게임 상태를 state.json 파일에 저장한다.\n    def save_to_file(self):\n        try:\n            with open(\"state.json\", \"w\", encoding=\"utf-8\") as file:\n                json.dump(self.to_dict(), file, ensure_ascii=False, indent=2)\n        except OSError:\n            print(\"저장 파일을 쓰는 중 오류가 발생했습니다.\")\n\n    # state.json 파일에서 게임 상태를 읽어 객체로 복원한다.\n    @classmethod\n    def load_from_file(cls):\n        try:\n            with open(\"state.json\", \"r\", encoding=\"utf-8\") as file:\n                data = json.load(file)\n        except FileNotFoundError:\n            print(\"저장 파일이 없어 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n        except json.JSONDecodeError:\n            print(\"저장 파일이 손상되어 기본 퀴즈로 복구합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n        except OSError:\n            print(\"저장 파일을 읽는 중 오류가 발생해 기본 퀴즈로 시작합니다.\")\n            return cls(cls.make_default_quizzes(), 0, [])\n\n        quizzes = [\n            Quiz(\n                item[\"question\"],\n                item[\"choices\"],\n                item[\"answer\"],\n                item.get(\"hint\", \"아직 등록된 힌트가 없습니다.\"),\n            )\n            for item in data[\"quizzes\"]\n        ]\n        return cls(quizzes, data[\"best_score\"], data.get(\"score_history\", []))\n\n\ndef main():\n    game = None\n\n    try:\n        # 저장 파일을 우선 읽고, 없으면 기본 퀴즈로 게임을 시작한다.\n        game = QuizGame.load_from_file()\n\n        # 객체에 저장된 값과 메서드 동작을 확인한다.\n        print(\"퀴즈 게임 시작\")\n        print(game.quizzes)\n        print(game.best_score)\n        print(game.to_dict())\n        game.save_to_file()\n        loaded_game = QuizGame.load_from_file()\n        print(loaded_game.to_dict())\n\n        if game.has_quizzes():\n            quiz1 = game.quizzes[0]\n            quiz1.show()\n            print(quiz1.check_answer(2))\n            print(quiz1.check_answer(1))\n        else:\n            print(\"확인할 기본 퀴즈가 없습니다.\")\n\n        # 메뉴 반복 흐름도 게임 객체에 맡긴다.\n        game.run_menu()\n    except (KeyboardInterrupt, EOFError):\n        print(\"\\n입력이 중단되어 프로그램을 안전하게 종료합니다.\")\n        if game is not None:\n            game.save_to_file()\n\n\nmain()\n```\n\n### 날짜/시간 포함 게임 기록 저장 확인\n\n```bash\n$ printf '1\\n1\\nn\\n2\\n6\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x1086a17f0>, <__main__.Quiz object at 0x103c99f90>, <__main__.Quiz object at 0x103c9b750>, <__main__.Quiz object at 0x108668770>, <__main__.Quiz object at 0x10866ab10>]\n0\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0, 'score_history': []}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 0, 'score_history': []}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 몇 문제를 풀까요? (1-5): 1문제를 랜덤으로 출제합니다.\n같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\nif\nfor\nprint\ninput\n2\n힌트를 볼까요? (y/n): 정답 번호: 정답입니다.\n최고 점수가 갱신되었습니다.\n이번 점수: 1\n현재 최고 점수: 1\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 날짜/시간 포함 게임 기록 저장 확인\n\n```bash\n$ python3 -m json.tool state.json\n{\n    \"quizzes\": [\n        {\n            \"question\": \"Python에서 문자열을 저장하는 자료형은?\",\n            \"choices\": [\n                \"int\",\n                \"str\",\n                \"bool\",\n                \"list\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"문자열은 따옴표로 감싸 자주 표현합니다.\"\n        },\n        {\n            \"question\": \"Python에서 3 > 1의 결과는 무엇인가?\",\n            \"choices\": [\n                \"0\",\n                \"False\",\n                \"True\",\n                \"None\"\n            ],\n            \"answer\": 3,\n            \"hint\": \"비교 연산의 결과는 참 또는 거짓입니다.\"\n        },\n        {\n            \"question\": \"여러 값을 순서대로 저장하는 자료형은 무엇인가?\",\n            \"choices\": [\n                \"dict\",\n                \"list\",\n                \"str\",\n                \"int\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"대괄호 [] 로 만드는 자료형을 떠올려보세요.\"\n        },\n        {\n            \"question\": \"같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?\",\n            \"choices\": [\n                \"if\",\n                \"for\",\n                \"print\",\n                \"input\"\n            ],\n            \"answer\": 2,\n            \"hint\": \"반복문 문법을 고르면 됩니다.\"\n        },\n        {\n            \"question\": \"함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?\",\n            \"choices\": [\n                \"break\",\n                \"class\",\n                \"return\",\n                \"import\"\n            ],\n            \"answer\": 3,\n            \"hint\": \"함수 바깥으로 값을 다시 보내는 단어입니다.\"\n        }\n    ],\n    \"best_score\": 1,\n    \"score_history\": [\n        {\n            \"played_at\": \"2026-08-06T19:55:55\",\n            \"total_questions\": 1,\n            \"score\": 1\n        }\n    ]\n}\n```\n\n### 날짜/시간 포함 게임 기록 저장 확인\n\n```bash\n$ printf '6\\n7\\n' | python3 main.py\n퀴즈 게임 시작\n[<__main__.Quiz object at 0x1083a17f0>, <__main__.Quiz object at 0x103971f90>, <__main__.Quiz object at 0x103973750>, <__main__.Quiz object at 0x1083688a0>, <__main__.Quiz object at 0x10836ac40>]\n1\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 1, 'score_history': [{'played_at': '2026-08-06T19:55:55', 'total_questions': 1, 'score': 1}]}\n{'quizzes': [{'question': 'Python에서 문자열을 저장하는 자료형은?', 'choices': ['int', 'str', 'bool', 'list'], 'answer': 2, 'hint': '문자열은 따옴표로 감싸 자주 표현합니다.'}, {'question': 'Python에서 3 > 1의 결과는 무엇인가?', 'choices': ['0', 'False', 'True', 'None'], 'answer': 3, 'hint': '비교 연산의 결과는 참 또는 거짓입니다.'}, {'question': '여러 값을 순서대로 저장하는 자료형은 무엇인가?', 'choices': ['dict', 'list', 'str', 'int'], 'answer': 2, 'hint': '대괄호 [] 로 만드는 자료형을 떠올려보세요.'}, {'question': '같은 동작을 여러 번 반복할 때 주로 사용하는 문법은?', 'choices': ['if', 'for', 'print', 'input'], 'answer': 2, 'hint': '반복문 문법을 고르면 됩니다.'}, {'question': '함수 실행 뒤 결과 값을 돌려줄 때 사용하는 키워드는?', 'choices': ['break', 'class', 'return', 'import'], 'answer': 3, 'hint': '함수 바깥으로 값을 다시 보내는 단어입니다.'}], 'best_score': 1, 'score_history': [{'played_at': '2026-08-06T19:55:55', 'total_questions': 1, 'score': 1}]}\nPython에서 문자열을 저장하는 자료형은?\nint\nstr\nbool\nlist\n2\nTrue\nFalse\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 플레이 기록\n7. 종료\n선택: 플레이 기록\n1. 2026-08-06T19:55:55 | 1문제 | 1점\n1. 퀴즈 풀기\n2. 퀴즈 추가\n3. 퀴즈 목록\n4. 퀴즈 삭제\n5. 점수 확인\n6. 플레이 기록\n7. 종료\n선택: 프로그램을 종료합니다.\n```\n\n### 플레이 기록 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 status --short\n M work/main.py\n M work/state.json\n```\n\n### 플레이 기록 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 add -A\n```\n\n### 플레이 기록 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 commit -m \"Feat: 플레이 기록 저장 기능 추가\"\n[codex/e1-2-history-rebuild 9d2cee1] Feat: 플레이 기록 저장 기능 추가\n 2 files changed, 47 insertions(+), 10 deletions(-)\n```\n\n### 플레이 기록 기능 완성 후 commit 기록\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 log --oneline --graph --decorate -n 3\n* 9d2cee1 (HEAD -> codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n```\n\n\n## Chapter 7. README, Git 로그, clone/pull 제출 증거\n\n### 테마\n\n- README 필수 항목 작성\n- git log --oneline --graph와 커밋 수 확인\n- GitHub 저장소 연결과 업로드 확인\n- 별도 디렉터리에 clone\n- clone 변경 push 후 원래 작업 폴더에서 pull\n- 개발 환경과 실행 화면 증거 정리\n\n### README 필수 항목 작성\n\n```bash\n$ sed -n '1,80p' README.md\n# E1-2 파이썬 이해하기\n\n## 프로젝트 개요\n\n터미널에서 실행하는 Python 퀴즈 게임입니다.  \n메뉴를 통해 퀴즈 풀기, 퀴즈 추가, 퀴즈 목록 확인, 퀴즈 삭제, 최고 점수 확인, 플레이 기록 확인을 수행할 수 있습니다.\n\n## 퀴즈 주제 선정 이유\n\nPython 기초 문법을 배우는 과제이기 때문에, 퀴즈 주제도 Python 기본 개념으로 맞췄습니다.  \n문자열, 불리언, 리스트, 반복문, 반환값처럼 과제에서 직접 설명해야 하는 개념을 퀴즈 문제로 다시 확인할 수 있게 구성했습니다.\n\n## 실행 방법\n\n과제 작업 루트에서 아래 명령으로 실행합니다.\n\n```bash\ncd /Users/hskim/Projects/codyssey/artifacts/e1-2/work\npython3 main.py\n```\n\n## 기능 목록\n\n- 메뉴 출력과 종료\n- 빈 입력, 문자 입력, 범위 밖 숫자 입력 예외 처리\n- `Quiz` 클래스 기반 개별 퀴즈 표현\n- `QuizGame` 클래스 기반 전체 게임 관리\n- 퀴즈 풀기\n- 퀴즈 추가\n- 퀴즈 목록 보기\n- 퀴즈 삭제\n- 최고 점수 확인\n- 플레이 기록 저장과 조회\n- `state.json` 저장/복구\n- 랜덤 출제\n- 문제 수 선택\n- 힌트 사용과 점수 차감\n\n## 파일 구조\n\n```text\nartifacts/e1-2/\n├── README.md\n├── state.json\n├── logs/\n│   └── practice.jsonl\n├── render/\n│   └── latest/\n│       ├── README.md\n│       └── presentation.html\n└── work/\n    ├── main.py\n    └── state.json\n```\n\n## 데이터 파일 설명\n\n이 과제의 실행 데이터는 기본적으로 `artifacts/e1-2/work/state.json`에 UTF-8로 저장됩니다.\n\n주요 필드는 다음과 같습니다.\n\n- `quizzes`: 문제, 선택지, 정답, 힌트 목록\n- `best_score`: 현재 최고 점수\n- `score_history`: 날짜/시간, 푼 문제 수, 점수 기록 배열\n```\n\n### README 필수 항목 작성\n\n```bash\n$ rg -n '^## ' README.md\n3:## 프로젝트 개요\n8:## 퀴즈 주제 선정 이유\n13:## 실행 방법\n22:## 기능 목록\n39:## 파일 구조\n56:## 데이터 파일 설명\n```\n\n### git log --oneline --graph와 커밋 수 확인\n\n```bash\n$ git rev-list --count --all\n12\n```\n\n### git log --oneline --graph와 커밋 수 확인\n\n```bash\n$ git log --oneline --graph --decorate --all --max-count=20\n* a12d1b7 (HEAD -> main, origin/main) Docs: clone 저장소 검증 메모 추가\n*   302870a Merge branch 'codex/e1-2-readme-evidence'\n|\\  \n| * 443cb6f (codex/e1-2-readme-evidence) Docs: README와 제출 증거 정리\n|/  \n* 15a283c Feat: 퀴즈 게임 기본 구조와 저장 흐름 구현\n* 9d2cee1 (codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현\n```\n\n### GitHub 저장소 연결과 업로드 확인\n\n```bash\n$ git remote -v\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (fetch)\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (push)\n```\n\n### GitHub 저장소 연결과 업로드 확인\n\n```bash\n$ gh auth status\ngithub.com\n  X Failed to log in to github.com account Logan-kim-the-philosopher (default)\n  - Active account: true\n  - The token in default is invalid.\n  - To re-authenticate, run: gh auth login -h github.com\n  - To forget about this account, run: gh auth logout -h github.com -u Logan-kim-the-philosopher\n```\n\n### 별도 디렉터리에 clone\n\n```bash\n$ git clone /private/tmp/e1-2-remote-cuFnUC.git /private/tmp/e1-2-clone-BOihMa\nCloning into '/private/tmp/e1-2-clone-BOihMa'...\ndone.\n```\n\n### 별도 디렉터리에 clone\n\n```bash\n$ git -C /private/tmp/e1-2-clone-BOihMa remote -v\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (fetch)\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (push)\n```\n\n### clone 변경 push 후 원래 작업 폴더에서 pull\n\n```bash\n$ git -C /private/tmp/e1-2-clone-BOihMa commit -m \"Docs: clone 저장소 검증 메모 추가\" && git -C /private/tmp/e1-2-clone-BOihMa push origin main\n[main a12d1b7] Docs: clone 저장소 검증 메모 추가\n 1 file changed, 2 insertions(+)\nTo /private/tmp/e1-2-remote-cuFnUC.git\n   302870a..a12d1b7  main -> main\n```\n\n### clone 변경 push 후 원래 작업 폴더에서 pull\n\n```bash\n$ git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 pull origin main\nFrom /private/tmp/e1-2-remote-cuFnUC\n * branch            main       -> FETCH_HEAD\n   302870a..a12d1b7  main       -> origin/main\nUpdating 302870a..a12d1b7\nFast-forward\n README.md | 2 ++\n 1 file changed, 2 insertions(+)\n```\n\n### clone 변경 push 후 원래 작업 폴더에서 pull\n\n```bash\n$ rg -n \"clone 저장소\" README.md\n89:- clone 저장소에서 2026-08-06 pull 실습 메모를 추가했습니다.\n```\n\n### 개발 환경과 실행 화면 증거 정리\n\n```bash\n$ python3 --version\nPython 3.14.6\n```\n\n### 개발 환경과 실행 화면 증거 정리\n\n```bash\n$ git --version\ngit version 2.55.0\n```\n\n### 개발 환경과 실행 화면 증거 정리\n\n```bash\n$ cd /Users/hskim/Projects/codyssey/artifacts/e1-2/work && pwd && ls -la\n/Users/hskim/Projects/codyssey/artifacts/e1-2/work\ntotal 64\ndrwxr-xr-x@ 8 hskim  staff    256 Aug  6 20:05 .\ndrwxr-xr-x@ 9 hskim  staff    288 Aug  6 22:17 ..\ndrwxr-xr-x@ 3 hskim  staff     96 Aug  6 20:05 __pycache__\n-rw-r--r--@ 1 hskim  staff  14378 Aug  6 20:00 main.py\n-rw-r--r--@ 1 hskim  staff    234 Aug  6 15:16 state.before-5-quizzes.json\n-rw-r--r--@ 1 hskim  staff   1550 Aug  6 20:01 state.json\n-rw-r--r--@ 1 hskim  staff   1066 Aug  6 18:54 state.json.bak\n-rw-r--r--@ 1 hskim  staff   1356 Aug  6 19:39 state.pre_hint_backup.json\n```\n\n### 증빙\n\n- artifacts/e1-2/README.md\n### 증빙\n\n- artifacts/e1-2/README.md",
          "cwd": "artifacts/e1-2"
        },
        {
          "theme": "README 필수 항목 작성",
          "command": "ls -la README.md",
          "output": "-rw-r--r--@ 1 hskim  staff  397412 Aug  6 22:21 /Users/hskim/Projects/codyssey/artifacts/e1-2/README.md",
          "cwd": "artifacts/e1-2"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "README 필수 항목 6개를 저장소 루트 README에 정리했다."
      ],
      "interpretation": "README.md에서 과제 요구 6개 항목 제목이 모두 확인되며, 실행 경로와 state.json 설명도 함께 포함됐다.",
      "evidence": "README.md section headers와 본문 출력으로 제출용 README 구성 확인",
      "artifacts": [
        "artifacts/e1-2/README.md"
      ]
    },
    {
      "id": "log-33",
      "at": "2026-08-06T13:21:04.568Z",
      "chapter": "README, Git 로그, clone/pull 제출 증거",
      "theme": "git log --oneline --graph와 커밋 수 확인",
      "action": "저장소 전체 커밋 수와 그래프 로그를 확인했다.",
      "purpose": "최소 10개 이상의 의미 있는 커밋이 존재하는지 실제 Git 출력으로 검증한다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "git log --oneline --graph와 커밋 수 확인",
          "command": "git rev-list --count --all",
          "output": "12",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "git log --oneline --graph와 커밋 수 확인",
          "command": "git log --oneline --graph --decorate --all --max-count=20",
          "output": "* a12d1b7 (HEAD -> main, origin/main) Docs: clone 저장소 검증 메모 추가\n*   302870a Merge branch 'codex/e1-2-readme-evidence'\n|\\  \n| * 443cb6f (codex/e1-2-readme-evidence) Docs: README와 제출 증거 정리\n|/  \n* 15a283c Feat: 퀴즈 게임 기본 구조와 저장 흐름 구현\n* 9d2cee1 (codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "git log --oneline --graph와 커밋 수 확인",
          "command": "git rev-list --count --all",
          "output": "12",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "git log --oneline --graph와 커밋 수 확인",
          "command": "git log --oneline --graph --decorate --all --max-count=20",
          "output": "* a12d1b7 (HEAD -> main, origin/main) Docs: clone 저장소 검증 메모 추가\n*   302870a Merge branch 'codex/e1-2-readme-evidence'\n|\\  \n| * 443cb6f (codex/e1-2-readme-evidence) Docs: README와 제출 증거 정리\n|/  \n* 15a283c Feat: 퀴즈 게임 기본 구조와 저장 흐름 구현\n* 9d2cee1 (codex/e1-2-history-rebuild) Feat: 플레이 기록 저장 기능 추가\n* 599708f Feat: 랜덤 출제와 힌트 기능 추가\n* baeba2e Feat: 퀴즈 추가 삭제와 점수 메뉴 구현\n* 9a48940 Feat: 퀴즈 플레이와 점수 계산 구현\n* a5d5759 Feat: state.json 저장과 복구 처리 추가\n* 054e3c8 Refactor: QuizGame으로 메뉴 책임 분리\n* 7e2694f Feat: Quiz 클래스와 정답 판정 구현\n* bfc04cf Feat: 반복 메뉴와 종료 흐름 구현",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "저장소 전체 기준 12개 커밋과 그래프 로그를 확인했다."
      ],
      "interpretation": "rev-list --count --all 결과가 12이므로 최소 10개 커밋 조건을 만족한다. graph 출력에는 feature commit, docs commit, branch merge 기록이 함께 나타난다.",
      "evidence": "git rev-list --count --all = 12, git log --oneline --graph --decorate --all 출력",
      "artifacts": []
    },
    {
      "id": "log-34",
      "at": "2026-08-06T13:21:04.601Z",
      "chapter": "README, Git 로그, clone/pull 제출 증거",
      "theme": "GitHub 저장소 연결과 업로드 확인",
      "action": "원격 저장소 연결 상태와 GitHub 인증 상태를 확인했다.",
      "purpose": "실제 GitHub 업로드 가능 여부를 원격 설정과 인증 출력으로 판단한다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "GitHub 저장소 연결과 업로드 확인",
          "command": "git remote -v",
          "output": "origin\t/private/tmp/e1-2-remote-cuFnUC.git (fetch)\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (push)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "GitHub 저장소 연결과 업로드 확인",
          "command": "gh auth status",
          "output": "github.com\n  X Failed to log in to github.com account Logan-kim-the-philosopher (default)\n  - Active account: true\n  - The token in default is invalid.\n  - To re-authenticate, run: gh auth login -h github.com\n  - To forget about this account, run: gh auth logout -h github.com -u Logan-kim-the-philosopher",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "GitHub 저장소 연결과 업로드 확인",
          "command": "git remote -v",
          "output": "origin\t/private/tmp/e1-2-remote-cuFnUC.git (fetch)\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (push)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "GitHub 저장소 연결과 업로드 확인",
          "command": "gh auth status",
          "output": "github.com\n  X Failed to log in to github.com account Logan-kim-the-philosopher (default)\n  - Active account: true\n  - The token in default is invalid.\n  - To re-authenticate, run: gh auth login -h github.com\n  - To forget about this account, run: gh auth logout -h github.com -u Logan-kim-the-philosopher",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "로컬 origin 원격은 연결됐지만 GitHub CLI 인증 토큰은 만료되어 업로드는 보류 상태다."
      ],
      "interpretation": "현재 저장소는 로컬 bare 원격으로 clone/pull 실습은 가능하지만, gh auth status 출력상 GitHub 토큰이 invalid 상태라 실제 GitHub 저장소 생성/업로드는 지금 바로 진행할 수 없다.",
      "evidence": "git remote -v, gh auth status 출력",
      "artifacts": []
    },
    {
      "id": "log-35",
      "at": "2026-08-06T13:21:04.634Z",
      "chapter": "README, Git 로그, clone/pull 제출 증거",
      "theme": "별도 디렉터리에 clone",
      "action": "과제 저장소를 별도 임시 디렉터리에 clone했다.",
      "purpose": "clone 명령으로 저장소 복제와 원격 연결이 실제로 되는지 확인한다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "별도 디렉터리에 clone",
          "command": "git clone /private/tmp/e1-2-remote-cuFnUC.git /private/tmp/e1-2-clone-BOihMa",
          "output": "Cloning into '/private/tmp/e1-2-clone-BOihMa'...\ndone.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "별도 디렉터리에 clone",
          "command": "git -C /private/tmp/e1-2-clone-BOihMa remote -v",
          "output": "origin\t/private/tmp/e1-2-remote-cuFnUC.git (fetch)\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (push)",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "별도 디렉터리에 clone",
          "command": "git clone /private/tmp/e1-2-remote-cuFnUC.git /private/tmp/e1-2-clone-BOihMa",
          "output": "Cloning into '/private/tmp/e1-2-clone-BOihMa'...\ndone.",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "별도 디렉터리에 clone",
          "command": "git -C /private/tmp/e1-2-clone-BOihMa remote -v",
          "output": "origin\t/private/tmp/e1-2-remote-cuFnUC.git (fetch)\norigin\t/private/tmp/e1-2-remote-cuFnUC.git (push)",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "별도 디렉터리 clone과 origin 연결을 확인했다."
      ],
      "interpretation": "clone 명령이 정상 완료됐고, 복제본 안의 origin도 같은 원격을 가리킨다. 즉 clone 요구사항은 실제 Git 명령으로 수행됐다.",
      "evidence": "git clone 완료 출력과 복제 저장소 remote -v 출력",
      "artifacts": []
    },
    {
      "id": "log-36",
      "at": "2026-08-06T13:21:04.673Z",
      "chapter": "README, Git 로그, clone/pull 제출 증거",
      "theme": "clone 변경 push 후 원래 작업 폴더에서 pull",
      "action": "복제 저장소에서 README를 수정해 push하고 원래 저장소에서 pull로 반영했다.",
      "purpose": "push와 pull이 실제로 동작하고 변경사항이 원래 작업 폴더에 반영되는지 검증한다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "clone 변경 push 후 원래 작업 폴더에서 pull",
          "command": "git -C /private/tmp/e1-2-clone-BOihMa commit -m \"Docs: clone 저장소 검증 메모 추가\" && git -C /private/tmp/e1-2-clone-BOihMa push origin main",
          "output": "[main a12d1b7] Docs: clone 저장소 검증 메모 추가\n 1 file changed, 2 insertions(+)\nTo /private/tmp/e1-2-remote-cuFnUC.git\n   302870a..a12d1b7  main -> main",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "clone 변경 push 후 원래 작업 폴더에서 pull",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 pull origin main",
          "output": "From /private/tmp/e1-2-remote-cuFnUC\n * branch            main       -> FETCH_HEAD\n   302870a..a12d1b7  main       -> origin/main\nUpdating 302870a..a12d1b7\nFast-forward\n README.md | 2 ++\n 1 file changed, 2 insertions(+)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "clone 변경 push 후 원래 작업 폴더에서 pull",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 diff HEAD~1 HEAD -- README.md",
          "output": "diff --git a/README.md b/README.md\nindex 027d04d..beacb8d 100644\n--- a/README.md\n+++ b/README.md\n@@ -85,3 +85,5 @@ artifacts/e1-2/\n   ]\n }\n ```\n+\n+- clone 저장소에서 2026-08-06 pull 실습 메모를 추가했습니다.",
          "cwd": "artifacts/e1-2"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "clone 변경 push 후 원래 작업 폴더에서 pull",
          "command": "git -C /private/tmp/e1-2-clone-BOihMa commit -m \"Docs: clone 저장소 검증 메모 추가\" && git -C /private/tmp/e1-2-clone-BOihMa push origin main",
          "output": "[main a12d1b7] Docs: clone 저장소 검증 메모 추가\n 1 file changed, 2 insertions(+)\nTo /private/tmp/e1-2-remote-cuFnUC.git\n   302870a..a12d1b7  main -> main",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "clone 변경 push 후 원래 작업 폴더에서 pull",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 pull origin main",
          "output": "From /private/tmp/e1-2-remote-cuFnUC\n * branch            main       -> FETCH_HEAD\n   302870a..a12d1b7  main       -> origin/main\nUpdating 302870a..a12d1b7\nFast-forward\n README.md | 2 ++\n 1 file changed, 2 insertions(+)",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "clone 변경 push 후 원래 작업 폴더에서 pull",
          "command": "git -C /Users/hskim/Projects/codyssey/artifacts/e1-2 diff HEAD~1 HEAD -- README.md",
          "output": "diff --git a/README.md b/README.md\nindex 027d04d..beacb8d 100644\n--- a/README.md\n+++ b/README.md\n@@ -85,3 +85,5 @@ artifacts/e1-2/\n   ]\n }\n ```\n+\n+- clone 저장소에서 2026-08-06 pull 실습 메모를 추가했습니다.",
          "cwd": "artifacts/e1-2"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "clone 저장소의 README 변경을 push한 뒤 원래 저장소에서 pull로 반영했다."
      ],
      "interpretation": "복제 저장소에서 commit/push가 먼저 성공했고, 원래 저장소의 pull이 fast-forward로 진행됐다. 마지막 grep 결과로 README 한 줄이 실제 반영된 것도 확인된다.",
      "evidence": "clone commit/push 출력, 원래 저장소 pull 출력, README 반영 grep 결과",
      "artifacts": [
        "artifacts/e1-2/README.md"
      ]
    },
    {
      "id": "log-37",
      "at": "2026-08-06T13:21:04.712Z",
      "chapter": "README, Git 로그, clone/pull 제출 증거",
      "theme": "개발 환경과 실행 화면 증거 정리",
      "action": "Python 버전, Git 버전, 실제 작업 폴더 구성을 확인했다.",
      "purpose": "개발 환경과 제출 대상 파일 구성을 실제 터미널 출력으로 남긴다.",
      "workDir": "artifacts/e1-2/work",
      "frames": [
        {
          "type": "terminal-step",
          "theme": "개발 환경과 실행 화면 증거 정리",
          "command": "python3 --version",
          "output": "Python 3.14.6",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "개발 환경과 실행 화면 증거 정리",
          "command": "git --version",
          "output": "git version 2.55.0",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "type": "terminal-step",
          "theme": "개발 환경과 실행 화면 증거 정리",
          "command": "cd /Users/hskim/Projects/codyssey/artifacts/e1-2/work && pwd && ls -la",
          "output": "/Users/hskim/Projects/codyssey/artifacts/e1-2/work\ntotal 64\ndrwxr-xr-x@ 8 hskim  staff    256 Aug  6 20:05 .\ndrwxr-xr-x@ 9 hskim  staff    288 Aug  6 22:17 ..\ndrwxr-xr-x@ 3 hskim  staff     96 Aug  6 20:05 __pycache__\n-rw-r--r--@ 1 hskim  staff  14378 Aug  6 20:00 main.py\n-rw-r--r--@ 1 hskim  staff    234 Aug  6 15:16 state.before-5-quizzes.json\n-rw-r--r--@ 1 hskim  staff   1550 Aug  6 20:01 state.json\n-rw-r--r--@ 1 hskim  staff   1066 Aug  6 18:54 state.json.bak\n-rw-r--r--@ 1 hskim  staff   1356 Aug  6 19:39 state.pre_hint_backup.json",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "editorChanges": [],
      "terminalSteps": [
        {
          "theme": "개발 환경과 실행 화면 증거 정리",
          "command": "python3 --version",
          "output": "Python 3.14.6",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "개발 환경과 실행 화면 증거 정리",
          "command": "git --version",
          "output": "git version 2.55.0",
          "cwd": "artifacts/e1-2/work"
        },
        {
          "theme": "개발 환경과 실행 화면 증거 정리",
          "command": "cd /Users/hskim/Projects/codyssey/artifacts/e1-2/work && pwd && ls -la",
          "output": "/Users/hskim/Projects/codyssey/artifacts/e1-2/work\ntotal 64\ndrwxr-xr-x@ 8 hskim  staff    256 Aug  6 20:05 .\ndrwxr-xr-x@ 9 hskim  staff    288 Aug  6 22:17 ..\ndrwxr-xr-x@ 3 hskim  staff     96 Aug  6 20:05 __pycache__\n-rw-r--r--@ 1 hskim  staff  14378 Aug  6 20:00 main.py\n-rw-r--r--@ 1 hskim  staff    234 Aug  6 15:16 state.before-5-quizzes.json\n-rw-r--r--@ 1 hskim  staff   1550 Aug  6 20:01 state.json\n-rw-r--r--@ 1 hskim  staff   1066 Aug  6 18:54 state.json.bak\n-rw-r--r--@ 1 hskim  staff   1356 Aug  6 19:39 state.pre_hint_backup.json",
          "cwd": "artifacts/e1-2/work"
        }
      ],
      "commands": [],
      "terminalOutputs": [],
      "outputs": [
        "Python 3.14.6, Git 2.55.0, work 폴더 파일 구성을 확인했다."
      ],
      "interpretation": "Python 3.10 이상 요구를 충족하고, work 경로에 main.py와 state.json 계열 파일이 존재해 제출 실행 폴더도 분명하다.",
      "evidence": "python3 --version, git --version, work 디렉터리 pwd/ls 출력",
      "artifacts": []
    }
  ],
  "github": {
    "owner": null,
    "repo": null,
    "pagesUrl": null
  },
  "paths": {
    "workDir": "artifacts/e1-2/work",
    "latestReadme": "artifacts/e1-2/render/latest/README.md",
    "latestHtml": "artifacts/e1-2/render/latest/presentation.html",
    "exportReadme": "submissions/E1-2/README.md",
    "exportHtml": "docs/E1-2/index.html"
  },
  "git": {
    "repoRoot": "/Users/hskim/Projects/codyssey/artifacts/e1-2",
    "branch": "main",
    "head": "a12d1b7a2dfaaf17da38126b1c8424ef4f9489a2"
  },
  "trackedFiles": [
    "submissions/E1-2",
    "submissions/E1-2/README.md",
    "docs/E1-2/index.html",
    "README.md",
    "docs/index.html"
  ],
  "history": [
    {
      "at": "2026-08-04T05:52:26.266Z",
      "event": "started",
      "runId": "e1-2-20260804-055226Z"
    },
    {
      "at": "2026-08-04T05:53:10.832Z",
      "event": "bonus_selected",
      "items": [
        "랜덤 출제",
        "문제 수 선택",
        "힌트 기능",
        "퀴즈 삭제 기능",
        "점수 기록 히스토리"
      ]
    },
    {
      "at": "2026-08-04T09:14:03.257Z",
      "event": "plan_replaced",
      "chapters": 8
    },
    {
      "at": "2026-08-05T03:57:35.956Z",
      "event": "practice_logged",
      "id": "log-1"
    },
    {
      "at": "2026-08-05T04:09:12.152Z",
      "event": "practice_logged",
      "id": "log-2"
    },
    {
      "at": "2026-08-05T04:22:25.168Z",
      "event": "practice_logged",
      "id": "log-3"
    },
    {
      "at": "2026-08-05T04:22:37.111Z",
      "event": "practice_logged",
      "id": "log-4"
    },
    {
      "at": "2026-08-05T04:36:41.569Z",
      "event": "practice_logged",
      "id": "log-5"
    },
    {
      "at": "2026-08-05T04:39:18.989Z",
      "event": "practice_logged",
      "id": "log-6"
    },
    {
      "at": "2026-08-05T05:26:38.619Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T05:31:14.990Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T05:33:23.811Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T05:41:17.418Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T05:45:21.097Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T05:45:52.223Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T05:47:50.567Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T05:51:00.579Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T05:51:53.292Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T06:13:45.592Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T06:17:42.451Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T06:28:31.395Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T06:34:15.760Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T06:35:39.870Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T06:37:15.819Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T06:40:37.851Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T06:40:59.226Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T07:01:21.126Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T07:06:36.484Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T07:09:43.493Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-05T07:14:59.843Z",
      "event": "practice_logged",
      "id": "log-7"
    },
    {
      "at": "2026-08-05T07:36:09.474Z",
      "event": "practice_logged",
      "id": "log-8"
    },
    {
      "at": "2026-08-05T09:49:35.774Z",
      "event": "practice_logged",
      "id": "log-9"
    },
    {
      "at": "2026-08-05T11:18:13.009Z",
      "event": "practice_logged",
      "id": "log-10"
    },
    {
      "at": "2026-08-05T11:22:08.849Z",
      "event": "practice_logged",
      "id": "log-11"
    },
    {
      "at": "2026-08-05T12:52:46.210Z",
      "event": "practice_logged",
      "id": "log-12"
    },
    {
      "at": "2026-08-05T13:15:26.077Z",
      "event": "practice_logged",
      "id": "log-13"
    },
    {
      "at": "2026-08-06T05:16:34.145Z",
      "event": "practice_logged",
      "id": "log-14"
    },
    {
      "at": "2026-08-06T05:48:40.449Z",
      "event": "practice_logged",
      "id": "log-15"
    },
    {
      "at": "2026-08-06T05:56:42.500Z",
      "event": "practice_logged",
      "id": "log-16"
    },
    {
      "at": "2026-08-06T06:19:52.281Z",
      "event": "practice_logged",
      "id": "log-17"
    },
    {
      "at": "2026-08-06T06:43:09.503Z",
      "event": "practice_logged",
      "id": "log-18"
    },
    {
      "at": "2026-08-06T06:45:18.665Z",
      "event": "practice_logged",
      "id": "log-19"
    },
    {
      "at": "2026-08-06T07:01:36.886Z",
      "event": "practice_logged",
      "id": "log-20"
    },
    {
      "at": "2026-08-06T07:16:22.935Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T07:18:30.824Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T07:19:12.849Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T09:39:41.492Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T09:41:05.876Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T11:07:46.916Z",
      "event": "practice_logged",
      "id": "log-21"
    },
    {
      "at": "2026-08-06T11:07:46.948Z",
      "event": "practice_logged",
      "id": "log-22"
    },
    {
      "at": "2026-08-06T11:07:46.979Z",
      "event": "practice_logged",
      "id": "log-23"
    },
    {
      "at": "2026-08-06T12:02:05.120Z",
      "event": "plan_replaced",
      "chapters": 8
    },
    {
      "at": "2026-08-06T12:20:14.878Z",
      "event": "practice_logged",
      "id": "log-24"
    },
    {
      "at": "2026-08-06T12:34:24.004Z",
      "event": "logged",
      "logId": "log-25"
    },
    {
      "at": "2026-08-06T12:34:24.107Z",
      "event": "logged",
      "logId": "log-26"
    },
    {
      "at": "2026-08-06T12:34:24.181Z",
      "event": "logged",
      "logId": "log-27"
    },
    {
      "at": "2026-08-06T12:34:24.233Z",
      "event": "logged",
      "logId": "log-28"
    },
    {
      "at": "2026-08-06T12:34:24.362Z",
      "event": "logged",
      "logId": "log-29"
    },
    {
      "at": "2026-08-06T12:34:24.478Z",
      "event": "logged",
      "logId": "log-30"
    },
    {
      "at": "2026-08-06T12:34:24.584Z",
      "event": "logged",
      "logId": "log-31"
    },
    {
      "at": "2026-08-06T12:44:16.594Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T12:50:11.892Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T12:54:53.459Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T13:08:18.842Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T13:12:27.573Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T13:21:04.535Z",
      "event": "practice_logged",
      "id": "log-32"
    },
    {
      "at": "2026-08-06T13:21:04.569Z",
      "event": "practice_logged",
      "id": "log-33"
    },
    {
      "at": "2026-08-06T13:21:04.602Z",
      "event": "practice_logged",
      "id": "log-34"
    },
    {
      "at": "2026-08-06T13:21:04.634Z",
      "event": "practice_logged",
      "id": "log-35"
    },
    {
      "at": "2026-08-06T13:21:04.674Z",
      "event": "practice_logged",
      "id": "log-36"
    },
    {
      "at": "2026-08-06T13:21:04.712Z",
      "event": "practice_logged",
      "id": "log-37"
    },
    {
      "at": "2026-08-06T13:21:19.941Z",
      "event": "rendered"
    },
    {
      "at": "2026-08-06T13:21:21.895Z",
      "event": "exported"
    },
    {
      "at": "2026-08-06T13:23:07.631Z",
      "event": "exported"
    }
  ],
  "selectedBonuses": [
    "랜덤 출제",
    "문제 수 선택",
    "힌트 기능",
    "퀴즈 삭제 기능",
    "점수 기록 히스토리"
  ]
}
