services:
  web:
    image: nginx:alpine
    ports:
      - "8082:80"
    volumes:
      - ./web:/usr/share/nginx/html:ro
    depends_on:
      - helper
  helper:
    image: ubuntu:latest
    command: sleep infinity
